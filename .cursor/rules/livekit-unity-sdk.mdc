---
description: LiveKit Unity SDK 版本适配注意事项
globs:
  - "unity/**/*.cs"
alwaysApply: false
status: ratified
status_note: "条目均来自已踩坑总结 (Play/Stop identity 抢占 / ICE 残留 / PublishTrack NRE)。按 sprint0_preflight.md §6.4 标 ratified。"
last_reviewed: 2026-04-22
---

# LiveKit Unity SDK 适配规则

## 版本管理

1. SDK 通过 UPM git URL 安装（`https://github.com/livekit/client-sdk-unity.git`），拉取的是最新 main 分支。
2. SDK 的 README 示例代码可能落后于实际 API — 生成 C# 代码前必须读实际 SDK 源码确认签名。
3. 实际源码位置: `Library/PackageCache/io.livekit.livekit-sdk@<hash>/Runtime/Scripts/`

## 已知 API 差异 (v1.3.5, 2026-04)

### Room.Connect 需要三个参数
```csharp
// SDK 实际签名 (Room.cs):
public ConnectInstruction Connect(string url, string token, RoomOptions options)

// 错误写法 (README 示例可能省略 options):
var connect = room.Connect(url, token);          // 编译错误 CS7036

// 正确写法:
var connect = room.Connect(url, token, new RoomOptions());
```

### ConnectInstruction 没有 .Error 属性
```csharp
// ConnectInstruction 继承 YieldInstruction，只有:
//   bool IsError { get; }
//   bool IsDone { get; }
// 没有 .Error 字符串属性

// 错误写法:
Debug.LogError($"Failed: {connect.Error}");      // 编译错误 CS1061

// 正确写法:
if (connect.IsError) Debug.LogError("Connection failed");
```

### RPC 注册在 LocalParticipant 上
```csharp
room.LocalParticipant.RegisterRpcMethod("methodName", handler);
// handler 签名: async Task<string>(RpcInvocationData data)
// data 包含: CallerIdentity, Payload, ResponseTimeout
```

### Android 麦克风采样率必须和 Unity 实际回调一致
LiveKit Unity `MicrophoneSource` 在构造时用 `RtcAudioSource.DefaultMicrophoneSampleRate`
创建 native audio source，随后每个 Unity audio callback 都会校验 `sampleRate/channels`。

已踩坑：部分 Android 设备实际回调为 24 kHz，但 SDK 默认期望 48 kHz，日志表现为：

```text
actualRate=24000 expectedRate=48000
InvalidState: sample_rate and num_channels don't match
audio capture failed / capture callback failed
```

当前项目规则：

1. 在创建 `MicrophoneSource` 前，把 `RtcAudioSource.DefaultMicrophoneSampleRate` 对齐到
   `AudioSettings.outputSampleRate`（见 `MicrophonePublisher.ConfigureLiveKitMicrophoneSampleRate`）。
2. HUD / 自检必须显示麦克风 configured rate，避免把“音频帧被拒”误判为 Gemini 没反应或视觉问题。
3. 如果未来改成自定义音频源或升级 SDK，必须复测 Android 真机 24k/48k 行为。

## 审查流程

生成或修改 Unity C# 代码时:
1. 先检查 `Library/PackageCache/io.livekit.livekit-sdk@*/Runtime/Scripts/` 中的实际类定义
2. 不要依赖 README 或 WebSearch 的示例代码作为唯一 API 参考
3. 注意 SDK 的 `namespace LiveKit` — 所有类型在 `LiveKit` 命名空间下
