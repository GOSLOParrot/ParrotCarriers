---
description: AR Foundation / ARCore 版本约束与已知坑 (Unity AR 客户端)
globs:
  - "unity/**/*.cs"
alwaysApply: false
status: ratified
status_note: "核心版本锁已固定：Unity 2022.3.62f3 + AR Foundation/ARCore/ARKit 5.2.2。Sprint3/4 期间继续补充真实踩坑，完成后冻结为 AR 工作区/app 独立设计规则。"
last_reviewed: 2026-04-25
---

# AR Foundation — 已知坑（代码级）

> **配套 Skills（深度参考）**
> - `.cursor/skills/ar-foundation-api/SKILL.md` — AR Foundation 5.1.x / 5.2.x API 参考（Packages.md；本仓库用到的 API 面在 5.1 → 5.2 中未变）
> - `.cursor/skills/ar-foundation-samples/SKILL.md` — 官方 Samples 实现模式（帧抓取、平面、面部、Anchor 等）
> - `.cursor/skills/livekit-unity-video-publish/IMPL_REF.md` — AR 帧抓取 → LiveKit 推流接缝（`ARCameraBackground` Blit / `_rt` / TrackSource / 12 条踩坑表）；与本规则 §2 双向引用
>
> **AR 工作区路由**：本规则只列代码级踩坑；架构 / 设计 / 工作区入口请进 `.cursor/memory/architecture/ar_workspace_index.md`。
>
> **仓位说明**：本规则的所有具体路径（`unity/ParrotDev/Assets/csc.rsp` 等）属于 **Sprint 1–3 测试床**。Sprint4 起新代码进 `unity/ArSpike/Assets/Scripts/ParrotApp/`；ECP/LiveKit/AR 各类脚本的搬迁顺序见 `unity/ArSpike/Assets/Scripts/ParrotApp/MIGRATION.md`。本规则的踩坑结论同样适用 ArSpike，但路径需要对应到 ArSpike 自己的 `csc.rsp` / `Packages/manifest.json`。

本规则是 AR 工作区和后续独立 app 设计的版本锁与踩坑清单。它不是追新清单；Sprint3/4 内优先稳定闭环，不随意升级 Unity/AR Foundation 主版本。

## 0. 当前版本决策

**继续使用 Unity 2022.3 LTS 没问题**。这是当前项目最保守、最适合稳定收口的选择：

- Unity 2022.3 LTS 是本项目当前编辑器基线（实测 Editor 版本：**2022.3.62f3**）。
- 这是“当前收口基线”，不是永久版本承诺。Unity LTS 的优势是稳定和非破坏性修复；若未来进入长期 app 独立开发，应在 Sprint4 后单独评估迁移到当时最新 LTS。
- `unity/ParrotDev/Packages/manifest.json` 与 `unity/ArSpike/Packages/manifest.json` 都锁定 `com.unity.xr.arfoundation` / `com.unity.xr.arcore` / `com.unity.xr.arkit` 为 **5.2.2**（2026-04-29 从 5.1.5 升级，原因：5.2.2 才把 `libarcore_sdk_c.so` 改 16KB 对齐，满足 Android 15+ Play 商店要求；5.2.x 仍是 Unity 2022.3 LTS 兼容线，**不**等于 6.x / Unity 6）。`xr.management` 不在两边的 manifest 中显式钉，让 UPM 自行解析（详见 `unity/ArSpike/README.md` §"XR Plugin Management"，那里是版本锁的活文档，避免两边漂移）。
- Sprint3/4 期间允许 5.2.x patch 级修补，但**不主动升到 6.x 或 Unity 6**（会强迫迁出 Unity 2022.3）。
- 若未来要再升级（>= 5.3 或 6.x），必须单独开“AR 版本迁移”任务，同时验证 XR Simulation、Android ARCore、LiveKit 视频推流、ARCameraManager.frameReceived、TapToPlace、权限和真机启动门。

联网核对依据：

- Unity AR Foundation 5.1 文档：AR Foundation 本体只提供接口，目标平台必须安装并启用 provider plug-in，例如 Android 的 Google ARCore XR Plug-in。<https://docs.unity3d.com/Packages/com.unity.xr.arfoundation@5.1/manual/index.html>
- Unity Learn 2022.3 AR 环境教程仍以 Unity 2022.3 LTS + AR Foundation + ARCore/ARKit + XR Plugin Management 为课程基线。
- Unity LTS 文档：LTS 适合生产锁版，提供修复而非破坏性新功能；是否迁移到更新 LTS 应作为单独升级项目处理。<https://docs.unity3d.com/2022.3/Documentation/Manual/WhatsNew2022LTS.html>
- Unity 自定义 scripting symbols 文档：`csc.rsp` 放在 `Assets` 根目录会在脚本编译前定义全局 C# 符号，适合 batch/CI 或无 asmdef 的项目。<https://docs.unity3d.com/2022.1/Documentation/Manual/CustomScriptingSymbols.html>
- Unity asmdef 文档：长期按包版本定义符号应使用 Assembly Definition 的 `versionDefines`，但符号只在对应 assembly 内生效。<https://docs.unity3d.com/Documentation/Manual/assembly-definition-includes.html>

## 1. AR Foundation 版本必须与 Unity 2022.3 及 ARCore XR Plugin 对齐

当前项目环境为 Unity 2022.3 LTS，Sprint3/4 **锁定 AR Foundation 5.2.x**（具体钉死 5.2.2，三件套同档）。不要进一步升到 6.x 或 Unity 6；升级会把 AR、XR Simulation、Android provider、LiveKit Unity SDK、视频推流问题混在一起，降低排障质量。

版本不对齐或 provider 未启用时常表现为**静默失败**：ARSession 不启动，`frameReceived` 永远不触发，Unity 仍可能发布一条 LiveKit 视频轨但画面停住或黑屏。

- 使用 AR Foundation 5.2.x → 必须同时安装对应 5.2.x provider：Android 用 ARCore XR Plugin 5.2.x（5.2.2 起 `libarcore_sdk_c.so` 16KB 对齐），iOS 用 ARKit XR Plugin 5.2.x。
- 仅安装 package 不够；还必须在 `XR Plug-in Management` 中为目标平台启用 provider。Android 测试必须确认 ARCore loader 在 Android Providers 中。
- 不要混用跨大版本组合（5.x ↔ 6.x），除非单独开迁移任务并完整复测。
- 5.1 → 5.2 行为差异：本仓库使用的 `XRCameraSubsystem` / `ARCameraManager.frameReceived` / `ARCameraBackground.material` / `ARPlane.extents` / `XRCpuImage` API **未变**；§5 的 5.1 已踩坑结论在 5.2 仍然适用。

## 2. `ARCameraManager.frameReceived` 在渲染线程回调，`Graphics.Blit` 必须在其中执行

`ARCameraBackground.material` 的 GPU 纹理路径不走标准相机管线。正确抓帧方式：

```csharp
// ARVideoPublisher.cs — 已正确实现
_arCameraManager.frameReceived += OnARFrameReceived;

private void OnARFrameReceived(ARCameraFrameEventArgs args)
{
    Graphics.Blit(null, _rt, _arCameraBackground.material);   // ✅ 在回调内
}
// 不要用 Camera.Render() 或 Camera.targetTexture — ARCore GPU 纹理路径不经过标准相机管线
```

## 3. `UNITY_AR_FOUNDATION` 是项目显式宏，不是 Unity 自动宏

`UNITY_AR_FOUNDATION` 不会因为安装 `com.unity.xr.arfoundation` 自动出现。当前项目通过
`unity/ParrotDev/Assets/csc.rsp` 显式定义它，否则 Android 真机包会把 ARCameraManager /
ARCameraBackground 路径编译掉，表现为：

- HUD 显示 `AR: UNITY_AR_FOUNDATION off`
- `SceneProfileManager` 落到 `DESKTOP_WEBCAM`
- `ARVideoPublisher` 只能走 WebCam fallback，LiveKit 轨道可能是 ON 但帧停住/黑屏

当前没有 asmdef，所以 `csc.rsp` 是正确的短期修复：它在脚本编译前定义全局 C# 符号，能覆盖 `Assets/Scripts` 里的普通脚本。修改 AR 相关代码或构建配置时，必须确认这个宏仍然生效。

长期治理建议：如果 Sprint4 后将 AR 工作区拆成独立 assembly，应把 `UNITY_AR_FOUNDATION` 迁移到 asmdef `versionDefines`：

- Resource/package: `com.unity.xr.arfoundation`
- Expression: `[5.2,5.3)`
- Define: `UNITY_AR_FOUNDATION`

注意 asmdef `versionDefines` 只对该 assembly 生效；迁移时要把 AR 相关脚本放入同一个或明确引用的 assembly，不要混用全局宏和局部宏造成行为漂移。

## 4. `#if UNITY_AR_FOUNDATION` 条件编译 — AR Foundation 包不存在时代码不应报错

所有 AR Foundation 类型引用必须包在条件编译块内（ARVideoPublisher.cs 和 XRHandTracker.cs 已正确实现）：

```csharp
#if UNITY_AR_FOUNDATION
using UnityEngine.XR.ARFoundation;
#endif

#if UNITY_AR_FOUNDATION
    // 所有 ARCameraManager / ARCameraBackground 调用放这里
#endif
```

未包裹的情况下，当 AR Foundation 包不在 Package Manager 时，编译直接报错。

## 5. AR Foundation 5.1 API 细节不要凭印象写

启用 `UNITY_AR_FOUNDATION` 后，之前被条件编译隐藏的代码会真正进入 Android Player 编译。已踩过的 API 坑：

- `ARPlane.extents` 是 `Vector2`，平面面积应使用 `extents.x * extents.y`。不要写 `extents.z`。
- `ARSession`、`ARPlane`、`ARCameraManager` 等类型在 Player 构建也需要 `using UnityEngine.XR.ARFoundation;`，不能只在 `UNITY_EDITOR` 下引用。
- 如果字段或阈值只在 `UNITY_XR_HANDS` / `UNITY_AR_FOUNDATION` 代码块里使用，字段声明也要放入同一条件块，避免非对应能力构建产生 misleading warning。

## 6. Unity Build UI 报错要和 AR 运行时错误分开

Sprint3/4 真机包阶段出现过：

- `GUI Error: Invalid GUILayout state in BuildPlayerWindow view`
- `Getting control 1's position in a group with only 1 controls when doing repaint`

这类错误常见于 Unity Editor BuildPlayerWindow 布局/重绘状态，不等同于 AR Foundation 运行时失败。处理顺序：

1. 先确认 Console 里没有 C# compile error。
2. 若只有 BuildPlayerWindow GUI/layout 报错，重启 Unity 或 Reset Layout 后重新 Build。
3. 不要把这类 Editor UI 状态误判成 `ARCameraManager.frameReceived`、LiveKit 推流、或 Android 权限问题。

同时，Android 真机测试前必须清掉 Player Settings 级阻塞项：ARM64、XR Plug-in Management Android provider、Camera/Microphone 权限、Input Handling 策略。

## 7. LiveKit Unity SDK 通过 UPM git URL 安装

不要通过 Asset Store 或手动复制 DLL。只能用 git URL 安装，具体 URL 和版本见 `.cursor/rules/livekit-unity-sdk.mdc`。

## 8. Android 运行时权限 CAMERA 和 RECORD_AUDIO 需要代码请求，AndroidManifest.xml 声明不够

```csharp
// 在 ARSession 初始化之前请求权限，否则 frameReceived 永远不触发（静默失败）
Permission.RequestUserPermission(Permission.Camera);
Permission.RequestUserPermission(Permission.Microphone);
```

在 Room 连接之前确认权限已授予；否则 `ARVideoPublisher.OnRoomConnected` 会发布一个无视频源的空轨。

## 9. 必须开启并使用 XR Simulation 进行 Editor Play 测试

由于频繁真机打包太慢，我们必须使用 AR Foundation 5.2.x 自带的 XR Simulation 进行初步测试。
- **环境要求**：在 `XR Plug-in Management` 里，对于 PC/Mac Standalone 平台，**勾选 XR Simulation**。
- **限制与坑**：Editor 下 `XR Simulation` 会模拟注入虚拟环境，WASD 控制相机，代码层应该把 XR Simulation 和真机视为同一逻辑流。如果发现模拟环境中 Plane 扫描正常但没法触发某些真机独有事件，请人工通过 Android 真机兜底测试。绝大部分情况，Gate 1 测试必须依赖 XR Simulation。
