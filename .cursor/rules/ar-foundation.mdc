---
description: AR Foundation / ARCore 版本约束与已知坑 (Unity AR 客户端)
globs:
  - "unity/**/*.cs"
alwaysApply: false
---

# AR Foundation — 已知坑（代码级）

以下 5 条均来自 ARVideoPublisher.cs / XRHandTracker.cs 实际代码或文档已知问题，不含设计建议。

## 1. AR Foundation 版本必须与 ARCore XR Plugin 对齐

AR Foundation 4.x 和 5.x 的 API 不兼容，且版本不对齐时**静默失败**（无报错，ARSession 不启动，`frameReceived` 永远不触发）。

- 使用 AR Foundation 5.x → 必须同时安装 ARCore XR Plugin 5.x
- 不要混用跨大版本组合

## 2. `ARCameraManager.frameReceived` 在渲染线程回调，`Graphics.Blit` 必须在其中执行

`ARCameraBackground.material` 的 GPU 纹理路径不走标准相机管线。正确抓帧方式：

```csharp
// ARVideoPublisher.cs — 已正确实现
_arCameraManager.frameReceived += OnARFrameReceived;

private void OnARFrameReceived(ARCameraFrameEventArgs args)
{
    Graphics.Blit(null, _rt, _arCameraBackground.material);   // ✅ 在回调内
}
// 不要用 Camera.Render() 或 Camera.targetTexture — ARCore GPU 纹理路径不经过标准相机管线
```

## 3. `#if UNITY_AR_FOUNDATION` 条件编译 — AR Foundation 包不存在时代码不应报错

所有 AR Foundation 类型引用必须包在条件编译块内（ARVideoPublisher.cs 和 XRHandTracker.cs 已正确实现）：

```csharp
#if UNITY_AR_FOUNDATION
using UnityEngine.XR.ARFoundation;
#endif

#if UNITY_AR_FOUNDATION
    // 所有 ARCameraManager / ARCameraBackground 调用放这里
#endif
```

未包裹的情况下，当 AR Foundation 包不在 Package Manager 时，编译直接报错。

## 4. LiveKit Unity SDK 通过 UPM git URL 安装

不要通过 Asset Store 或手动复制 DLL。只能用 git URL 安装，具体 URL 和版本见 `.cursor/rules/livekit-unity-sdk.mdc`。

## 5. Android 运行时权限 CAMERA 和 RECORD_AUDIO 需要代码请求，AndroidManifest.xml 声明不够

```csharp
// 在 ARSession 初始化之前请求权限，否则 frameReceived 永远不触发（静默失败）
Permission.RequestUserPermission(Permission.Camera);
Permission.RequestUserPermission(Permission.Microphone);
```

在 Room 连接之前确认权限已授予；否则 `ARVideoPublisher.OnRoomConnected` 会发布一个无视频源的空轨。
