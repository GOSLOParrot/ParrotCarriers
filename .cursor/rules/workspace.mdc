---
description: ParrotCarriers 工作区全局路由与项目概述
globs: []
alwaysApply: true
status: ratified
status_note: "长期基建路由, 经多轮 Sprint 使用, 按 sprint0_preflight.md §6.4 标 ratified。"
last_reviewed: 2026-04-22
---

# ParrotCarriers — 工作区路由

## 项目定位

ParrotCarriers 是 GOSLOParrot 的通信总线子项目（RFC 1149: IPoAC, but powered by parrots），构建 Unity AR + 三层 Bus 的传输骨架。

## 当前阶段

查看 `.cursor/memory/active_context.md` 获取当前进度与下一步计划。

当前状态 (2026-04-22):

1. **P2 + P2.5 连通已验证** — Bus V1 + 记忆共享 + L2-B + 触发器 + Unity 音视频推流 + Gemini Live 三端已验通
2. **现处 Sprint 0 前置** — 跑 `sprint0_preflight.md` 14 项 (S0.A-N) → 再进 Sprint 0 基建修缮 (S0.1-7) → Sprint 1-4
3. 代码资产: Bus 骨架 + 6 个 Python 包 + 10 个 Gemini tools + 4 个 DSG 触发器 + L2-B RustworkX
4. 双仓库: `GOSLOParrot/ParrotCarriers` (主) + `GOSLOParrot/nanobot` (fork, Agent 框架)
5. 新会话必读: `active_context.md` 头部 → `sprint0_preflight.md` → 当前 Sprint 段

## 文件路由表

| 要找什么 | 去哪里 |
|:---------|:-------|
| **全局索引**（唯一真相源） | `.cursor/memory/INDEX.md` |
| **当前进度与下一步** | `.cursor/memory/active_context.md` |
| **模块职责/数据流/成熟度** | `.cursor/memory/architecture/module_map_p2.md` |
| **P2 里程碑**（实现清单 + 架构决策） | `.cursor/memory/milestone_p2.md` |
| **完整功能需求 v2**（67 功能项 + 决策） | `.cursor/memory/requirements.md` |
| **AR App 工程计划** | `.cursor/memory/architecture/ar_app_plan.md` |
| **AR 摄影互动调研** | `.cursor/memory/architecture/ar_camera_interaction_survey.md` |
| **AR 特性愿景 & 架构收口** | `.cursor/memory/architecture/ar_feature_vision.md` |
| **AR 特性实施计划 (Sprint 0-4)** | `.cursor/memory/architecture/ar_feature_implementation_plan.md` |
| **Sprint 0 前置 Checklist (开工前必读)** | `.cursor/memory/architecture/sprint0_preflight.md` |
| **视频流采样审计** | `.cursor/memory/architecture/audit_identify_object_no_screenshot_20260420.md` |
| **总线架构 v4.2**（三层协议 + 拓扑） | `.cursor/memory/architecture/bus_v4.md` |
| **家族拓扑** | `.cursor/memory/architecture/scene.md` |
| **协议快照 V1**（RPC/Redis/DataChannel） | `.cursor/memory/architecture/protocol_snapshot_p1.md` |
| **部署快照** | `.cursor/memory/deploy_snapshot_p2_20260412.md` |
| **鹦鹉行为状态规则** | `.cursor/memory/parrot_behavior_rules.md` |
| **协议污染复盘** | `.cursor/memory/BigIssue.md` |
| **灵感 & 展望** (人类手写, AI 只读) | `.cursor/memory/lore/ideas.md` |
| **领域技能** | `.cursor/skills/` — 按需发现；`nanobot` 优先显式调用 |
| **AR Foundation API 参考 (5.1.x)** | `.cursor/skills/ar-foundation-api/` |
| **AR Foundation Samples 参考 (5.1.x)** | `.cursor/skills/ar-foundation-samples/` |
| ~~模块划分~~ (archived) | 已被 `module_map_p2.md` 取代, 见 INDEX §1.3 |
| ~~内部数据流 v3~~ (archived) | `system_core.md` 是 2026-03 愿景, 已 archived, 见 INDEX §1.3 |

## 关键约束

1. **服务器**: 东京双节点 — 常驻城堡 (2C8G ECS) + 按需机甲 (A10 抢占式)，同 VPC
2. **FalkorDB 替代 Neo4j**: Castle 2C8G 内存限制，FalkorDB ~100-500MB vs Neo4j ~2.7GB
3. **DSG 是耦合子系统**: 通过触发器/预加载/直写和 Graphiti/Brain 紧密耦合，非独立模块
4. **代码驱动协议**: 不在纸上做协议 schema 设计，候选字段等代码验证后收敛
5. **INDEX.md 为唯一真相源**: 所有路径、目录用途以 INDEX.md 为准

## 源码结构速查

```
src/parrot/          Python 源码根
├── bus/             总线框架（注册/心跳/挂载/Processor 接口）
├── brain/           云端大脑（Gemini Agent + 10 个 function tools）
│   └── tools/       fly_to, animate, dispatch_task, remember, query_memory,
│                    query_scene, set_mode, identify_object, manage_episode
├── scheduler/       调度器（py-trees BT 路由 + Blackboard V2）
├── dsg/             DSG 感知耦合层（L2-B 语义图 + 触发器 + Graphiti 接口）
│   └── triggers/    日程/消息/SSOT充实/场景上下文 4 个触发器
├── memory/          记忆子系统（Graphiti 客户端 + 对话归档）
└── shared/          跨模块共享（config/redis/constants/types/telemetry）

src/scripts/         运维/工具脚本（sim_unity_client, sim_dsg_desktop, sync_obsidian 等）
unity/ParrotDev/     Unity AR 客户端（LiveKit + XR Hands + AR Video）
```

## CRITICAL REASONING OVERRIDE
1. **LANGUAGE OF THOUGHT:** You MUST ALWAYS perform your internal reasoning and step-by-step logic inside `<thinking>` tags strictly in **ENGLISH**. This is non-negotiable for maximum coding accuracy.
