# App/Web Parallel TODO Board (2026-05-13)

This board coordinates two parallel chats. It starts intentionally empty:
the App chat and Web chat decide their own TODO lists with the user inside
their own conversations.

Workflow rules:

- Do not add implementation tasks here until the owning chat has scoped them.
- Use this board for status, ownership, blockers, and links.
- Put detailed business-interface notes in the lane directories under
  `codex_workspace/design_workspace/backend_interface_map/`.
- Put proposed shared core interfaces in
  `codex_workspace/design_workspace/backend_interface_map/core_interface_candidate_queue_20260513.md`.
- Before major implementation slices or core-impacting decisions, run a
  research/architecture gate and record the source skills/docs used. For DSG,
  L1.5/L2-B, graph rewrite, attention/decay, Graphiti, Runtime Flow, Scheduler,
  or LiveKit work, explicitly cite relevant Parrot skills such as
  `parrot-cursor-skill-bridge`, `dsg-rustworkx-master`,
  `dsg-l2b-node-organization-options`,
  `dsg-l1-5-l2a-conceptgraph-distilled`,
  `dsg-attention-schema-papers`, `graphiti`, `py-trees`, `nanobot`, and
  `parrot-bus-orchestration`. Do not start large backend/DTO changes from UI
  intuition alone.

## Status Legend

| Status | Meaning |
|:--|:--|
| `intake` | Mentioned or discovered, not yet scoped by the owning chat. |
| `proposed` | Owning chat proposed it; waiting for user decision. |
| `approved` | User agreed; ready for implementation planning in that chat. |
| `in_progress` | Owning chat is actively implementing or validating. |
| `blocked_core` | Needs a new shared core interface candidate. |
| `blocked_cross_lane` | Needs the other chat/lane. |
| `done` | Implemented or documented with an observable verification signal. |
| `deferred` | Postponed with a short reason. |

## App Lane

Owner: Unity App chat.

| ID | Status | Item | Business doc | Core candidate | Notes |
|:--|:--|:--|:--|:--|:--|
| APP-001 | in_progress | Startup / RoomSetting six-axis boundary: `Room`, `Model`, `Persona`, `Line`, `Theme`, `Agent Team`. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` | CORE-001, CORE-002 | `Theme` is the user-facing `skin_id` / UI suite selector. Internal `scene_profile_id` is no longer presented as a desktop/indoor/outdoor RoomSetting row; environment/baseline selection should be automatic. `Persona` can now enumerate through App HTTP `GET /api/app/personas`; `LineProfile` through `GET /api/app/line-profiles`. `Agent Team` starts as fixed `CatMaid Agent Team` placeholder until core `agent_team_id` / registry is confirmed with the user in this chat. |
| APP-002 | in_progress | START transition boundary: permission checks, App HTTP RoomSetting apply, token mint, LiveKit connect, `applyRoomProfile`, silent connect success, failure states, and IPoAC/progress placeholder. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | Keep progress/loading as a separate transition page after START, not part of the first startup screen. START must not infer success from HTTP/Mint/room join alone. |
| APP-003 | in_progress | LineB / LiveKit status UI audit: readiness, cold-start/restart requirement, `selection_drift`, echo/voiceprint/TTS status, pause/resume/reconnect visibility. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | Line selection is visible in RoomSetting, but runtime Line hot-swap is not promised; show Brain restart/supervisor boundary clearly. |
| APP-004 | in_progress | Canvas menu boundary: reuse `MenuRegistry`, `canvas_snapshot`, typed Ref/Edge data, and tiered setting actions without hard-coding one renderer. | `backend_interface_map/app/canvas_menu_ref_workspace_app_interface_20260513.md` | CORE-007 | Unity owns touch-first rendering; shared core should stay renderer-agnostic. |
| APP-005 | in_progress | Main ready contract: define which HUD, tool drawer, 2D workspace, photo/focus/BBox, model driver, resource, state, and connection modules must be ready when startup completes. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | Startup is not complete just because LiveKit connects; greeting remains gated by scene ready + explicit placement. |
| APP-006 | in_progress | App-side Ref workspace boundary for future 2D workspace / Red String / Evidence Board partial features. | `backend_interface_map/app/canvas_menu_ref_workspace_app_interface_20260513.md` | CORE-006, CORE-007 | App consumes shared candidates and may help refine them with the user here; do not directly write Graphiti/FalkorDB/L2-B from Unity App DTOs. |
| APP-007 | done | Unity App scene/folder hygiene: isolate Smoke scene/scripts under `Assets/Tests/Smoke/**`; keep formal App entry under `Assets/ParrotApp/Scenes/ParrotApp_Startup.unity`. | `backend_interface_map/app/README.md` |  | Formal scene is clean and non-Smoke: Camera, Directional Light, `ParrotAppRoot`; Unity console verified with zero errors/warnings. |
| APP-008 | done | Startup visual/material audit for horizontal phone first screen, RoomSetting entry, START transition, and placeholder loading/progress style. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | User approved using the supplied paper/wood placeholder set as slots. Whitebox screenshots cover startup and RoomSetting; PlayMode now uses backend `experience_mode` values instead of a UI-only preview switch. |
| APP-009 | done | Formal Unity startup whitebox: iQOO Neo9 landscape-target paper/wood shell, RoomSetting page, START handoff to `AppStartupFlowController`, PlayMode / `experience_mode` lever, transition/progress, and main-ready hold screen. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | Implemented in `Assets/ParrotApp/Runtime/Scripts/Startup/ParrotAppStartupUiController.cs`; scene uses `Assets/ParrotApp/Scenes/ParrotApp_Startup.unity` and `Assets/ParrotApp/Art/Startup/Resources/StartupPaperCraft/**`. RoomSetting has backend `New` / `Save`; saved Rooms persist through App HTTP when `appApiUrl` points to ECS. Theme writes `skin_id`. This is not yet the final formal AR homepage/menu HUD. |
| APP-010 | done | Startup cold-load implementation audit: Unity folder/resource cleanup, Build Settings formal entry, RoomSetting HTTP snapshot/preview/apply client, orchestrator Tier 1 prewrite, LiveKit RPC business-failure parsing, DataChannel heartbeat binding, and main-ready cleanliness contract. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | No new core candidate added. Verified by Unity static tests, backend RoomSetting/menu/monitor tests, Unity MCP runtime smoke, and zero console errors/warnings after the shutdown drain fix. 2026-05-15 non-phone Castle probe now proves real room join, Brain presence, business RPC, and heartbeat; formal HUD/menu main-ready ownership remains APP-015. |
| APP-011 | done | Unity project physical cleanup and inventory SSOT: collapse formal App into `Assets/ParrotApp/**`, remove legacy duplicate roots/templates/sample scene, and index scripts/resources/real assets/test evidence. | `backend_interface_map/app/unity_project_inventory_app_ssot_20260513.md` |  | `Assets/Scripts/ParrotApp` is removed and forbidden. Formal runtime scripts now live under `Assets/ParrotApp/Runtime/Scripts/**`; runtime resources under `Assets/ParrotApp/Resources/**`; curated App art under `Assets/ParrotApp/Art/AppV1/**`; current runtime model visuals under `Assets/ParrotApp/Resources/Models/**`; `Assets/ParrotApp/Models/**` is source/import staging only with a tracked README placeholder; smoke/Ner tuning stay under `Assets/Tests/**` as test evidence only. |
| APP-012 | done | Resource usage separation audit: split runtime-loaded startup art from candidate art, document Sprint4 migration archive value, and add guards against reference/test assets becoming completion evidence. | `backend_interface_map/app/unity_project_inventory_app_ssot_20260513.md` |  | `StartupPaperCraft` Resources now only contains sprites loaded by `ParrotAppStartupUiController`; unused startup candidates live under `Assets/ParrotApp/Art/Startup/Candidates/**`. |
| APP-013 | done | Live App API / LiveKit runtime verification: run with `8790` App API, `7880` LiveKit, token mint, orchestrator, Brain participant, and confirm START is not a fake success. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | 2026-05-15 post-ECS-restart script passed the non-phone formal chain: App HTTP RoomSetting snapshot/save/apply for `ner_lineb_room`, orchestrator LineB prewrite, Mint, LiveKit connect, Brain `agent-*` participant presence without manual dispatch, `applyRoomProfile` business-ok, `setAppCapabilityMode` business-ok for both script-safe silent mode and `FullARCompanion`, and `parrot.ecp.state` DataChannel publish. A second observer participant received the heartbeat topic, proving LiveKit DataChannel delivery. `/api/app/live-state` did not expose Brain `session/ecp_state`, so Brain-side ECP ingest observability remains a follow-up and must not be called formal main-ready. Current selected state is intentionally left on `ner_lineb_room` / `line_b` for the next phone START pass. This does not complete formal HUD/menu main-ready ownership or iQOO Neo9 mic/Bluetooth/app-switch/AR/video verification; those stay under APP-015/APP-024. |
| APP-014 | done | Formal scene runtime service mounting: explicitly wire RoomSetting client, orchestrator client, heartbeat publisher, ECP event publisher, microphone publisher, AR video publisher, audio route detector, and capability receivers under `RuntimeServices`. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | `ParrotApp_Startup` now mounts AppRoomSettingClient, AppHomeMenuClient, OrchestratorClient, LifecycleHeartbeatPublisher, EcpEventPublisher, AudioRouteDetector, MicrophonePublisher, ARVideoPublisher, VideoStateReporter, VideoTierReceiver, AudioRoutePolicyBrainReporter, LiveKitReconnectSupervisor, FormalMainReadyGate, FormalHomeHudController, FormalHomeMenuLoader, FormalHomeMenuController, FormalHomeToolController, FormalModelReadyReporter, FormalModelPlacementController, FormalArRuntimeBootstrap, and FormalArSessionBaselineReporter. `AudioRouteManager` is resolved/added by formal startup/audio services until the scene is next saved with the component mounted. Startup flow refs are non-null. Castle non-phone LiveKit/Brain/business RPC and DataChannel publish/delivery are verified; Brain-side ECP ingest visibility and full phone media/device verification remain APP-015/APP-024. |
| APP-015 | in_progress | Formal AR homepage/menu load: replace the startup main-ready placeholder with real HUD/menu loading gates while keeping canvas menu core implementation out of the startup critical path. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md`, `backend_interface_map/app/canvas_menu_ref_workspace_app_interface_20260513.md`, `backend_interface_map/app/formal_homepage_hud_menu_plan_20260515.md` | CORE-007 | FormalMainReadyGate now owns `ReportRunning()` and waits for START transport sync, LiveKit, Brain, heartbeat DataChannel, mic/video where required, HUD, menu snapshot, model, and AR baseline gates. FormalHomeHudController reports `hud_loaded`; FormalHomeMenuLoader loads `/api/app/canvas` through App HTTP and emits success/failure events; FormalHomeMenuController renders a compact always-on toolbar plus separate canvas menu/settings panels from that HTTP canvas, plus a bottom-center demo-like cube placement button. Workspace, camera mode, photo awareness, and XR-hand UI mode apply through App HTTP; selector rows remain read-only. FormalHomeToolController is active for CAM only through PhotoController ECP metadata + HTTP upload; MAG/BBox are disabled/deferred until after phone stability and backend SVA/ECP evidence design. FormalModelReadyReporter resolves the selected Resources manifest; FormalModelPlacementController is the first placement/`onGosloPlaced` owner and waits for `FormalMainReadyGate.IsReady` before trying AR plane raycast; AR miss now fails visibly instead of fake-placing, and whitebox is only a missing-runtime-asset fallback after valid placement. FormalArRuntimeBootstrap mounts ARSession, XROrigin, ARRaycastManager, ARPlaneManager, TrackedPoseDriver, ARCameraManager, and ARCameraBackground only on demand from the AR baseline gate and assigns the copied Unity AR Mobile template `ARFeatheredPlane` prefab to `ARPlaneManager.planePrefab`; the old custom plane/point-dot visuals are removed. 2026-05-17 Parrot pose fix: standing/on-hand `AnimationDriver` now keeps the neutral GLB body pitch (`minecraftStandingBodyPitchWeight=0f`) so the body cube should not jut forward, while flying retains Minecraft forward lean. 2026-05-17 plane blocker fix: phone screenshots still showed magenta, so `ShadowReceiver.mat` and `OcclusionMaterial.mat` are pinned to the App-owned Built-in-compatible translucent/transparent `ParrotARPlaneFallback.shader`; exact demo2 ShaderGraph parity is deferred until after the phone loop is usable. Do not mount `AppV1SmokeReferenceUiController` wholesale; extract only useful ideas into formal loaders. |
| APP-016 | in_progress | AppV1SmokeReferenceUiController reference extraction: old Smoke UI is now explicitly test/reference-only; next copy approved HUD/tool drawer/camera/workdesk/note/Focus/BBox ideas into the formal homepage plan. | `backend_interface_map/app/unity_project_inventory_app_ssot_20260513.md`, `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md`, `backend_interface_map/app/unity_homepage_menu_livekit_audit_20260515.md` |  | Demotion/rename complete: `Assets/Tests/Smoke/Scripts/AppV1SmokeReferenceUiController.cs` preserves the old script GUID via `.meta`, formal startup scene does not mount it, and Smoke builder/tests use the new name. It was moved out of `Assets/ParrotApp/Runtime/Scripts/UI` so the formal App runtime tree no longer visually mixes the legacy Smoke shell with production UI owners. The Smoke UI and `LifecycleSmokeForcer` are now wrapped in `#if UNITY_EDITOR`, so they remain Editor/Smoke evidence and do not compile into Android player builds. It remains reference-only because it still contains legacy local-preview assumptions and may not match mobile startup/mic/Bluetooth flow. The startup `Scene` confusion is already corrected in the formal controller: visible row is `Theme`/`skin_id`, while `scene_profile_id` remains automatic baseline. |
| APP-017 | done | Castle START repair: deploy Unity-token Brain dispatch, fix systemd Brain LineB ADC or force a LineA smoke, clean duplicate tmux Brain evidence, then rerun non-phone Brain RPC START proof. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | Server-side repair complete as of 2026-05-14: ADC moved to `data/secrets/google-stt-service-account.json` with `parrot:parrot 400`, env updated, `data/secrets/` gitignored, `PARROT_MINT_AGENT_DISPATCH=unity`, token-mint rebuilt, commit `26a142f` pushed, Mint reports `agent_dispatch_requested: True`, Brain has no new crash, and five services are active. After Castle `c0f1705`, `sim_unity_client.py --startup-rpc-check --startup-room-profile-id ner_lineb_room` passed Brain participant plus post-join `applyRoomProfile` / `setAppCapabilityMode` business-ok. After the 2026-05-15 ECS restart, fresh-room and formal-chain probes also proved Brain appears without manual dispatch. The Mint response still lacks newer `agent_dispatch_active_*` diagnostics, so keep that as a deploy-visibility gap only. The old RoomSetting RPC payload diagnostic has been removed from active backend code. Phone/Bluetooth/AR testing moves to APP-015.8/APP-024. |
| APP-018 | done | Unity App transport/interface taxonomy and homepage/menu responsibility audit: classify App HTTP, Orchestrator HTTP, Mint, LiveKit media, ECP protocol plane, Brain RPC, SVA/video, Unity local state, and smoke evidence boundaries. | `backend_interface_map/app/unity_app_transport_interface_taxonomy_20260515.md`, `backend_interface_map/app/unity_homepage_menu_livekit_audit_20260515.md`, `backend_interface_map/app/unity_livekit_ecp_sva_data_flow_map_20260515.md`, `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | Taxonomy, audit, and data-flow map are now aligned: ECP is the broad embodied-control protocol (`EcpCommand`/`EcpAck`, `EcpState`, `EcpEvent`, lossy tick, command causality, snapshot/sighting/ref links), not just DataChannel heartbeat. Formal homepage/menu loading remains undesigned; do not prebuild HUD/menu from startup placeholder or old Smoke UI. Full menu/canvas snapshots stay App HTTP or future paged HTTP; RPC stays compact control plane. |
| APP-019 | done | Formal START button/business flow repair: `StartFromConfig` must App HTTP apply the selected RoomProfile before Mint/LiveKit and fail instead of fake-starting from a local draft. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | Implemented in `AppStartupFlowController`: permission -> Tier 1 orchestrator prewrite when required -> App HTTP `/api/app/room-setting/apply` -> Mint -> LiveKit -> Brain RPC sync. Missing RoomSetting endpoint fails with `room_setting_http_apply_required`; backend rejection fails with `room_setting_http_apply_failed:<reason>`. |
| APP-020 | done | RoomSetting ECS persistence verification: confirm `New Room`, saved RoomProfile/preset list reload, reserved-id handling, ECS file persistence, and no fake local-save success. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | 2026-05-15 formal App HTTP probe passed: `/new` returned an unsaved `room_*` draft, `/save` persisted fixed probe `room_codex_persistence_probe`, reload listed it from ECS, and active Room stayed `default` without an apply call. Unity Save now treats `default`, `ner_lineb_room`, `ephemeral`, and `workspace_only` as save-as-new ids to avoid overwriting built-in baselines; AppRoomSettingClient now rejects malformed save/apply responses instead of fake success. |
| APP-021 | in_progress | Homepage/menu predesign reset: remove or quarantine accidental startup main-ready/menu assumptions, then re-audit Unity game UX, LiveKit/ECP constraints, SVA/video requirements, and backend menu data ownership before designing home. | `backend_interface_map/app/unity_homepage_menu_livekit_audit_20260515.md`, `backend_interface_map/app/unity_app_transport_interface_taxonomy_20260515.md`, `backend_interface_map/app/formal_homepage_hud_menu_plan_20260515.md`, `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | 2026-05-15 audit confirms current main-ready surface is a hold screen only. Do not treat it as a homepage. Do not copy Smoke UI wholesale. APP-016 demotion/rename is done; `formal_homepage_hud_menu_plan_20260515.md` records the reusable assets/scripts and first implementation order. |
| APP-022 | in_progress | Menu loading and save/load architecture plan: decide exact App HTTP vs LiveKit RPC vs ECP protocol boundaries for menu blocks, full canvas snapshots, workspace switching, settings persistence, SVA/video controls, and DSG/GOSLO/Scheduler events. | `backend_interface_map/app/unity_homepage_menu_livekit_audit_20260515.md`, `backend_interface_map/app/unity_app_transport_interface_taxonomy_20260515.md`, `backend_interface_map/app/unity_livekit_ecp_sva_data_flow_map_20260515.md`, `backend_interface_map/app/formal_homepage_hud_menu_plan_20260515.md`, `backend_interface_map/app/canvas_menu_ref_workspace_app_interface_20260513.md` | CORE-007 | Round-1 decision recorded: persistent saves and large snapshots are HTTP; formal homepage workspace/camera/photo-awareness/XR-hand menu apply is also HTTP; RPC is compact in-room request/response for START/session sync and true latency-sensitive controls; ECP is the larger embodied-control/event/state/ref protocol and must be used deliberately, not as a dump pipe. MAG/BBox are deferred until phone stability and backend SVA/ECP evidence design. |
| APP-023 | in_progress | Mint/LiveKit/Brain connection stability design pass: document and then implement temporary degraded modes, silent keepalive, 2D workspace pause policy, session holding, fresh-token reconnect, disconnect/backoff, Brain re-dispatch, and fake-success prevention. | `backend_interface_map/app/unity_homepage_menu_livekit_audit_20260515.md`, `backend_interface_map/app/unity_app_transport_interface_taxonomy_20260515.md`, `backend_interface_map/app/unity_livekit_ecp_sva_data_flow_map_20260515.md`, `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md` |  | Current partial coverage: silent mode, chokepoint shutdown, route-aware mic republish, background FSM, AR/video tiers, audio-route Brain RPC reporting, ECP object-payload parsing, fresh-token reconnect/backoff owner, and degraded-state reporting from reconnecting/connecting/token-gate startup paths now exist. Remaining production gaps are 2D pause policy, degraded HUD, and phone evidence. |
| APP-024 | in_progress | Android device lifecycle/input route implementation plan: use iQOO Neo9 formal App for microphone permission, Bluetooth/SCO/A2DP input/output switching, app switch/background/resume, network flap, AR camera/video publish, and reconnect verification. | `backend_interface_map/app/startup_roomsetting_app_interface_20260513.md`, `backend_interface_map/app/unity_app_transport_interface_taxonomy_20260515.md`, `backend_interface_map/app/unity_audio_route_research_20260516.md` |  | 2026-05-15 phone pass started on `V2338A` with the formal package. Scripts/Editor tests may be used only for quick non-phone gates. Production completion requires phone App evidence and must not be polluted by Smoke scenes or Editor-only helpers. First findings: the old template package id was replaced by `com.parrotcarriers.app`, Android is locked to landscape, and Release builds needed `insecureHttpOption: 2` while Castle uses dev-local `http://` endpoints. 2026-05-16 code slice adds the formal Android route plugin/manifest and Unity `AudioRouteManager` facade. 2026-05-17 iQOO screenshots showed Unity mic publish false-success prevention working (`audio_read_timeout`, `frames=0`) but no uplink; the next build adds a final Android `AudioRecord` fallback even when Unity lists a mic device, after forcing the input route to phone mic. This is still not phone-stable until a rebuilt App proves Bluetooth pre-connected, connect-after-start, disconnect fallback, phone mic fallback, other-media coexistence, pause/resume, LineA/LineB voice, and no LiveKit room deadlock. |
| APP-025 | intake | Listening body-language and trigger body-feel animation hooks: parrot head-tilt while the user speaks/listens, notice glance for C3, surprise peek for high surprise, and urgent alert only for C4/interrupt-class events. | `.cursor/memory/architecture/Interface/goslo_trigger_awareness_taxonomy_20260515.md` |  | Recorded from Web chat as an App-owned requirement, not a Web implementation assignment. The App lane should decide exact animation clips, blend parameters, and when ASR/listening state drives head tilt. |
| APP-026 | backend_unblocked / app_in_progress | BBox/MAG production unblock before the next Web roadmap slice: formalize what the App can implement now, what backend still blocks, and what packet/asset/visual-tool evidence APIs are required for real tool controllers. | `backend_interface_map/app/app_evidence_tools_bbox_mag_photo_intent_workspace_20260515.md`, `.cursor/memory/architecture/Interface/time_aligned_evidence_interface_20260515.md`, `.cursor/memory/architecture/Interface/goslo_trigger_awareness_taxonomy_20260515.md` | CORE-012, CORE-014 | 2026-05-16 implementation pass: Photo/CAM can continue through `FormalHomeToolController` + `PhotoController`; App should add upload timebase headers next. BBox/MAG now have a backend/App V1 lifecycle surface: `POST /api/app/visual-tool/event`, asset route `POST /api/app/visual-tool/asset/{asset_id}`, ECP `visual_tool.lifecycle`, Web debug `/api/vision/evidence/tool-lifecycle`, BB receipt `transient/visual_tool_lifecycle_receipt`, and tool-cabinet `backend_ready` state. App can implement real controllers behind feature flags, upload optional rendered crops/previews, and send stable phases (`lock`, `confirm`, `explicit_send`, `cancel`, `release`) without backend blockage. Production toolbar enablement still needs APP-024 phone/screen-share smoke and UI/body-feel tuning. Backend owns shared salience/threshold policy, evidence alignment, IntentWorkspace staging, C3/C4 policy, and L1.5/L2-B promotion; App owns touch/AR affordances and semantic tool-state events. This is not the full DSG L3 attention module. Validation: focused backend/App/Web route set `7 passed`. |
| APP-027 | done | Local laptop Castle sandbox: isolate a Docker Desktop LiveKit/App API/token-mint/orchestrator/optional Brain stack so iQOO can compare local latency/audio behavior against public ECS without mixing `.env`, `data/runtime_config.json`, or Unity formal config. | `backend_interface_map/app/local_laptop_castle_app_env_20260518.md`, `backend_interface_map/web_console/web_console_environment_switch_prompt_20260518.md` |  | Implemented, local secrets copied only into gitignored `infra/laptop.env.local`, and verified on laptop IP `192.168.2.4`: `infra/docker-compose.laptop.yml`, `infra/laptop-castle.ps1`, `infra/laptop.env.example`, generated `codex_workspace/local_runtime/castle_laptop/**`, shifted ports `17880/17881/17888/17890/18790`, local Redis/Falkor ports `16379/16380`, token-mint split URL support (`LIVEKIT_URL` returned to phone, `PARROT_MINT_LIVEKIT_INTERNAL_URL` for in-Docker Brain dispatch), mounted `PARROT_PRESETS_DIR` / `PARROT_LINE_PROFILES_DIR`, no-BOM runtime writes, and local RoomProfile rewrite to `livekit_room_id=parrot-laptop-main`. Health checks passed for token-mint, orchestrator, and App API; RoomSetting returns `default` and `ner_lineb_room` on the laptop room. Brain starts in the isolated `parrot-laptop-castle` project; Mint returns the phone-facing URL and active server-side Brain dispatch without token `roomConfig`; `sim_unity_client.py --startup-rpc-check --startup-room-profile-id default --identity laptop-start-local-room2 --agent-name parrot-brain` passed in `parrot-laptop-main` with Brain `agent-*` presence plus `applyRoomProfile` / `setAppCapabilityMode` business-ok. Config switching is now explicit through `infra/switch-unity-app-config.ps1 -Target laptop|ecs|show`; active Unity `Resources/parrot_config.json` is build-time input, so phone target changes require rebuild/reinstall. 2026-05-18 script smoke passed `show -> ecs -> laptop -> show`, preserved the gitignored ECS backup, printed only secret booleans, and left active Unity config on `parrot-laptop-main`. Web Console/Obsidian/setting-file targets are separate environment choices and require Web-side environment selector/proxy work. This completes the lab route; actual iQOO latency/audio comparison remains APP-024 and is not production/TLS/TURN proof. |
| APP-027.1 | done | Laptop CAM upload and visual-tool dev profile: make the laptop backend usable for phone CAM upload plus BBox/MAG dev smoke. | `backend_interface_map/app/local_laptop_castle_app_env_20260518.md`, `backend_interface_map/app/_tmp/app026_bbox_mag_visual_tool_research_todo_20260517.md` | APP-024, APP-026 | 2026-05-18 follow-up: laptop compose now exposes Brain's in-job `photo_upload_server` as `0.0.0.0:17889 -> 7889` with cache root `/app/data/photos`; laptop Unity config generation now writes `photoUploadUrl=http://<laptop-ip>:17889`, `visualToolDevEnabled=true`, and `visualToolHttpEnabled=true`; active Unity config was switched to the regenerated laptop profile. `sim_unity_client.py --startup-rpc-check --startup-room-profile-id default` triggered a Brain room job, then `http://192.168.2.4:17889/health` returned photo-upload ok. Additional bugfix/proof: app-monitor now proxies read-only live-state/L2-B to the active Brain job through `PARROT_APP_MONITOR_BRAIN_LIVE_STATE_URL=http://brain:7889`; a real LiveKit ECP probe published `photo.taken_preview`, POSTed photo bytes through HTTP, and Web/app-monitor refresh saw a `photo` node with `/app/data/photos/2026-05-18/ph_probe_*.jpg` in `reference_image_path`. Upload completion now locally dispatches the same Brain-source `photo.asset_uploaded` event into `EcpEventIngest`, because room publish is not a self-delivery guarantee. This closes the Web refresh visibility path for laptop CAM; phone body-feel and production APP-024 smoke still remain. |

### APP-015 Detail Checklist

This checklist is the current Unity App homepage-loading handoff. It keeps
real phone implementation work separate from connectivity-only smoke tests.

| Step | Status | Scope | Implementation Notes | Verification Signal |
|:--|:--|:--|:--|:--|
| APP-015.1 | done | Current-state audit | Formal scene mounts startup, RoomSetting, RuntimeServices, LiveKit, heartbeat, mic/video, lifecycle, orchestrator, and main-ready placeholder. `AppV1SmokeReferenceUiController` is Smoke/reference only. | Updated `startup_roomsetting_app_interface_20260513.md` and this board. |
| APP-015.2 | done | START RPC proof | Reused `sim_unity_client.py --startup-rpc-check`; added `--startup-room-profile-id` so the same script can verify the room profile that matches the running Brain line. The script now mirrors formal startup by loading the selected RoomProfile locally/HTTP-side and only checking post-join Brain sync. Default LineA correctly failed against LineB Brain; `ner_lineb_room` passed `applyRoomProfile` and `setAppCapabilityMode` business-ok. 2026-05-15 formal chain script also proved App HTTP save/apply, orchestrator prewrite, Mint, LiveKit join, Brain presence, RPC business-ok, and DataChannel heartbeat publish/delivery to an observer. | Castle non-phone proof: Brain participant + apply profile + capability mode business-ok + heartbeat publish/delivery. Brain-side ECP ingest is not yet externally visible through App monitor live-state, and RoomSetting read/write RPCs are removed; load/save proof is App HTTP. |
| APP-015.3 | done | Runtime endpoint injection | Phone/ECS `parrot_config.json` includes `appApiUrl`, `orchestratorUrl`, mint/LiveKit values, and bearer secrets in the gitignored local config. Public `http://8.216.45.45:8790/api/app/room-setting` returns 2 rooms, `GET /api/app/line-profiles` is reachable, and `http://8.216.45.45:7890/health` returns orchestrator ok from this workstation. New `GET /api/app/personas` is ready for the next deploy. Unity mint client now normalizes a root `:7888` URL to `/mint`, so phone config may store either the service root or full endpoint. Camera capture additionally needs a phone-reachable `photoUploadUrl` or `photoUploadHost`/`photoUploadPort`; the formal toolbar refuses loopback upload endpoints on device. | Do not commit or echo local secrets. Endpoint reachability, LiveKit token validation, client URL normalization, and non-phone Brain dispatch verification are resolved. If `PARROT_APP_MONITOR_SECRET` is enabled, fill Unity `appApiSecret` only in ignored config. Remaining blockers are formal main-ready/homepage and real phone media/lifecycle, not endpoint injection. |
| APP-015.4 | done | Main-ready gate owner | `FormalMainReadyGate` now waits for RoomProfile/START sync, LiveKit connected, Brain present, startup Brain RPC sync, heartbeat DataChannel ready, HUD loaded, menu snapshot loaded, model resolved, and AR/session baseline clean before calling `ReportRunning()`. Mic/video publish health is deliberately not a blocking home gate: missing microphone/video remains visible through HUD/health/degraded state, but the AR homepage can open so placement and device-route diagnosis are not hidden behind the startup hold surface. It also self-reevaluates while waiting, so missing one-shot loader events degrade instead of silently hanging. Startup hold screen says `Loading home gates` and shows only true home blockers instead of claiming home is ready. | `ReportRunning()` is no longer emitted from a placeholder; media publish readiness must be proven in the phone stability pass and must not be marked stable just because home opens. |
| APP-015.5 | done | Formal HUD shell | `FormalHomeHudController` now builds a low-occlusion landscape status HUD from lifecycle/health/startup state and reports `hud_loaded` to `FormalMainReadyGate`. It shows LiveKit, Brain, mic, video, room/profile, line/profile, home-gate state, startup failure, menu loader failure, and reconnect-pending state without using Smoke UI. | Dialogue gate, final visual polish, and phone HUD evidence remain pending; HUD no longer completes the whole homepage by itself. |
| APP-015.6 | in_progress | Formal menu/tool drawer loader | `AppHomeMenuClient` + `FormalHomeMenuLoader` now load the formal home canvas snapshot from App HTTP `/api/app/canvas` and report `menu_snapshot_loaded`; no full snapshot goes through LiveKit RPC. The loader catches up if mounted after `OnMainUiReady`, retries bounded HTTP failures, clears completed coroutine refs, and rejects timestamp-only/half-parsed payloads by requiring real workspace/menu shell fields. `generated_at` is parsed as a JsonUtility-safe `float` so a valid ECS canvas snapshot is not misread as empty. It also loads `/api/app/personas` and `/api/app/line-profiles` as optional read-only selector catalogs. `FormalHomeMenuController` renders a compact toolbar plus separate canvas menu/settings panels. Workspace, camera mode, photo awareness, and XR hand mode now apply through App HTTP (`/api/app/workspace/apply`, `/api/app/camera/mode`, `/api/app/awareness`, `/api/app/xrhand/mode`) with business-failure parsing before local UI updates. Audio-route rescan/report remains a session diagnostic owner for the phone-stability slice. CAM delegates to `PhotoController.CapturePhoto()` only when the photo upload endpoint is phone-safe. MAG/BBox are disabled/deferred and do not emit ECP. Generic tool cards and durable save/edit affordances remain read-only until their HTTP or owner action is explicitly implemented. | Missing backend shows degraded state, not fake data. Legacy menu RPC wrappers are removed from active Brain registration; full `canvas_snapshot`, selector persistence, and menu persistence are HTTP-owned. MAG/BBox are postponed until after iQOO phone stability and backend SVA/ECP evidence design. |
| APP-015.7 | in_progress | Model/AR/home resources | `FormalModelReadyReporter` resolves the selected Resources manifest before the home gate and catches up after late mounting; `ModelManifestDto` now falls back to lowercase Resources paths so `GOSLO_default` can resolve `goslo_default.json` on Android. Runtime model visuals are separated under `Assets/ParrotApp/Resources/Models/**`: `goslo_default.json` loads `Models/GOSLO`, `ner_skin2.json` loads `Models/Ner/NerSkin2_SkeletonData`, and `FormalModelPlacementController` can instantiate GameObject resources or create a Spine `SkeletonAnimation` visual before falling back to a whitebox placeholder only after a valid placement when the runtime asset cannot load. Runtime-created models now call `ModelDriver.BootstrapNow()` in the placement frame so GOSLO uses the existing `AnimationDriver`/Minecraft-pose/`GosloLegacyController` chain immediately for the placement greeting and joystick. The placement owner waits for `MainUiReadyOnce` and `FormalMainReadyGate.IsReady`, refuses fake placement on AR miss, routes demo2 `ObjectSpawner` poses into the RoomSetting-selected model, attaches `XRGrabInteractable` + `ARTransformer` for selection/drag/pinch, keeps the older EnhancedTouch path as fallback only when the XRI bridge is inactive, and calls `ReportGosloPlaced()` exactly once. `FormalArRuntimeBootstrap` mounts XROrigin, ARRaycastManager, ARPlaneManager, TrackedPoseDriver, ARCameraManager, ARCameraBackground, demo2 `ARFeatheredPlane`, copied XRI actions/screen-ray/spawner scripts, demo2 `XRUIInputModule`, Device tracking origin, and serialized plane detection `-1`; the old custom point-dot path is removed. `XRGeneralSettings` automatic loading/running stays disabled for Android, iPhone, and Standalone so Editor Android Play does not call `StopSubsystems` before a manager has initialized. | Needs rebuilt iQOO proof that the startup hold surface clears, real camera background appears, AR Mobile template plane visuals appear, GOSLO places on a detected plane through the center cube/tap flow, tap selection, one-finger drag, pinch scale, and joystick animation behave on device. |
| APP-015.8 | in_progress | iQOO Neo9 device pass | Use the phone as the real scene for mic permission, Bluetooth/SCO/A2DP route changes, app switch/resume, AR camera/video publish, reconnect, and long background. First adb pass confirmed the formal package starts as `com.parrotcarriers.app`; CAMERA/RECORD_AUDIO must be granted on the formal package; Release build HTTP cleartext blocked RoomSetting until `ProjectSettings.asset` was changed to `insecureHttpOption: 2`. | Device log must still prove RoomSetting HTTP, Mint, LiveKit, Brain, heartbeat, main-ready, route switching, and lifecycle recovery without crash or fake success after the rebuilt package is installed. |
| APP-015.9 | done | Homepage/menu design reset | 2026-05-15 audit records that current main-ready is a hold screen only, ECP is a broad embodied-control protocol plane, App HTTP owns full menu/canvas reads and persistent saves, and Smoke UI cannot be copied wholesale. | `backend_interface_map/app/unity_homepage_menu_livekit_audit_20260515.md`; new work must start from APP-018 taxonomy, not from Smoke UI or an accidental startup placeholder. |
| APP-015.10 | approved | RoomSetting persistence phone/ECS proof | From formal Unity App, create a Room, save it, reload snapshot, verify it appears in selector, then START from it. | Must prove ECS persistence, not just local fallback or script-side save/apply. |
| APP-015.11 | done | Menu transport boundary audit | Inventory every existing menu/RPC/HTTP/DataChannel surface and classify keep/move/delete before homepage implementation. Legacy menu read/write RPC wrappers are removed from active Brain registration; full `canvas_snapshot` and persistent saves stay HTTP or future paged HTTP. | `backend_interface_map/app/unity_homepage_menu_livekit_audit_20260515.md`; prevents RPC abuse and prevents full snapshots from going through LiveKit RPC. |
| APP-015.12 | in_progress | Connection stability policy implementation plan | APP-023 audit identified existing partial coverage and remaining service work. 2026-05-15 added audio-route Brain RPC reporting, ECP object-payload parser, `LiveKitReconnectSupervisor` fresh-token reconnect/backoff, and FSM degraded reporting from reconnecting/connecting/token-gate paths so reconnect failures do not silently remain in an optimistic state. Menu apply no longer piggybacks workspace/camera/photo/XR-hand state through compact RPC; those are App HTTP. Remaining concrete services: explicit 2D workspace AR/video pause policy, manual/native route picker limits, and richer degraded HUD copy. | Must become concrete Unity tasks with failure strings before phone verification. |
| APP-015.13 | approved | Formal phone-only verification bundle | Define adb/logcat capture, device matrix, manual steps, success/failure criteria, and evidence storage for iQOO Neo9. | Editor/SmokeScene cannot satisfy this checklist. |
| APP-016.1 | done | Smoke UI demotion | Renamed/demoted `AppV1MetaUiController` to `AppV1SmokeReferenceUiController`, preserving `.meta` GUID, and updated Smoke builder/tests/docs. | Smoke tests updated; formal scene remains free of the old controller. |
| APP-015.14 | in_progress | Fresh-token reconnect owner | `LiveKitReconnectSupervisor` now listens for passive post-main-ready disconnects, skips intentional chokepoint shutdown/background states, applies bounded exponential backoff, asks `AppStartupFlowController.RequestFreshTokenReconnect()` to mint a fresh token, reconnects LiveKit, reruns `applyRoomProfile`/`setAppCapabilityMode`, and rebinds heartbeat. START while already connected to a Tier1/LineB-changing Room now runs orchestrator prewrite + App HTTP apply + graceful chokepoint shutdown + fresh Mint reconnect + Brain RPC resync instead of hard-failing, waits for shutdown cool-down/`ReportDisconnected()` to finish, explicitly re-enters token/AR-starting lifecycle gates so the FSM does not stay in `Disconnected`, and can report degraded from token/AR/connecting/reconnecting failure paths. Degraded HUD surfacing is still pending. | Cached-token debug reconnect is no longer the only path, but iQOO Neo9 network-flap/background proof still belongs to APP-015.8/APP-024. |
| APP-015.15 | in_progress | Audio route policy publisher | `AudioRoutePolicyBrainReporter` now reports current route and route changes to Brain `setLineBAudioRoutePolicy` after LiveKit/Brain presence, using `unity_audio_route_policy` source. It separates `input_route` from `output_route` so A2DP/speaker output no longer falsely claims the same mic input, and now treats both `status:error` and `result.success:false` RPC payloads as business failures instead of false success. Unity mic selection now mirrors that rule: only `BluetoothSco` prefers a Bluetooth microphone device name and uses the 16 kHz SCO capture policy, while A2DP keeps Android's default input and the normal 48 kHz policy. `AudioRouteDetector.RefreshCurrentPolicy()` and `AudioRoutePolicyBrainReporter.RefreshAndReportCurrentPolicy()` support a formal menu manual rescan/re-report without the UI issuing raw RPC. Android route detection now tries native `AudioManager.getDevices(...)` first, falls back to legacy flags, and exposes detection source/device summary to HUD/menu. Manual device picker is still pending. | Static path is implemented and visible in formal HUD/menu; iQOO Neo9 Bluetooth/SCO/A2DP changes still need phone proof. |
| APP-015.16 | in_progress | Formal App HTTP menu client | `AppHomeMenuClient` now loads `/api/app/canvas` for modules, workspaces, tool cabinet, asset manifest, paper notes, and photo refs without Smoke UI local placeholders. It also reads `/api/app/personas` and `/api/app/line-profiles` for homepage selector status rows, and applies menu-owned state through `/api/app/workspace/apply`, `/api/app/camera/mode`, `/api/app/awareness`, and `/api/app/xrhand/mode`. 2026-05-15 workstation HTTP check against deployed Castle returned canvas 200 and business-ok for workspace/camera/awareness. The new local `/api/app/xrhand/mode` route and deferred MAG/BBox canvas state are not on ECS yet, so deployed Castle still returns 404 for that new route and old enabled MAG/BBox cards until the next deploy. Selector edit/apply flows still need an owner and likely remain Room/Profile save/apply rather than ad-hoc menu RPC. | Menu missing backend shows degraded state; loaded menu reflects backend read models and HTTP business failures are not treated as success. |
| APP-015.17 | done | ECP event payload parser | `EcpEventBuilder.FromWireJson` now extracts the embedded wire `payload` object into `payload_json`, and `EcpEventDispatcher` uses it instead of raw `JsonUtility`. | Static guard covers the parser path; downstream homepage event UI can depend on non-empty ECP payloads, with phone/runtime event proof still under APP-024. |
| APP-015.18 | done | Legacy menu/RPC cleanup decision | Old menu read/write and preset RPC wrappers (`listMenuBlocks`, `applyMenuSelection`, `applyPreset`, `saveAsPreset`) were removed from active Brain room registration. Remaining Brain RPCs are classified in code as START/session sync, LineB/audio diagnostics, workspace control, compact media/module controls, and ops reconnect signal. | Formal menu load/save must use App HTTP; new RPC additions require a compact real-time command justification. Static tests guard the old RPCs against reintroduction. |
| APP-015.19 | done | LiveKit/ECP/SVA data-flow map | `unity_livekit_ecp_sva_data_flow_map_20260515.md` now maps formal Unity game/App flow across App HTTP, Orchestrator, Mint, LiveKit media, LiveKit RPC, ECP state/event/tick/command, SVA/video, Brain/DSG/GOSLO/Scheduler, Unity local state, and phone config rules. | Main/homepage design must cite this map before adding new channels or controls. It records the current photo/captureSnapshot mismatch: `PhotoController` owns ECP metadata + HTTP image upload, and formal Unity must not send photo bytes through RPC. |
| APP-015.20 | done | Formal homepage HUD/menu asset/script reuse audit | `formal_homepage_hud_menu_plan_20260515.md` records what can be reused from formal scripts, what is reference-only from Smoke/Ner tuning, which AppV1/Startup/model assets are real runtime candidates, and which transport layer owns each homepage action. | TODO/Ref is now detailed enough to start HUD/menu V1 without copying Smoke UI or reintroducing old menu RPCs. |
| APP-015.21 | in_progress | Formal homepage HUD/menu V1 implementation | First code slices are in: `FormalHomeMenuController` renders the App HTTP canvas into a compact bottom-right toolbar plus separate canvas menu/settings panels; `FormalHomeMenuLoader` publishes snapshot success/failure plus selector catalog events; startup runtime service resolution mounts the controller plus `FormalHomeToolController`; workspace controls, camera mode, photo awareness, and XR-hand mode now use App HTTP via `AppHomeMenuClient` and update labels only after business-ok responses; audio-route rescan/report remains a session diagnostic owner for the phone-stability slice; read-only persona/line-profile selector status rows show current IDs/catalog availability; the HUD surfaces startup/menu/reconnect degraded state in the home status line. CAM now uses `PhotoController`'s ECP preview + HTTP upload path and refuses loopback upload endpoints on phone. MAG/BBox are visible toolbar placeholders only: App canvas marks them `deferred_phone_stability`, toolbar taps report the delay, and `FormalHomeToolController` no longer mounts Focus/BBox/ECP owners. Bugfix: `PhotoController` preserves `photoUploadUrl` scheme (`http`/`https`). Generic tool cards stay read-only until explicit App HTTP owner actions are wired, so no fake save/load/capture/focus/workspace/quick-control success is shown. The Settings quick actions plus the new bottom-center AR Mobile template cube button delegate to `FormalModelPlacementController`, not raw RPC; placement now tries AR plane raycast, refuses fake placement on miss, uses runtime `Resources/Models/**` visuals, and uses whitebox only as a missing-asset fallback after valid placement. | Static Unity guard plus App facade/monitor tests pass. Still needs Unity Editor/phone visual pass, note/persona/line selector edit/apply wiring, camera-mode photo notification proof with a real `photoUploadUrl`, native/manual device picker, joystick/XRHand/animation, phone-only stability under APP-024, then MAG/BBox attention wiring after backend evidence design. |
| APP-015.22 | approved | Homepage pre-implementation research/order guard | Before each homepage implementation slice, re-read `main_hud_landscape_v0_20260509.md`, `app_v1_whitebox_shell_20260510.md`, `menu_canvas_mvp_2dworkspace_20260509.md`, `formal_homepage_hud_menu_plan_20260515.md`, and the transport map. Before each LiveKit/audio/AR device slice, also re-read the relevant Cursor skill and any App-side research note such as `unity_audio_route_research_20260516.md`. The formal home target is camera/AR background + compact HUD + compact toolbar; settings and canvas menu open as separate pages/drawers. | This item exists to prevent large always-on drawers, ad-hoc RPC payloads, Smoke UI shortcuts, or old connectivity-script behavior from becoming production code. |
| APP-015.23 | in_progress | Input device detection and switching V1 | Research/design record: `backend_interface_map/app/unity_audio_route_research_20260516.md`. Formal rule is now implemented for the first code slice: `MicrophonePublisher` is the LiveKit mic track executor; `AudioRouteManager` is the accepted route facade; `AndroidAudioRouteManager` + `Assets/Plugins/Android/ParrotAudioRoute.androidlib/**` own Android communication-device routing, audio focus, and Bluetooth permission snapshots; `AudioRouteDetector` is telemetry/fallback; `AudioRoutePolicyBrainReporter` is a compact Brain observer; `MIC NEXT` / `MIC AUTO` remain debug/diagnostic controls, not the final phone audio UX. Route changes rebuild only the local mic track and must not reconnect the LiveKit room. Follow-up audit fixed three strategy/compatibility edges: native route preferences are cached until voice communication mode is active, the API-31 communication-device listener is isolated so the minSdk 30 plugin can degrade on Android 11, and `MicrophonePublisher` refreshes accepted route state on formal lifecycle resume. Android build follow-up removes the Java androidlib dependency on `UnityPlayer`; C# now passes Activity plus `AudioRouteSnapshotCallback` proxy and marshals callbacks through `UnityMainThread`. The route androidlib manifest plus source `build.gradle` own `com.parrotcarriers.audio` and disable route-library `BuildConfig`, avoiding duplicate launcher `BuildConfig`. 2026-05-17 iQOO no-uplink follow-up adds `AndroidPcmMicrophoneSource` + native `AndroidPcmMicCapture` (`AudioRecord`) as the formal fallback when Unity `Microphone.devices` is empty and `MicrophoneSource(null)` produces zero frames; this is local PCM capture only, not a room reconnect, token mint, or Brain dispatch. Latest wider LiveKit/Unity audit fixed two state-machine bugs: `AndroidPcmMicrophoneSource.Start()` rolls back failed native startup so a half-subscribed source cannot stick across retries, and AudioRecord sample-rate fallback is owned by `MicrophonePublisher` as separate 48 kHz / 44.1 kHz / 16 kHz LiveKit source attempts because LiveKit FFI rejects PCM frames whose rate differs from source construction. Latest iQOO greeting-only fix: Java `AndroidPcmMicCapture` now prefers raw Android `MIC` before `VOICE_COMMUNICATION`, reports `source_name`, and automatic AudioRecord retries stay on `system_default` so Bluetooth/A2DP downlink is not stolen by a forced PhoneMic override. The empty-device gate still tolerates stale/unknown native input-route snapshots after mic permission is granted. | Do not call route handling stable until iQOO Neo9 formal App logs prove Bluetooth pre-connected, connect-after-start, disconnect fallback, phone mic fallback, other-media coexistence, pause/resume, LineA and LineB voice, and no LiveKit room deadlock. Next phone proof must show HUD `frames/ch/readSr/peak` become non-zero and user speech reaches Brain; if frames are healthy but Brain still ignores speech, investigate remote track/STT ingestion next. Then replace debug MIC controls with a production route status/override panel. |
| APP-015.24 | in_progress | Formal homepage HUD+toolbar shell | First shell implemented in `FormalHomeMenuController`: the large drawer no longer opens by default; the always-on home surface is a compact toolbar with 32x32 pixel placeholders for camera, magnifier, BBox, canvas menu, 2D workspace, and settings. Settings and canvas menu open as separate surfaces. Magnifier/BBox are currently disabled/deferred placeholders, not active tools. | Static guard passes; still needs Unity Editor/iQOO visual pass before calling the shell polished. Do this before adding more tool actions, so audio picker/settings and menu pages have a proper place to live. |
| APP-015.25 | in_progress | Core model interaction sequence | First slices complete: `FormalModelPlacementController` owns the placement gate and `onGosloPlaced`; it waits for `FormalMainReadyGate.IsReady`, tries AR plane raycast placement, refuses fake placement on miss, then loads the selected manifest visual from `Resources/Models/**` when available or whitebox fallback under `AssetPreviewStage` only after a valid placement. `ModelDriver.ConfigureModelId()` lets runtime-created visuals/placeholders use the selected RoomSetting manifest. `FormalArRuntimeBootstrap` now uses the imported AR Mobile template `ARFeatheredPlane` prefab instead of custom dots/planes, and `FormalHomeMenuController` exposes a demo-like center cube placement button. 2026-05-16 strict-copy correction: the formal App now imports the demo2 `XRI Default Input Actions`, `Screen Space Ray Interactor`, `ObjectSpawner`, and `ARInteractorSpawnTrigger` under `Assets/ParrotApp/**` with `.meta` preserved. `FormalArRuntimeBootstrap` mounts that screen-ray/object-spawner chain at AR-runtime startup, switches the active EventSystem to demo2-style `XRUIInputModule` with the copied `XRI UI` action map, while `FormalModelPlacementController` routes spawned poses into the RoomSetting-selected Parrot/Ner manifest and attaches `XRGrabInteractable` + `ARTransformer` with demo bounds (`0.25` to `2.0`). This keeps `ModelDriver`, LiveKit/main-ready, and `onGosloPlaced` ownership but moves placement/select/drag/pinch closer to the actual Unity AR Mobile template stack instead of adding more custom gesture code. `FormalModelRemoteController` now mounts as a local bottom-left joystick after main-ready and placement: Ner routes to `spine_walk`, GOSLO routes to `ParrotController`/`AnimationDriver.WalkOnPlane`, and missing owners visibly degrade to local translation. `FormalXrHandPerchController` is now the formal local XRHand/perch owner: it mounts `HandGestureSource`, attaches `PerchOnHand` only after main-ready + placed model + `perch` capability + `AnimationDriver`, and reports `model_perch_unsupported` or `model_no_animation_driver` instead of pretending success. 2026-05-17 compile-symbol correction: `com.unity.xr.hands` / `UNITY_XR_HANDS` is present, but `UNITY_AR_FOUNDATION` must remain defined in `Assets/csc.rsp`; both symbols are now guarded by static tests. Next slices: rebuilt phone proof of plane detection/place/select/scale/drag, XRHand/perch behavior validation on device, and continued model/Minecraft-style animation wiring through manifest/controller/RPC. | Requires iQOO Neo9 phone proof before calling AR placement stable. This is a strict minimal XRI-chain bridge, not a wholesale SampleScene/object-catalog import. Any remaining divergence from demo feel must be fixed by adapting this XRI bridge, not by reintroducing ad-hoc gesture paths or Smoke scene evidence. XR Hands is enabled in code/package terms, but remains unproven until phone hand-tracking/perch testing. |
| APP-015.26 | done | Inline captureSnapshot RPC cleanup | Removed the formal Unity `SnapshotRpcHandler`/`CaptureSnapshotForRpc` path, disabled the legacy Brain `capture_current_frame()` inline-image RPC helper, removed stale snapshot/ByteStream lifecycle config fields, and updated the transport docs/tests so photos use `PhotoController` ECP metadata plus HTTP/storage assets. | Future camera-mode work may add a compact command/notification surface, but it must carry metadata/refs only. Photo bytes must not travel through LiveKit RPC. |
| APP-015.27 | approved | SVA identify_object and attention evidence design note | User clarified that `identify_object` is a GOSLO Intent-layer behavior and should obtain a time-aligned frame from the LiveKit background video stream or SVA frame cache, not from the camera-mode capture RPC. BBox/magnifier should eventually produce attention/ref evidence for L2-B/GOSLO with coordinates, pose, timestamp, and optional HTTP/storage image refs; strong attention may trigger a compact GOSLO notification, but the exact trigger payload is still a Web/backend design task. | App homepage may expose camera immediately. Magnifier and BBox must stay disabled/deferred until after iQOO phone stability and the backend SVA/Intent evidence upgrade; do not invent ad-hoc image RPCs or fake identify-object success. Web/backend chat owns the SVA/Intent upgrade and future Node/ref storage details. |
| APP-015.28 | in_progress | iQOO Neo9 phone pass 1 log triage and formal UI fixes | 2026-05-15/16 LineA phone logs/screens show formal package startup, `parrot_config` load, RoomSetting/menu save exercise, Mint/LiveKit connect, Brain `agent-*` audio/RPC presence, AR video first frame / 1280x720 publish, and short background pause/resume video-state reporting. Bugfixes added this pass: startup `MainReadySurface` hides after formal home gates are ready so it no longer covers the AR/camera background; RoomSetting preview requests now carry a revision so late HTTP preview responses are ignored; repeated expired `setVideoTier` warnings are throttled; and the serialized formal scene no longer blocks home entry on `microphone_published`/LiveKit video publish health. 2026-05-16 AR visual correction imports the user's fresh AR Mobile demo2 `ARFeatheredPlane` visual chain and center cube button, removes custom formal point/plane visuals, disables fake placement on AR miss, and then imports the demo2 XRI Starter Assets (`XRI Default Input Actions`, `Screen Space Ray Interactor`, `ObjectSpawner`, `ARInteractorSpawnTrigger`) as the minimal strict-copy bridge. The formal bridge keeps RoomSetting manifest selection and `onGosloPlaced` ownership while using demo2 `XRUIInputModule`, Device tracking origin, serialized plane detection `-1`, `XRGrabInteractable`, and `ARTransformer` for selected-model tap/drag/pinch. Follow-up compile audit fixed a `TouchPhase` ambiguity in the legacy fallback path and the AR Foundation 5.2.2 `PlaneDetectionMode.Everything` API mismatch by using `(PlaneDetectionMode)(-1)`. The formal HUD now includes AR baseline/spatial-visual status plus placement diagnostics (`ar_raycast_no_plane`, `placed:ar_drag_plane`, selected/visual source), so phone failures are visible without guessing from screenshots. Mic/video publish status remains visible as degraded HUD/health state rather than a startup overlay blocker. 2026-05-17 phone screenshots showed the copied `Shader Graphs/ShadowReceiver` can still render magenta in ArSpike Android, so `ARFeatheredPlaneMeshVisualizerCompanion` now falls back to the bundled `ParrotARPlaneFallback.shader` translucent-white material for that graph/error path; the same run showed mic routes valid but frames zero, so APP-015.23 added the Android AudioRecord source fallback. | Static guard passes after the fix. Needs a rebuilt phone re-run to prove the hold surface clears, AR camera background appears, the AR plane is no longer magenta, XRI-bridged placement/selection/drag/scale are usable with the selected Parrot model, and HUD audio `frames/ch/readSr` become non-zero. Still not stable-complete: Bluetooth/SCO/A2DP, long background/session hold, network reconnect, LineB voice path, native device-route behavior, and the Unity console `liblivekit_ffi.so` 16KB-alignment Android 15 warning remain APP-024 / APP-015.23 work. |
| APP-015.29 | in_progress | BBox/Mag active evidence tools and IntentWorkspace/L2-B graph-link policy for the post-phone-stability homepage. | `backend_interface_map/app/formal_homepage_hud_menu_plan_20260515.md`, `backend_interface_map/app/unity_livekit_ecp_sva_data_flow_map_20260515.md`, `backend_interface_map/app/app_evidence_tools_bbox_mag_photo_intent_workspace_20260515.md`, `backend_interface_map/app/canvas_menu_ref_workspace_app_interface_20260513.md` | CORE-006, CORE-012, CORE-013 | 2026-05-15 App-facing audit/handoff now records the readiness split: CAM/Photo may continue through `FormalHomeToolController` + `PhotoController` with phone-safe HTTP upload; Photo Awareness/IntentWorkspace staging is backend-owned and C3/no-interrupt only; BBox/MAG backend evidence/ref/threshold paths pass module tests but formal toolbar emission remains disabled until APP-024 phone stability and accepted packet fields. User wants MAG/BBox to follow the same evidence family as photo preview/capture metadata: Unity emits compact ECP/ref/time/region metadata, image bytes move by HTTP/storage, backend evidence ledger aligns BBox/Focus refs to frames/assets, attention participates in L3/threshold policy, and optional GOSLO/IntentWorkspace notices use the awareness taxonomy. Do not add a `WorkspaceNodeKind` or make IntentWorkspace itself an L1.5 bucket yet; design as canonical L2-B graph plus workspace/pool/attention overlays and bounded graph rewrite/link rules. Validation for the handoff audit: focused backend/App guard tests `118 passed`. App animation/body-language follow-up: listening posture such as head tilt belongs to the App animation backlog and should be coordinated with the model animation slice. |

Latest APP-015.28 note: the formal placement clear action now reports `cleared` in `FormalModelPlacementController.LastPlacementStatus`, so a phone clear/place retest cannot inherit stale `placed:*` HUD evidence. The formal AR bootstrap mirrors demo2's active debug-plane slider by keeping `ARFeatheredPlaneMeshVisualizerCompanion.visualizeSurfaces` true, but the current Android phone path may use the App-owned translucent-white `ParrotARPlaneFallback.shader` when the copied `Shader Graphs/ShadowReceiver` material renders magenta. Treat that as a phone usability fallback; final demo2 parity still requires ShaderGraph/material cleanup after the immediate pink-plane blocker is gone.

Latest APP-015.7 / APP-015.25 phone-blocker note: the 2026-05-17 iQOO screenshot exposed three formal AR issues. `goslo_default.json` now enables `auto_scale_to_pet_height` and targets 0.16 m so the raw GLB cannot spawn at head-sized scale. The missing demo2 `ShadowReceiverShaderFunctions.hlsl` include is copied into the formal `ARMobileTemplate` shader chain with `.meta` preserved, addressing the magenta plane material symptom. Clearing the first placed GOSLO now calls `ReportGosloRemovedFromView()`, which reuses the existing in-session `2d_workspace` policy (`VoiceOnlyNoVideo` plus `applyWorkspace`) instead of disconnecting LiveKit, minting a new token, or starting a new Brain job. Re-placing the same model calls `ReportGosloReturnedToView()` to restore `ar_workspace` / `FullARCompanion` without replaying the first greeting. The local joystick now appears only after the placed model is selected. Audible greeting and final plane/material parity still need a rebuilt iQOO pass.

Latest APP-015.7 / APP-015.25 second phone-blocker note: the second iQOO rebuild still showed magenta/orange plane surfaces and oversized placement. The formal copied `ARFeatheredPlaneMeshVisualizerCompanion` has a mobile-safe transparent `PlanePatternDot` fallback when the demo ShaderGraph is unsupported, but the follow-up ShaderGraph import fixed enough of the chain that the App should no longer force fallback on all mobile builds. The template `ARInteractorSpawnTrigger` is configured with `requireHorizontalUpSurface=true` so placement does not choose vertical planes near the user's head. `FormalModelPlacementController` now performs post-active renderer-bounds height normalization after placement, and the HUD appends `LastBrainRpcStatus` so the next phone run can prove whether `onGosloPlaced` is `ok`, skipped, errored, or business-failed.

Latest APP-015.7 / APP-015.23 correction: the plane ShaderGraph is not treated as impossible; the formal copy was still missing part of demo2's shader chain. `URPShadowReceiver.shader` and `InteractablePrimitive.shadergraph` are now copied into `Assets/ParrotApp/Resources/ARMobileTemplate/Shaders/**` with `.meta` preserved, and `ShadowReceiver.mat` no longer points at a missing `_Texture2D` GUID. Editor build logs show `Shader Graphs/ShadowReceiver` compiling for `gles3`, so the next task is to diagnose any remaining phone magenta as ShaderGraph/build/runtime parity, not to elaborate fallback visuals. The fallback remains a simple translucent-white phone guard. The formal HUD now exposes route diagnostics beyond `Mic on`: native/fallback route source, route version, Bluetooth permission, audio focus/mode, microphone selected device, sample rate, available device count, local route policy, manual device pick, and Brain route-report success/attempt counts. Next iQOO run should use these fields to verify Bluetooth disconnect/reconnect and LineA mic publishing without ADB guesswork.

Latest APP-015.23 / APP-015.25 iQOO screenshot triage: native Android route diagnostics showed `phone_mic` with permission and focus granted while Unity's `Microphone.devices` list was empty, causing `no_microphone_devices` and no user speech uplink. `MicrophonePublisher` now has an Android-only default communication-input fallback: when the native route manager is present, microphone permission is granted, and Unity lists zero devices, it labels the path `android_default_microphone` and passes `null` to LiveKit/Unity's `MicrophoneSource` so Android supplies the current default input. AR plane parity remains demo2 ShaderGraph-first; fallback is simple translucent white if the graph fails, while `showArMobileTemplatePlaneSurfaces` stays true to match the demo2 transparent-plane plus dot affordance when the graph works. Model height normalization now repeats over several delayed passes after spawn, so late renderer/model-driver bounds can correct an oversized Parrot.

Latest APP-015.23 follow-up: Unity compile error `IEnumerator<T> requires 1 type arguments` is fixed by importing `System.Collections` in `FormalModelPlacementController`. The Android route bridge now treats Bluetooth as connected-device preference, not as a hard gate: if Bluetooth is enabled but no SCO communication device is connected, or an explicit Bluetooth preference cannot be satisfied, routing falls through to wired/earpiece/speaker with phone mic instead of returning `apply_preferred_device_no_target` and leaving the App stuck. `MIC NEXT`/device-label diagnostics now show/use `auto:android_default_microphone` when Unity lists zero microphones but Android native routing can provide the default input.

Latest APP-015.25 visual follow-up: the phone screenshots separated two issues. Pink/magenta is the AR plane material path; orange was the selected-model visual. `FormalModelPlacementController` now uses a transparent white `LineRenderer` selection ring instead of a cylinder/slab mesh, so selection should not cover the camera feed. `ARFeatheredPlaneMeshVisualizerCompanion` keeps Android error-shader protection enabled, but it no longer forces fallback for every Android ShaderGraph; a healthy `Shader Graphs/ShadowReceiver` must run so the demo2 transparent plane, feathered edge, and shrinking dot pattern can be validated. If the material resolves to a real error/fallback shader, the fallback is deliberately simple translucent white. `FormalArRuntimeBootstrap.LastPlaneMaterialStatus` is now shown in HUD diagnostics so the next phone pass can identify the actual plane shader/fallback state instead of guessing from screenshots. Rebuild phone proof is still required before calling plane material parity complete.

Latest APP-015.25 strict-copy note: demo2's mobile interaction stack is now present as a minimal formal bridge: copied `XRI Default Input Actions` + `Screen Space Ray Interactor` + `ObjectSpawner` + `ARInteractorSpawnTrigger`, mounted by `FormalArRuntimeBootstrap`, with placed RoomSetting-selected models receiving `XRGrabInteractable` + `ARTransformer`. Follow-up parity audit matched the demo2 `ObjectSpawner.spawnAsChildren = true` setting, records that the demo screenshot's white dots are the `ARFeatheredPlane` plane visual chain, and now also matches demo2's `XRUIInputModule` EventSystem path by disabling the startup `InputSystemUIInputModule` after AR runtime mount and binding the copied `XRI UI` actions. Origin/plane parity is also tightened to demo2 `TrackingOriginMode.Device`, camera Y offset `0`, and serialized plane detection mode `-1` via `(PlaneDetectionMode)(-1)`. Follow-up phone-blocker fix keeps formal HUD/menu/tool decorative graphics out of UI raycasts while preserving real buttons, so the copied `ARInteractorSpawnTrigger` is less likely to reject plane taps as UI touches. Placed models now also match demo grab defaults more closely: `ColliderPosition`, single focus, and default grab transformers stay enabled around the explicit `ARTransformer`. Latest bugfix makes the runtime spawner bridge idempotent: if `ObjectSpawner`, `ARInteractorSpawnTrigger`, or the transient spawn proxy is missing/stale after refresh/re-entry, the bootstrap reattaches it to the current XROrigin before binding the ray interactor. Follow-up UI responsiveness fix adds `FormalModelPlacementController.OnPlacementStateChanged`, consumed by the formal HUD/menu/joystick, so place/clear/select/scale/XRI status updates are immediate and not dependent on polling. The next iQOO run must prove tap place, selection, drag, and pinch. If it still differs from demo2, keep fixing this XRI bridge instead of adding more custom gestures or using Smoke scene evidence.

Latest APP-015.23 / APP-015.25 blocker fix: repeated oversized Parrot placement is addressed by making `ForceManifestHeightAfterPlacement()` refresh `_placedBaseScale` and reset `ScaleMultiplier` after normalizing renderer bounds to the manifest `default_pet_height_m`; delayed passes now run longer for late-loaded renderers and stop after intentional user scale or XRI/user drag selection. Microphone upstream now has a stronger truth guard: `MicrophonePublisher` waits for Unity `Microphone.GetPosition(...)` to advance and for LiveKit Unity `MicrophoneSource.AudioRead` to emit captured frames after `PublishTrack`; timeout/exception/room-disconnect/publish-intent-disable unpublishes the track and reports degraded HUD/health instead of fake `audio_published=true`, with `audio_read_timeout` distinguishing "Unity mic position moved but no SDK audio frames reached LiveKit". Track creation / publish failure paths now also clean local `_micSource` / `_audioTrack` before retry so no half-published local source survives. The formal HUD now has separate `UsingMic` and `Uplink` lines for selected/default mic, device list, publish stage, sample rate, route version, audio frame count, captured channel count, captured sample rate, peak level, and error. Static guard still passes (`28 passed`); rebuild/iQOO proof is required for size, LineA/LineB speech uplink, Bluetooth/SCO/A2DP, and background/reconnect stability.

Latest APP-015.23 deep audit: see `backend_interface_map/app/unity_audio_livekit_deep_audit_20260517.md`. Reread of LiveKit lifecycle/client SDK skills plus Android/Unity/LiveKit primary docs confirms the current formal split is still correct: Android native route owner, Unity `AudioRouteManager` policy facade, `MicrophonePublisher` as LiveKit mic executor, route changes rebuild only the local mic track, and RoomSetting/menu remain App HTTP. The P1 code gap from that audit is now implemented: after initial `AudioRead` success, `MicrophonePublisher` keeps a steady-state microphone watchdog using `Microphone.IsRecording`, stale `AudioReadFrameCount` age, last-frame age, degraded HUD state, and serial local mic republish recovery. APP-015.23 / APP-024 still stay in-progress until iQOO proves Bluetooth pre-connect, connect-after-start, disconnect fallback, pause/resume, LineA, and LineB.

Latest iQOO APP-015.23 / APP-015.25 bugfix: phone screenshot showed `bt-sco@16k` selected but formal mic publish stuck at `microphone_start_timeout`, `frames=0`, `readSr=0`; this is local Unity/LiveKit capture startup failure, not Brain RPC/STT failure. `MicrophonePublisher` now retries the same local capture source with a `bluetooth_sco_capture_48k` policy (`sco_capture_48k_retry`) and then falls back to `phone_default_microphone` at 48 kHz before failing, without reconnecting the LiveKit room, minting a token, or dispatching a new Brain job; HUD adds `fb=` for fallback status. Follow-up corrections now prefer `AudioRoutePreference.SystemDefault` for automatic recovery so Android can preserve Bluetooth/A2DP downlink while the native AudioRecord fallback captures plain phone MIC; forced PhoneMic is deferred to a future manual recovery action. The same pass changes Android AR plane material fallback to catch magenta `ShadowReceiver` ShaderGraph failures and uses a translucent white dot material whose fallback shader is checked with `shader.isSupported` so the fallback itself cannot turn magenta under the current Built-in Android path; follow-up also forces Android safety fallback on even if an old copied prefab did not serialize the new guard booleans. Placement now snaps model bounds back to the AR hit plane after GLB scale/height normalization. Parrot standing/on-hand body pitch also defaults back to the neutral GLB body pose so the placed body block should no longer jut forward during idle/placement greeting; flying still keeps its Minecraft forward lean. Rebuild/iQOO proof is required.

Latest APP-015.23 audio-route bugfix: Android native routing no longer reports `bluetooth_sco` as the active input/output just because `AudioManager.getDevices()` lists a SCO-capable device. Active SCO now requires `getCommunicationDevice()` confirmation; otherwise the formal path keeps A2DP as output-only and falls back to phone mic/default communication input for capture. The PhoneMic fallback is now a temporary native override through `AudioRouteManager.ApplyTemporaryNativePreference(...)`, not a durable Settings preference mutation, and it is sticky until user preference changes or a new session starts; restoring Auto/Bluetooth on `device_added` / `device_removed` can immediately undo a successful phone/default mic fallback. This should reduce the "Bluetooth route shown, but `AudioRead` frames stay zero" fake-route failure. Validation: Unity static guard `28 passed`; still needs rebuilt iQOO proof for connected Bluetooth, disconnect fallback, connect-after-start, LineA/LineB speech uplink, and pause/resume.

Latest APP-015.25 placement follow-up: formal Parrot placement now keeps GOSLO world-upright on accepted horizontal AR planes instead of rotating the whole companion to the detected plane normal. The plane is still used for hit position and bottom snapping, but noisy bed/blanket normals should no longer make the body look folded forward. `minPlacementPlaneUpDot` is tightened to `0.75f` so strongly tilted surfaces are less likely to accept placement. Rebuild/iQOO proof is still required.

Latest APP-015.23 / APP-015.25 phone-blocker fix: the final microphone fallback now tolerates a stale/unknown native input-route snapshot after permission is granted, because iQOO evidence showed the route UI can look valid while Unity still produces `frames=0`. This path still rebuilds only the local mic source and must not reconnect LiveKit, mint a token, or dispatch a Brain job. Plane material fallback is also tightened: on Android, both the copied `Shader Graphs/ShadowReceiver` and the App-owned `Parrot/ARPlaneFallbackTransparent` placeholder are treated as replaceable if they risk magenta, and the runtime fallback now prefers Unity built-in transparent shaders with the copied dot texture plus a transparent occlusion slot. Rebuild/iQOO proof is required before marking mic uplink or plane visuals stable.

Latest APP-015.23 diagnostic follow-up: successful native AudioRecord fallback no longer clears the fallback/source evidence. `MicrophonePublisher` now tracks `ActiveAudioSourceKind`, `NativeAudioRecordState`, and `NativeAudioRecordError`, and the formal HUD shows those fields beside `frames/ch/readSr/peak`. The next phone run can distinguish "Unity mic path", "AudioRecord fallback active", "AudioRecord started but no frames", and "Brain/STT still did not consume frames" without guessing from route labels alone.

Latest APP-015.23 / APP-024 frames=0 P0 implementation: imported TODO source is now `backend_interface_map/app/imported_frames0_root_cause_audit_todo_20260517.md`. Formal App adds `ParrotMicForegroundService` plus Android 14 microphone foreground-service permissions, starts/stops that service around local mic track publish, exits native `AudioRecord` capture on persistent `read<0` or persistent zero-byte reads, cleans native route singleton callback/focus state on re-init, retries Android `setCommunicationDevice`, and keeps route changes as local mic-track rebuilds only. Stopping the foreground mic guard now uses `stopService(...)` so pause/teardown cannot be blocked by Android background-start restrictions. The latest follow-up consumes persistent native AudioRecord errors during the C# startup wait and preserves them as `native_audio_record_failed:*` instead of flattening them into `audio_read_timeout`, letting the next retry/HUD branch show the real local capture blocker. Native Android device add/remove callbacks now re-apply the current communication-device preference while voice mode is active, and `system_default` still clears the communication device so Bluetooth/A2DP output is not stolen by stale speaker pinning. Formal Android capture now prefers the App-owned `AndroidPcmMicrophoneSource` before Unity `MicrophoneSource` when no manual mic device is selected, because LiveKit Unity/device evidence shows Unity can report local `AudioRead` frames while remote uplink still receives no usable mic media. Video fake-success gates were tightened at the same time: no Android webcam fallback by default, HUD now shows video source/frame age/error, and `video_published=true` waits for post-publish frames. Lifecycle config now uses 35s shutdown cool-down and 8s first-frame timeout. Validation: Java androidlib compile passed, Unity MCP refresh produced 0 console errors, and `uv run pytest tests/test_unity/test_app_v1_meta_ui_static.py -q` passed 28/28. Still in-progress: rebuild iQOO proof must show non-zero local audio `frames/ch/readSr/peak`; only then test Brain/STT, Bluetooth/SCO/A2DP, pause/resume, LineA/LineB, and network reconnect.

Latest APP-015.23 / APP-024 permission proof: after the rebuilt package was installed on iQOO, ADB showed `RECORD_AUDIO: ignore` and package permission `granted=false`; that alone explains "only hear greeting, GOSLO cannot hear me". The test phone was manually corrected to `RECORD_AUDIO: allow` / `granted=true`. Follow-up code audit found the first `AndroidRuntimePermissions` helper still OR'ed Android runtime permission with Unity's legacy `Application.HasUserAuthorization`, so Android could be denied while Unity looked granted and no prompt appeared. The helper now treats `Permission.HasUserAuthorizedPermission(RECORD_AUDIO)` as the only Android truth, uses `PermissionCallbacks`, and logs both Android and legacy states. Startup, `MicrophonePublisher`, `AudioRouteManager`, and `AndroidPcmMicrophoneSource` all use the shared gate. Validation: focused static Unity tests pass `29 passed`; the next Unity compile/build must verify the `PermissionCallbacks` API on Unity 2022.3.62f3. Next phone run must rebuild/reinstall, START, grant the prompt if shown, and prove an active record path plus HUD non-zero `frames/ch/readSr/peak/nz` before debugging Brain/STT or LineB.

Latest APP-015.23 / APP-024 iQOO live-run evidence: the rebuilt App now reaches a real local uplink start. LogCat shows `android_audio_record` selected, LiveKit audio track published, and `audioReadFrames=1`; the screenshot/HUD showed fresh AR frames and non-zero local peak. The later stall lines up with Android `dumpsys audio` marking the recorder `silenced` and then `release` near a `ShortBackground` pause, so the active blocker is steady-state Android lifecycle/audio-policy recovery rather than START/Mint/Brain presence. `MicrophonePublisher` no longer queues a republish on `focus_resume_during_publish`; focus regain now delays and probes frame freshness/recorder state before rebuilding the local mic track. Next work: add/verify end-to-end voice latency telemetry, rerun with fresh LogCat and foreground hold, then isolate app switch, Bluetooth connect/disconnect, and network flap. Do not call the 3-5s delay LiveKit/ECS capacity until the client-capture -> Brain STT/VAD -> LLM -> TTS -> downlink timing is measured.

APP-024 local-route correction (2026-05-18): user clarified that laptop Castle helps isolate latency, but Bluetooth/mic selection is a phone-local Android route problem. `AndroidAudioRouteManager` no longer keeps media mode for `auto` unconditionally. `system_default` / `phone_mic` remain media-safe; `auto` now enters Android communication mode only when `getAvailableCommunicationDevices()` exposes a selectable SCO / BLE headset / wired / USB communication capture device, otherwise it preserves A2DP/output-only Bluetooth and uses phone/default mic. Next iQOO build should prove the three cases separately: Bluetooth headset capture, A2DP output-only + phone mic, and no-Bluetooth fallback.

Latest APP-026 App-side review/fix: detailed log is `backend_interface_map/app/_tmp/app026_bbox_mag_visual_tool_research_todo_20260517.md`. This chat added the feature-flagged BBox/MAG packet builder, HTTP lifecycle/asset wrapper, local BBox/MAG controllers, optional dev screen-region asset probe, Photo upload timebase header support, and the formal camera-mode HUD/controller. Follow-up audits fixed phase allow-listing, stale async crop/upload work, local lock/body-feel handling, disabled asset fallback, EventSystem isolation, and camera-mode pending races. Current status: App-side Editor/static/backend-route validation passes, but production enablement still waits for APP-024 iQOO phone/screen-share smoke, real rendered/HUD body-feel tuning, tool asset throughput proof, and CAM/Photo regression checks. Camera mode is App-side complete for static/backend-route validation as of 2026-05-18; phone-production complete still belongs to APP-024.

Latest APP-015.23 audio blocker follow-up: iQOO still showed `Mic wait` and
`microphone_start_exception` during the native fallback, while route,
permission, and focus looked valid. Wider LiveKit/Unity audit found two
candidate stuck points. First, native `AndroidPcmMicrophoneSource.Start()` now
unsubscribes `RtcAudioSource.base.Start()` if Java start fails, preventing a
half-started LiveKit audio source from surviving into retries. Second, sample
rate fallback moved out of Java: `MicrophonePublisher` creates separate
AudioRecord attempts at 48 kHz, 44.1 kHz, and 16 kHz, and Java stays strict to
the requested rate. This matters because LiveKit FFI rejects captured PCM when
the frame sample rate differs from the audio source construction rate. The
empty-device gate also tolerates stale/unknown native route snapshots once mic
permission is granted. Rebuild/iQOO proof is required.

Latest APP-015.23 wider bug sweep: Android route ownership now includes BLE
headsets in addition to classic SCO. `TYPE_BLE_HEADSET` can be selected as a
communication voice route, while BLE speaker/hearing-aid remain output-only
diagnostic routes; the fallback detector mirrors this so HUD/debug labels do
not hide modern Bluetooth devices. Native AudioRecord -> Unity callback errors
are no longer swallowed: `AndroidPcmMicCapture` emits `pcm_callback_failed:*`
through HUD `native=` / `nerr=` if the AndroidJavaProxy bridge fails. Formal
cleanup also now detaches, stops, and disposes every local mic source because
the pinned LiveKit Unity SDK does not honor `stopOnUnpublish` internally. Static
guard and Java compile pass, but iQOO proof is still required.

Latest APP-015.24 output-route correction: `auto` no longer forces
speaker/earpiece when Android has output-only Bluetooth (`A2DP` / BLE speaker /
hearing-aid style output) but no selectable communication headset. Formal
capture fallback now keeps automatic AudioRecord retries on `system_default` so
Bluetooth downlink can stay on the headset while Java captures the plain phone
MIC source. Explicit PhoneMic forcing is deferred to a future manual recovery
control because it can pin downlink back to the phone speaker. Follow-up bugfix: if a previous
speaker/earpiece communication device was already selected, output-only
Bluetooth now actively calls `clearCommunicationDevice()` so Android can return
downlink audio to A2DP while keeping phone/default mic capture. Static guard and
Java compile pass; this still needs iQOO proof for `frames/ch/readSr/peak` and
Bluetooth output.
Latest follow-up: A2DP-only Bluetooth must also avoid entering
`MODE_IN_COMMUNICATION` in the first place. The native route owner now keeps
`MODE_NORMAL` / media routing when there is Bluetooth media output but no
selectable SCO/BLE headset communication target, so Parrot downlink can remain
on Bluetooth while the App-owned AudioRecord path captures phone/default MIC.
This also covers connect-after-START device callbacks: an A2DP-only route exits
communication mode instead of merely clearing a stale speaker/earpiece pin.
`MicrophonePublisher.StopPublishing(...)` also disables communication mode on
room disconnect/destroy so stale route state does not leak into the next START.
Follow-up correction: default phone speaker + phone MIC should also stay in
`MODE_NORMAL` when no real headset communication target exists; the native
owner now reports `normal_phone_output` rather than forcing a speaker/earpiece
communication device. If Android rejects a selected Bluetooth communication
device while media Bluetooth output exists, it falls back to
`normal_bt_output` instead of remaining in a half-communication state. This is
still a local mic-track route fix; rebuilt iQOO proof must show non-zero
`frames/ch/readSr/peak` before APP-015.23/APP-024 can advance.
2026-05-18 iQOO screenshot showed `pref auto` still reached
`mode communication` / `out earpiece` and `microphone_start_exception`. The
route owner now makes `auto`, `system_default`, and `phone_mic` unconditional
media-mode capture preferences (`MODE_NORMAL`), leaving explicit Bluetooth mic
as future/manual. It also clears stale speakerphone pins and exposes the full
native start exception message in HUD for the next run. Follow-up hardening
forces the Java route singleton and C# facade back to observe/media mode during
route initialization, and the App-owned Android `AudioRecord` source now uses
LiveKit's custom PCM source contract while setting both Unity-SDK sample-rate
defaults before each retry source is constructed.
Rebuild/iQOO proof is still required.

Latest APP-015.23 greeting-only uplink fix: iQOO screenshots now prove the
placement greeting/downlink path is alive but user speech still does not
advance the dialogue. `AndroidPcmMicCapture` now tries Android `MIC` before
`VOICE_COMMUNICATION`, reports `source_name` in native state, and
`AndroidPcmMicrophoneSource` accepts fast Java PCM callbacks during native
startup instead of dropping them before `_started` flips. Rebuilt phone proof is
still required; if HUD frames/peak are healthy but Brain stays silent, the next
bug bucket is remote track subscription or STT ingestion.

Latest APP-015.25 callback-thread hygiene: Android AudioRecord fallback invokes
Unity through an `AndroidJavaProxy` callback thread. Formal App diagnostics on
that hot path now avoid `Mathf.*` and use pure C# math for frame length,
channel clamp, and peak calculation. This reduces avoidable Unity thread
affinity risk while keeping LiveKit room/token/Brain lifecycle untouched.

Latest APP-015.23 SCO/bridge diagnostics follow-up: confirmed SCO routes now
get a short settle window but a short startup probe timeout, so a dead Bluetooth
voice route should fall through to system/default or phone-mic recovery instead
of spending repeated full mic timeouts before the user can speak. Native
AudioRecord bridge startup now classifies missing APK bridge/classes as
`android_pcm_bridge_unavailable:*`, distinct from `pcm_callback_failed:*`,
native init errors, and Brain/STT consumption. Static guard and Java compile
pass; rebuilt iQOO proof still needs non-zero HUD `frames/ch/readSr/peak` or a
specific `native=` / `nerr=` blocker before APP-015.23/APP-024 can advance.

Latest APP-015.23 fake-silence guard: the "only placement greeting, no user
turn" phone evidence means we must distinguish real uplink from a local source
that publishes fresh but digital-zero frames. `MicrophonePublisher` now watches
Unity `MicrophoneSource` peak level after startup; if fresh frames stay below
the zero-peak threshold for the watchdog window, it treats the Unity path as a
fake uplink and rebuilds directly with Android `AudioRecord` attempts. HUD now
adds `nz=` (age since source-start or last non-zero peak) beside
`frames/ch/readSr/peak`, and native AudioRecord state JSON is fixed so
`source_name` parses cleanly before `recording`. This is still a local-track
rebuild only: no room reconnect, no token mint, no Brain dispatch.

Latest APP-015.23 Brain RoomIO binding fix: greeting-only evidence also exposed
a server-side participant-binding risk. LiveKit Agents `RoomIO` auto-selects
the first accepted remote participant when no `participant_identity` is set, so
a stale Unity/Web/diagnostic participant in the long-lived Castle room can make
Brain greet through RPC while listening to the wrong audio/video input. Brain
now rebinds `session.room_io` to the current Unity `caller_identity` on
`onSceneReady` and `onGosloPlaced`. This is an AgentSession input fix only: no
Unity room reconnect, Mint token refresh, Brain redispatch, RoomSetting change,
or Android audio-route mutation. Next iQOO proof should pair HUD
`frames/ch/readSr/peak/nz` with Castle logs showing `RoomIO input participant
rebound` and actual user transcript events.

Latest APP-015.23 focus-resume local capture fix: Android app switching,
permission dialogs, and Bluetooth settings focus hops can reset the
communication device or microphone session while the LiveKit room stays alive.
`MicrophonePublisher.OnApplicationFocus(true)` now refreshes the current route
snapshot and queues/restarts only the local mic source/track. This is explicitly
not a room reconnect, Mint token refresh, Brain dispatch, or RoomSetting apply.

Latest APP-015.23 AudioRecord zero-peak fix: fresh `AudioRead` frames are not
enough if the samples are digitally silent. If Android AudioRecord's plain
`MIC` source stays zero-peak beyond the watchdog window, the formal App now
rebuilds only the local mic track and retries the AudioRecord ladder with
`VOICE_COMMUNICATION` preferred. If that path also stays silent, health/HUD
remain degraded with `uplink_watchdog_zero_peak_android_audio_record` instead
of claiming the mic is healthy.

Latest APP-015.23 route override lifetime fix: temporary native capture
overrides are now explicitly session-local. When communication mode is disabled,
`AudioRouteManager` restores the native bridge to the durable user preference so
a previous phone/default mic recovery cannot silently poison the next START.

Latest APP-015.23 compatibility sweep: see
`backend_interface_map/app/unity_audio_livekit_deep_audit_20260517.md`.
External Unity/Android/LiveKit checks confirm this is a fragile but valid stack:
Unity officially recommends built-in mic fallback for unresolved Android
Bluetooth mic issues, LiveKit Unity remains Developer Preview and has open
Android audio-source reliability reports, Android may legally silence a capture
client when another higher-priority app owns input, and Android 15+ native
16KB alignment remains a release packaging gate. The formal policy stays:
Android native route truth, Unity/LiveKit local mic executor, serial mic-track
rebuild only, phone/default MIC fallback, and Brain RoomIO rebinding to the
current Unity phone. Remaining gate is a rebuilt iQOO proof with HUD local
capture fields plus Castle Brain transcript/rebind logs.

### App Lane Execution Order (Current)

This order is binding for the next Unity App slices unless the user changes it
in this chat. Do not jump directly from START proof into homepage UI code.

| Order | TODO | Gate |
|:--|:--|:--|
| 1 | APP-018, APP-022, APP-023, APP-015.19 | Responsibility audit for ECP, LiveKit data flow, Mint, App HTTP, RPC, SVA/video, and Unity local state is mapped in `unity_livekit_ecp_sva_data_flow_map_20260515.md`. Continue by turning APP-022/APP-023 gaps into implementation tasks, not by adding new ad-hoc channels. |
| 2 | APP-016, APP-016.1, APP-015.18, APP-015.20 | Smoke UI demotion is done and the formal homepage reuse audit is now indexed. Old menu/RPC surfaces are classified: Smoke is reference-only, old menu read/write RPCs stay removed, HTTP owns durable menu data, and compact RPCs are allowed only for latency-sensitive in-room controls. |
| 3 | APP-020 | RoomSetting `New`/`Save` persistence from the formal Unity App HTTP path is verified; continue to connection-stability foundations before relying on homepage menu state. |
| 4 | APP-015.12, APP-015.14, APP-015.15, APP-015.17 | Implement connection-stability foundations: fresh-token reconnect/backoff, degraded/silent/session-hold states, 2D pause policy, audio-route policy publishing, and ECP event parsing. |
| 5 | APP-015.4 | Main-ready gate owner is implemented; future loaders must satisfy its HUD/menu/model/AR gates instead of calling `ReportRunning()` directly. |
| 6 | APP-015.6, APP-015.7, APP-015.16, APP-015.20, APP-015.21, APP-015.22, APP-015.24, APP-015.25 | Continue formal homepage loaders: HUD shell exists, menu canvas HTTP loading exists, compact toolbar plus separate menu/settings panels exist, menu-owned apply paths now use App HTTP, read-only persona/line-profile catalog status exists, startup/menu/reconnect degraded states are visible in the HUD, model/AR baseline gates exist, AR runtime bootstrap exists, runtime GOSLO/Ner visuals are separated under `Resources/Models/**`, and the reuse audit/implementation plan is indexed. The first placement owner `onGosloPlaced` path exists with AR raycast + Resources visual + fallback; the first local model joystick owner exists after placement; CAM is the only active toolbar tool owner right now; MAG/BBox are delayed placeholders; the first XRHand/perch owner exists but stays debug-only/package-missing until XR Hands and phone proof. Next implementation is visual/phone verification of the shell, selector edit/apply owner flow, note/audio-route owner wiring, XRHands package validation, model animation slices, and phone AR/video proof. Do not copy Smoke UI wholesale. |
| 6a | APP-015.27 | Keep SVA/attention semantics separate while building the home shell: camera/photo mode, identify_object, BBox, and magnifier may share timestamps/refs later, but homepage V1 must only create honest UI hooks and metadata surfaces. Web/backend owns the actual video-frame sampling and GOSLO Intent upgrade. BBox/MAG implementation is after phone stability. |
| 6b | APP-015.29 | Before enabling BBox/MAG buttons, confirm the tool packet fields, HTTP/storage asset upload path, IntentWorkspace staging policy, and L2-B graph-link policy. The App side owns the touch/AR tool UI and animation affordances; backend owns evidence alignment, attention thresholding, and GOSLO awareness delivery. |
| 6c | APP-026 | Treat BBox/MAG backend unblock as the immediate cross-lane gate before adding more Web-only feature work: the backend/App V1 lifecycle packet and asset upload route now exist, so App can build visual controllers behind feature flags. Production publish waits for APP-024 phone/screen-share smoke and UI/body-feel tuning. Existing `bbox.placed` / `focus.anchored` remain compatibility pulses; CORE-014 is a small visual-tool evidence contract, not DSG L3. |
| 7 | APP-015.8, APP-015.13, APP-024 | Validate the formal App on iQOO Neo9: mic, Bluetooth/SCO/A2DP, app switch/background/resume, network flap, AR/video publish, reconnect, and no fake success. BBox/MAG active attention tooling is after this stability pass. |

## Web Lane

Owner: Web Console chat.

| ID | Status | Item | Business doc | Core candidate | Notes |
|:--|:--|:--|:--|:--|:--|
| WEB-001 | done | Web Console requirements summary, information architecture, visual direction, and implementation TODO list. | `backend_interface_map/web_console/web_console_step1_console_plan_20260513.md` |  | Step 1 docs and directory discipline are in place. Split visual language remains: Obsidian-like console first; Papers Please-inspired interaction layer later. |
| WEB-002 | done | ECS/module health and orchestrator `/status` console skeleton. | `backend_interface_map/web_console/observability_runtime_business_flow_20260513.md` | CORE-003 | Implemented BFF/static shell in `src/parrot/web_console/` + `web/console/`; includes status topology and zh/en setting. When `PARROT_ORCH_SECRET` is set, BFF sends `Authorization: Bearer <secret>`. |
| WEB-003 | in_progress | L1.5 management, L2-B visualization, node CRUD boundary, and photo management read model. | `backend_interface_map/web_console/memory_graph_workspace_business_flow_20260513.md` | CORE-006, CORE-008 | Added Web-only operator-safe BFF routes for `/api/l15/pool`, L1.5 bucket op drafts, Obsidian three-profile node drafts, L2-B node create/update/delete drafts, and L2-B edge draft/dry-run controls. Memory UI now has graph filters and an operator workbench with L1.5 bucket op draft/dry-run controls plus L2-B target UUID update dry-run; true writes still require explicit operator mode. |
| WEB-004 | in_progress | Blackboard, IntentWorkspace, Plan/task, Scheduler, Nanobot, AgentTeam/Maid Team status monitoring. | `backend_interface_map/web_console/observability_runtime_business_flow_20260513.md` | CORE-001, CORE-002, CORE-003, CORE-004 | Runtime Monitor exposes the read snapshot plus a Trigger Lab for catalog/draft/fire dry-run receipts. Plan rows include Web-only DAG nodes/edges, ready/blocked/critical hints, and clickable step detail; Blackboard/IntentWorkspace have polling diff row highlights; Blackboard has a Web-only py-trees activity drawer; Scheduler/Nanobot collaboration now renders as a safe channel-flow topology. |
| WEB-005 | in_progress | GOSLO/Nanobot collaboration, chatroom plan, and task-scheduler monitoring. | `backend_interface_map/web_console/observability_runtime_business_flow_20260513.md` | CORE-004, CORE-005 | Added Web dry-run routes for Nanobot Gmail `message_check` dispatch and synthetic `message_push` trigger tests. 2026-05-14 cleanup moved `MessageNotificationTrigger` to `TriggerOutcome.commit_observations` with `GOOGLE_MESSAGE` L1.5/Ingest routing, so message tests now match Calendar/Obsidian write boundaries. |
| WEB-006 | in_progress | Graphiti/FalkorDB management: read, search, partitions, visualization, Episode, Graphiti API node/edge/fact surgery, and FalkorDB operator mode. | `backend_interface_map/web_console/graphiti_management_business_flow_20260513.md` | CORE-006 | Web BFF/UI now exposes Graphiti status, partition search, Episode draft, and dry-run Episode write. FalkorDB direct writes stay Web operator only with dry-run/audit/backup posture. |
| WEB-007 | approved | Web Evidence/String Board using the shared Ref/Edge data model while keeping the Web renderer independent. | `backend_interface_map/web_console/memory_graph_workspace_business_flow_20260513.md` | CORE-006, CORE-007 | Do not invent a separate storage format for board edges; allow Red String and future edge renderers through renderer/style ids. |
| WEB-008 | approved | Web lane doc hygiene and core-change guardrail. | `backend_interface_map/web_console/README.md` |  | Business docs stay under `web_console/`; shared gaps go to the candidate queue, never directly to core SSOT. |
| WEB-009 | in_progress | Web LineB no-video voice lab, React LiveKit/Brain Bridge, and Menu Canvas smoke surface. | `backend_interface_map/web_console/observability_runtime_business_flow_20260513.md` |  | Implemented BFF/UI control-path smoke: LineB route, TTS segment, mic/asr classification, line profile list/apply, canvas snapshot, workspace apply, Web LiveKit join-token mint, browser audio client wiring, and visible LiveKit connection/transcript event panes. 2026-05-15 React Runtime Bridge can mint a Web token, join the configured LiveKit room without camera, attach remote agent audio, and publish screen share as `web-console-screen`; browser smoke connected and subscribed to an `agent-* / audio / microphone` track. Full conversation verification still needs user-approved mic/screen-share clicks plus a running server-side LineB/LiveKit Agents session. |
| WEB-009.1 | done | React Runtime LiveKit/Brain Bridge no-camera path | `backend_interface_map/web_console/observability_runtime_business_flow_20260513.md` | WEB-015 | Added Runtime page controls for room/identity, token mint, connect/disconnect, microphone toggle, screen-share publish, screen-share sample check, remote-audio attach, event log, and transcript log. The UI stores the short-lived token in component state only, receipts show token length rather than raw token, and the LiveKit browser SDK log level is lowered so browser diagnostics do not echo join-token payloads. Bugfix: `检查采样` no longer treats any fresh frame as a successful screen-share smoke; it only succeeds when the latest/nearest evidence also looks like a screen-share source and otherwise records a warning with `fresh_any_evidence` vs `screen_share_confirmed`. Follow-up: Runtime Time/Evidence now auto-refreshes every 3s and `检查采样`/screen-share start poke the evidence panel so sampler/frame-cache freshness is visible without manual page refresh. | Next smoke with the user: click screen share, choose a window, press `检查采样`, confirm Brain sampler/frame-cache sees the screen track as fresh evidence, then test no-camera voice with Bluetooth/headset routing. |
| WEB-010 | in_progress | Real-time Memory Visualizer plan/research: L2-B graph, Blackboard, IntentWorkspace, Ref links, and future Evidence/String Board renderer. | `backend_interface_map/web_console/memory_graph_workspace_business_flow_20260513.md` | CORE-006, CORE-007, CORE-008, CORE-009 | Renderer tracks new/changed L2-B nodes, Blackboard keys, and IntentWorkspace refs; it supports filters, grouped rows, soft Intent/Ref-to-L2-B links, bounded render modes, visible edge path/cross-compartment hints, and a collapsed Blackboard Activity drawer. 2026-05-15 added Web-only Memory changed-since V1. 2026-05-16 adds Web-only read SSE at `GET /api/memory/live-state/stream`, wrapping the same `memory_runtime_delta_v1` event shape; React Memory now attempts EventSource and falls back to polling when disconnected/unavailable. |
| WEB-011 | in_progress | React Memory Graph Workspace: L1.5/L2-B/Graphiti/Ref/Evidence page with large graph canvas, direct node/edge dry-run CRUD, bucket board, receipts, and drawers. | `backend_interface_map/web_console/memory_graph_workspace_business_flow_20260513.md` | CORE-006, CORE-007, CORE-008, CORE-009 | User audit split the old Visual Memory Operations Cockpit: Memory work stays on this page; Runtime/Plan/Blackboard/Nanobot collaboration moves to WEB-012. Formal frontend route is React + Vite under `web/console_app/`; vanilla console is transition/reference only. |
| WEB-012 | in_progress | React Runtime Flow Workspace: GOSLO Intent -> Plan/HITL -> Blackboard -> IntentWorkspace -> Scheduler -> Nanobot -> Message/Trigger swimlane/DAG, event tape, manual triggers, and HITL V1. | `backend_interface_map/web_console/observability_runtime_business_flow_20260513.md` | CORE-001, CORE-002, CORE-003, CORE-004, CORE-005, CORE-009, CORE-010, CORE-011 | New long-running Web lane task approved 2026-05-13. First backend slice adds Web-only `/api/runtime/flow`, changed-since polling diff, and HITL draft/apply routes. Plan/Scheduler/Nanobot result wiring is part of the backend capability upgrade; App DTOs stay untouched. Temporary research/audit ledger: `backend_interface_map/web_console/_tmp/runtime_flow_memory_upgrade_research_20260513.md`. |
| WEB-013 | pending | Full-screen L2-B realtime graph page and engine adapter plan: React Flow remains Memory Canvas/editor; React-Force-Graph/Cytoscape-style renderers are evaluated for L2-B monitor, subgraph, cluster, filter, and high-volume topology. | `backend_interface_map/web_console/memory_graph_workspace_business_flow_20260513.md` | CORE-008, CORE-009 | User audit 2026-05-14 asked to stop treating the Memory Canvas as the final L2-B knowledge-graph monitor. First step is L2-B/RustworkX capability inventory and transport design; backend/core gaps stay in candidate queue until reviewed. |
| WEB-014 | in_progress | Graphiti/L1.5 import test upgrade: DeepSeek V4 Graphiti config, `arknights_test` partition, temporal Arknights episode fixture, Graphiti natural-language search to L2-B subgraph export, Obsidian/Google source-board import previews, and L1.5 source board refactor. | `backend_interface_map/web_console/graphiti_management_business_flow_20260513.md`, `backend_interface_map/web_console/memory_graph_workspace_business_flow_20260513.md`, `backend_interface_map/web_console/graphiti_l2b_ref_identity_design_20260517.md` | CORE-006, CORE-008, CORE-009, CORE-015 | Temporary ledgers: `backend_interface_map/web_console/_tmp/l15_graphiti_import_test_20260515.md` and `_tmp/graphiti_l2b_ref_identity_workplan_20260517.md`. Design conclusion: `roleplay` is a mode/profile that may group many source packs, not a singleton UI bucket. Arknights imports use compact original summaries/facts plus source URLs/descriptions and should preserve chapter/time/state changes for Graphiti temporal extraction. 2026-05-16 adds unified import-plan routes for Graphiti, Obsidian, and Google Calendar, combining L1.5 Observation drafts, source-specific rows, and CORE-013 destination policy before any apply. Empty source selections now skip destination policy instead of showing a fake graph placement plan; import-plan receipts always remain `dry_run=true/operator_mode=false` even if a caller asks for apply flags. 2026-05-17 adds the Graphiti/L2-B/Ref identity design note and first CORE-015 Web route skeleton: Episode is provenance, IdentityMap owns UUID equivalence, RefIndex owns mutable locators, and Graphiti raw relation data must be preserved rather than collapsed into EdgeKind enums. |
| WEB-015 | in_progress | Time-aligned Evidence / `identify_object` backend upgrade: unified TimebaseStamp, Temporal Evidence Ledger, Focus/BBox/photo/frame evidence rows, Web evidence debug routes, and no old snapshot RPC path. | `backend_interface_map/web_console/observability_runtime_business_flow_20260513.md`, `backend_interface_map/web_console/memory_graph_workspace_business_flow_20260513.md`, `.cursor/memory/architecture/Interface/time_aligned_evidence_interface_20260515.md` | CORE-006, CORE-009, CORE-012, CORE-014 | First slices implemented 2026-05-15: `TimebaseStamp` / `TimeAlignedSampleRef` live in `parrot.brain.vision.evidence`; ECP top-level schemas stay unchanged and only payload/meta timebase is consumed. `identify_object` now resolves stored evidence or records pending requests; Web exposes `/api/vision/evidence/*` plus `/api/app/test/visual-attention`. Continuations added storage-backed `parrot.brain.vision.frame_cache`, `record_livekit_frame_bytes()`, Web/operator frame-cache upload, React Cache Frame smoke, a Brain room-scoped `livekit_sampler`, stored-image/crop VLM describe, IntentWorkspace staging/session-policy decisions, ContextInjector C3 chat-context delivery, Photo Awareness C3 delivery, and the conservative threshold bridge. 2026-05-16 App-blocker implementation adds CORE-014 backend/App V1: `parrot.brain.vision.tool_lifecycle`, ECP `visual_tool.lifecycle`, App routes `/api/app/visual-tool/event` and `/api/app/visual-tool/asset/{asset_id}`, Web debug route `/api/vision/evidence/tool-lifecycle`, and BB receipt `transient/visual_tool_lifecycle_receipt`. Formal App controllers are no longer blocked by missing backend surface; production enablement still needs phone/screen-share smoke and C3/C4 body-feel review. |
| WEB-016 | in_progress | L2-B graph rewrite / graph transform / incremental update policy: pool import destination, persistent subgraph/cluster overlays, foldable subgraphs, controlled aggregation/merge operations, graph health metrics, and useful L2-B analysis presets. | `backend_interface_map/web_console/memory_graph_workspace_business_flow_20260513.md`, `backend_interface_map/app/canvas_menu_ref_workspace_app_interface_20260513.md` | CORE-006, CORE-008, CORE-009, CORE-013 | Research gate first pass completed: read Parrot bridge, RustWorkX, L2-B organization, L1.5/ConceptGraph, attention skills, and current L1.5/L2-B code. Chosen constraint: canonical RustWorkX topology plus typed overlays/receipts; no `WorkspaceNodeKind`, no IntentWorkspace-as-bucket, and no silent whole-graph rewrites. 2026-05-15 first backend slice added Web-only CORE-013 draft/read routes for import destination, subgraph overlay, graph transform, and graph health. 2026-05-17 live-context slice adds a true read path for selected L2-B UUIDs -> bounded ego subgraph -> WCC clusters -> overlay draft. Persistent overlay storage/apply remains next. |
| WEB-017 | in_progress | Web Console fixed mainline roadmap and research-gate discipline: order RustWorkX graph policy -> realtime stream -> Memory operations -> source imports -> Runtime Flow collaboration -> full-screen L2-B monitor -> SSOT review. | `backend_interface_map/web_console/web_console_major_roadmap_20260515.md` | CORE-008, CORE-009, CORE-010, CORE-011, CORE-013 | Roadmap and user decision questionnaire are now resolved. 2026-05-16 user decisions are recorded: React Flow stays the L2-B operation/editor page; future React-Force-Graph page is the full-screen monitor; SSE is first realtime; receipt stream is separate; Google manual fetch/import is V1; Obsidian roleplay is a profile with many source packs; Arknights is a large temporal pack; Runtime Flow is swimlane overview plus workflow detail. Every large implementation round must still repeat the relevant skill/code/interface/UX research gate and define its true-vs-fake completion signal before code changes. |

Latest WEB-014 M16: Graphiti search/export/import-plan receipts now include a
schema-versioned `graphiti_bundle` that preserves raw envelopes, fact/entity/
episode/community sections, UUID lookup payloads, search plan/config,
edge/ref drafts, and the L2-B import overlay together. Validation: full Web
route tests report `83 passed`. Next Web proof is ECS/app-monitor deployment
plus a live 7893 -> 8790 canary for `_search` mode and bundle section counts.

Latest WEB-014 M17: the 7893 Source Board now renders `graphiti_bundle`
directly with section counts, search/lookup summary, projection policy,
import overlay, and sample rows. Frontend typecheck/build passed and Web route
tests remain `83 passed`. 7893 was restarted with ECS Graphiti proxy; live
search returned true fact UUID `0ea2009c-402d-4332-81b4-31fa57e67688`, bundle
counts `facts=3/entities=4/episodes=0/communities=0`, and import overlay
`isolated_compartment`. ECS 8790 still needs the M15/M16 adapter before remote
`_search` mode can be proven.

Latest WEB-014 M18: the ECS/app-monitor adapter is deployed. 8790 now forwards
Graphiti SearchConfig recipe/filter inputs, exposes remote subgraph import-plan,
and returns true `_search` receipts for `combined_rrf + Entity/CrisisFact`.
Remote 8790 and 7893 passthrough canaries both returned first UUID
`0ea2009c-402d-4332-81b4-31fa57e67688`, bundle counts
`facts=3/entities=4/episodes=3/communities=0`, UUID lookup `10/10`, and import
overlay `isolated_compartment`.

### WEB-011 Detail Checklist

This checklist stays here instead of being split into throwaway docs so the long task does not disappear during multi-round work.

| Step | Status | Scope | Implementation Notes | Audit Signal |
|:--|:--|:--|:--|:--|
| WEB-011.1 | done | Memory cockpit IA and first visual shell | Replaced the dense Memory page with a cockpit shell: compact status band, source rail, larger L2-B canvas workspace, selected-node inspector, edge draft controls, receipt stream, and separate advanced Obsidian draft drawer. Preserved current BFF routes and DOM ids. | `node --check web/console/assets/app.js`; duplicate-id scan clean; in-app browser Memory smoke clean with no console errors. |
| WEB-011.2 | in_progress | Active-view live refresh | Memory active view now caps polling at 5s even when the BFF default remains `PARROT_WEB_CONSOLE_REFRESH_S=15`; Runtime still uses the configured cadence. 2026-05-15 React Memory switched from broad `/api/app/live-state` refreshes to Web-only `/api/memory/live-state/changes`, which returns `changed=false` and no snapshot when L2-B/Blackboard/IntentWorkspace/Ref content is stable. This is still polling/diff, not SSE/WebSocket true realtime. | UI shows refresh cadence and changed nodes without exposing secrets; no frontend secret is rendered. Current validation covers no-op changed-since behavior. |
| WEB-011.3 | in_progress | Direct L2-B canvas operations | React Memory canvas now supports direct React Flow drag/connect to draft an L2-B Edge and render a frontend-only preview Edge. Successful Node drafts auto-stage From/To endpoints, so two Node drafts can immediately produce an Edge preview. React Flow now uses loose connection mode plus custom Obsidian-like Memory Node cards with four visible directional handles; internally each side maps to `source-*` / `target-*` handles so preview and persisted edges attach to the intended side instead of falling back to top-to-top. Node positions are tracked through React Flow node changes so preview Nodes can be dragged instead of snapping back. The former fake Blackboard/Intent/Refs/L2-B placeholder boxes were replaced with a compact semi-transparent status overlay. Draft IDs include a random suffix to avoid rapid-click collisions. Empty-canvas double-click creates a Node preview at the clicked flow position and disables default double-click zoom. Blank-canvas click clears Web detail selection, Node/Edge selected state is controlled from React state, and `聚焦/Focus` + `整理/Layout` recover view context. Left nav and right records rail are collapsible; records default collapsed to protect canvas width. The canvas toolbar is now collapsed by default and exposes Edge endpoint inputs in an expandable tool drawer. React Flow controls/minimap and edge labels are dark-mode styled; topbar/empty-state descriptions were reduced. Remaining work: better force/local graph layout, Node/Edge info panels, label/subgraph/cluster CRUD, and real operator execution UI. True writes remain operator gated. | Typecheck/build/Web route tests pass; static scan confirms the previous awkward graph/record Chinese labels are gone from React source/dist. Local HTTP checks returned `200`; browser smoke reconnected and found Focus/Layout/Records text with zero console errors. |
| WEB-011.4 | in_progress | L1.5 pool cards | React Memory now shows a compact L1.5 health panel with total nodes, refs, capacity pressure, current scene, per-bucket node meters, frozen/open state, and last activity. Bucket cards still expose freeze/unfreeze/clear quick draft buttons and preserve the L1.5 route path. Added a simple Obsidian setting-node draft card for the three profiles: `daily` and `roleplay` support UUID-free draft receipts, while `ref` now shows the UUID requirement before draft and returns a local failure receipt if UUID is missing. BFF normalization now falls back from blank/whitespace setting labels to `Web Console setting` so generated note keys are not empty. 2026-05-15 design correction: `roleplay` should be presented as a mode/profile that can contain many source packs or virtual source buckets, not as one fixed UI bucket. Remaining work: refactor into source board with Obsidian/Google/Graphiti/Manual imports. | Bucket and Obsidian actions return receipts and do not bypass L1.5. Tests cover blank-label fallback; browser smoke confirmed ref-without-UUID warning/failure receipts get unique local receipt ids and zero console errors. |
| WEB-011.3a | done | Memory canvas toolbar and selection layout slice | Replaced the wide graph form strip with a compact icon toolbar over the canvas. Tools now open one dock at a time: Node, Edge, Subgraph, Filter, Tag, State color, L1.5 Pool, and Settings. L1.5 Pool/Source Board now live in the Pool dock, while selected Node/Edge details render as a floating canvas inspector. Added state-color markers and a visual subgraph preview box. | Browser smoke confirmed toolbar labels, Node preview, subgraph preview, Pool panel, and zero console errors; `npm run typecheck`, `npm run build`, and Web route tests pass. |
| WEB-011.5 | in_progress | Manual trigger palette | Runtime workspace now has a grouped React trigger palette with message check, message push, LLM context push, scheduler tick, calendar test, scene switch, and roleplay-open dry-run buttons. It also reads `/api/dsg/triggers/catalog` and renders registered trigger chips by trigger kind (`startup`, `periodic`, `event_driven`, `on_demand`) so preset buttons can be compared with the real backend registry. 2026-05-14 cleanup fixed on-demand event shapes, made `TriggerRunner.fire_event()` route `ON_DEMAND`, moved trigger implementations to `TriggerOutcome`, and replaced the single raw receipt block with a receipt timeline. Remaining work: richer catalog labels/sample payload editing, agent prompt presets, and explicit operator execution guard copy. | Browser smoke: Runtime page showed registered trigger chips including `event_driven` and `on_demand`, LLM context push returned `dsg.trigger.draft_event` with matched trigger text, and console errors stayed at zero. Message buttons continue through Nanobot dispatch routes; message results commit through `GOOGLE_MESSAGE` L1.5/Ingest. |
| WEB-011.6 | in_progress | Blackboard visual renderer | Empty L2-B graph now shows a DSG state map with Blackboard/Intent/Refs/L1.5/L2-B compartments; Blackboard rows are status-light cards grouped by scope/writer. Remaining work: dedicated scope/writer lanes, activity timeline, and graph/detail links from blackboard keys. | Blackboard count and recent writes are visible without opening raw JSON. |
| WEB-011.7 | in_progress | IntentWorkspace visual renderer | Empty L2-B graph now shows Intent as a DSG compartment; Intent refs render as status-light cards grouped by role/owner with expiry/link hints. Remaining work: validity/pressure visuals and richer links to L2-B/Plan. Attention/decay/validity changes must cite the attention-schema skill. | Intent changes are visible and grouped; no new shared DTO without candidate review. |
| WEB-011.8 | pending | Plan/task visual renderer | Render plan phases/steps as a DAG or swimlane with ready/blocked/critical path hints. Keep Web read model separate from Unity App DTO. | Plan state is understandable without reading raw lists. |
| WEB-011.9 | pending | Menu Canvas hot-switch UX | Workspace/line/menu canvas switching should show optimistic selection, receipt timeline, active canvas diff, and failure rollback. | Hot-switch feels immediate and audit-safe. |
| WEB-011.10 | pending | Evidence/String Board route | Start as Web renderer over shared Ref/Edge data, with red-string as `renderer_id` / `visual_style_id`, not a storage-only field. | Board uses CORE-006/CORE-007 candidates only. |
| WEB-011.11 | in_progress | Round audit | Current slices updated the root backend-interface SSOT with the active Web business-interface index, kept route details in `web_console/README.md`, and recorded the Memory cockpit preview/edge/toolbar/retarget/terminology polish in the module business file. Continue this audit after every implementation slice. | No scattered docs, no App DTO pollution, no frontend secret leak; the latest polish changed only React renderer state/copy and existing dry-run BFF calls. |
| WEB-011.12 | done | L2-B RustworkX capability inventory | First pass completed in `memory_graph_workspace_business_flow_20260513.md`: stable Node UUID vs internal RustWorkX index, Node semantic/runtime/provenance/grouping fields, Edge kind/strength/source/meta/cross-compartment shape, endpoint-filtered Edge surgery, L2-B filtered views, WCC clustering, bounded BFS/iterative spreading activation, IntentEvent boundary hooks, Graphiti bridge, and explicit gaps for graph health, PPR, VF2, stable Edge id, typed source_meta, and SSE/WebSocket deltas. | No App DTO changed. Shared gaps stay candidates only; Web renderer prep uses `web/console_app/src/graphModel.ts`. |
| WEB-011.13 | in_progress | Memory Canvas CRUD completeness | First React Flow operation pass: Memory page now has a compact icon toolbar for Node, Edge, Subgraph, Filter, Tag, State color, L1.5 Pool, and Settings; selected Node/Edge details moved into a floating canvas inspector; Node delete and Edge update/delete have Web dry-run/operator-gated route coverage; subgraph creation is a visual grouping preview box only. 2026-05-15 bugfix: Tag input no longer reuses Edge meta state, the floating inspector close button no longer renders a corrupted glyph, L2-B Edge update/remove now has direct RustWorkX regression coverage, canvas Edge rendering filters out edges whose endpoints are hidden by the active Node filter, and the floating inspector now closes when a Filter hides the selected Node/Edge. 2026-05-17 bugfix: the Memory tool dock and selection inspector are now independent Photoshop-like canvas panels: both can be focused, closed, dragged by the header, and resized, so L1.5/source tools no longer fight selected Node/Edge details for the same right-side space. Follow-up polish: the icon toolbar now has its own drag grip, can dock vertically on side edges, and floating canvas panels now use stronger snap on drag/release plus resize clamp to canvas and nearby panel edges. Unmoved tool/selection panels default to a wider right-side stacked dock sized to the current canvas, so the expanded/collapsed operation-record rail does not leave them born in the old left/center positions. Resize now uses explicit left/right/bottom/bottom-corner handles instead of the browser-only bottom-right corner, so docked panels can be expanded from either boundary; after the 2026-05-17 recording review, handles were moved inside panel bounds, panel horizontal scroll was disabled, the minimum panel width was raised, and ResizeObserver now writes the real border-box size instead of content-box width so manual resizing is not immediately shrunk back. 2026-05-17 library review: the newest recording still shows snap/resize recognition drift, so the next implementation must stop extending the custom coordinate-threshold panel system. 2026-05-17 implementation: `react-rnd@10.4.13` now owns floating toolbar/tool/selection drag, resize, parent bounds, min/max sizes, and grid increments; the custom pointermove/ResizeObserver panel substrate and manual resize-handle component were removed. Follow-up default-layout fix: Memory now opens with no active tool panel, initial React Flow `fitView` is capped at a modest zoom, Focus/Layout also cap max zoom, selected Node/Edge details are fixed to the right-side inspector default, and when a tool panel is also open the tool panel yields left if there is enough width. Default panel positions are now computed before render and copied into state on drag start, so panels should not flash from a temporary top-right/default coordinate before settling. Panel edge docking remains a lightweight release-time boundary clamp; if true guide snapping or panel-to-panel docking is required next, evaluate `interact.js` snap targets or `dockview` rather than rebuilding heuristics. `Animal-Island-UI` is recorded only as a future 2D workspace visual/reference candidate, not as the current utilitarian console theme. L2-B fallback de-dup also changed from graph-wide label substring matching to `NodeKind + exact label` after stable source IDs fail; different kinds may reuse the same visible label, while repeated same-kind labels still merge. Remaining work: persistent subgraph/cluster model, richer label/tag mutation policy, source/bucket assignment UI, and final full-screen L2-B monitor. | Each action needs preview receipt, operator gate, and no App DTO pollution. Latest validation: `npm run typecheck`, `npm run build`, `git diff --check`, and local Web 200 smoke pass. |
| WEB-011.13a | done | Floating panel left/top regression fix | Corrects the WEB-011.13 follow-up: render-time transient default panel coordinates were removed after they could clamp the selected Node/Edge inspector to the canvas left/top corner during a zero-size first layout frame. Tool/detail open events now first write the right-dock state, increment a remount version, and then open/select, so closing and reopening Node tools or selecting a Node/Edge no longer depends on a later layout correction. The outer panel `panel-pop` transform animation was replaced with opacity-only `panel-fade` because it was overriding `react-rnd`'s translate transform and causing every floating panel to visibly spawn from the left/top before moving. Follow-up collision fix: when tool and detail panels are both open, wide canvases place them side-by-side; narrow canvases use a right-dock vertical split with bounded heights, so the operation-record rail can expand without the panels fighting for the same position. Default tool/detail width is wider for readability, and Memory still opens with no active tool panel. | Validation: `npm run typecheck`, `npm run build`, `git diff --check`, and local Web 200 smoke pass. No L2-B DTO or App DTO changes. |
| WEB-011.14 | in_progress | One-click import/preload actions | Design safe buttons for Google Calendar bucket, Obsidian setting profile packs, WorkIntent subgraph/workflow, and selected Ref/Graphiti slices. These should route through L1.5/trigger/Graphiti BFF receipts rather than directly mutating L2-B by default. WEB-014 owns the first concrete import pipeline: Obsidian scan/preview/import preview, Google Calendar raw-to-observation preview/import preview, Graphiti search/export draft, and source-board receipts. | Document exact BFF route or candidate gap before implementation. Current concrete routes include Obsidian scan/import-draft/import-plan/import, Google Calendar preview/import-draft/import-plan/import, and Graphiti subgraph search/export-draft/import-plan/export; all real writes remain operator gated. |
| WEB-011.15 | in_progress | Visual component/reference library | Added `web/console_app/src/graphModel.ts`, an engine-neutral L2-B render model adapter for React Flow today and React-Force-Graph later. The existing draft skill `codex_workspace/codex_skills/react-force-graph-l2b/SKILL.md` now records official React-Force-Graph, React Flow, Obsidian Graph View, and D3 force anchors plus the adapter boundary. Remaining work: actual full-screen renderer component and examples after UI design is approved. | Store reusable code in `web/console_app/src/` and durable notes in this TODO/business file, not scattered docs. |
| WEB-011.16 | pending | Memory page role refinement | Preserve the current React Flow page as an advanced Memory operation canvas: L2-B draft/edit preview, L1.5 bucket management, Node detail, bound Ref/file/photo management, and IntentWorkspace-related L2-B subgraph guidance. IntentWorkspace is a GOSLO Intent-layer workspace; requests that change it must route to GOSLO Intent/task/workspace-file/Plan-edit flows, not pretend that editing an L2-B graph directly edits IntentWorkspace. | BFF route docs must state whether an action mutates L1.5/L2-B/Ref only or submits a GOSLO Intent/IntentWorkspace guidance request. |
| WEB-011.17 | in_progress | Ref binding and source file detail management | Added a first Web-only Source Board `Refs/Photos` card that reads current RefBinding rows, session PHOTO RefBindings, IntentWorkspace PHOTO refs, and L2-B Photo Nodes from live-state. Added draft-only `POST /api/refs/binding/draft` so operators can preview retarget/bind intent against CORE-006 without mutating the Brain registry. 2026-05-15 bugfix: `target_kind=unresolved` receipts now report `operation=unresolve_ref`, `would_resolve=false`, and `would_unresolve=true` instead of pretending to resolve to a durable target. 2026-05-15 continuations: the selected Node floating inspector now shows read-only Ref/Photo links from RefBinding, IntentWorkspace, and Photo Node asset/episode metadata, and `GET /api/photos/asset/{day}/{photo_id}` serves safe cache-root thumbnails for existing photo assets. Remaining work: manual bind/unbind flows and reviewed operator apply after CORE-006 is ratified. | Manual Ref/file/photo bindings produce receipts, audit fields, and use shared candidate fields only where confirmed; Web-only operator repair actions stay out of App DTOs. |
| WEB-011.18 | in_progress | Memory page realtime and visual state polish | Improve Node base-state visuals, stale/fresh/ref-bound/source-bucket badges, selection previews, and changed-since highlighting. 2026-05-15 backend/frontend V1 added stable-signature changed-since polling for Memory live-state; remaining polish is visual change markers, clearer freshness badges, and eventual SSE/WebSocket evaluation. This page must not be described as fully realtime until an event stream or equivalent exists. | UI communicates refresh mode honestly; no manual-refresh-only hidden state for operator actions. |
| WEB-011.19 | done | Memory live-state changed-since V1 | Added Web-only `parrot.web_console.memory_live_state` and `GET /api/memory/live-state/changes?since=...&limit=...`. The route consumes the existing App live-state builder but keeps its own stable content sequence, ignores only transport noise such as root snapshot sequence/generated timestamps, emits bounded operator-readable events, and returns the full snapshot only when content changes. React Memory now uses this route while Runtime continues to use `/api/runtime/flow/changes`. | `tests/test_web_console/test_web_console_server.py` covers first-change/no-op/content-change behavior; no Unity/App DTO changed; CORE-009 remains draft. |

### WEB-013 Detail Checklist

This checklist tracks the new full-screen L2-B realtime graph page requested on 2026-05-14. It is separate from WEB-011 so the current ReactFlow Memory Canvas can be fixed and preserved while the knowledge-graph monitor gets a cleaner design.

| Step | Status | Scope | Implementation Notes | Audit Signal |
|:--|:--|:--|:--|:--|
| WEB-013.1 | done | L2-B/RustworkX mechanism review | Field/algorithm inventory completed and indexed under WEB-011.12. L2-B is not just a skeleton: it has Node semantic/runtime/provenance/grouping fields, Edge metadata, WCC clustering, filtered views, bounded BFS, iterative spreading activation, and IntentEvent boundary hooks. Gaps are now explicit: graph health metrics, PPR, VF2, stable Edge ids, typed source_meta, persistent subgraphs/clusters, and realtime deltas. | No backend code change until user reviews which gap to implement first. |
| WEB-013.2 | in_progress | Realtime transport upgrade | Move beyond polling/diff for the new monitor: design Web-only SSE/WebSocket over changed-since sequence/events, with browser reconnect behavior and no frontend secrets. Memory and Runtime now both have SSE streams over their existing delta envelopes, and React can consume both through EventSource with polling fallback. | Candidate CORE-009 updated as Web-only. Topbar now distinguishes `SSE · LIVE`, `POLL · FALLBACK`, and `CONNECTING`. Separate receipt stream remains pending. |
| WEB-013.3 | in_progress | Renderer adapter architecture | Added dependency-free `graphModel.ts` as the first component-boundary adapter: it converts L2-B snapshots into engine-neutral nodes/links, visual state, groups, and filters. ReactFlow remains the editable canvas; React-Force-Graph is the planned full-screen monitor engine; Cytoscape.js remains a later dense/compound option. | First implementation supports engine switching at component boundary, not route/API boundary. |
| WEB-013.4 | pending | Full-screen L2-B monitor page shell | Add a separate route/page with graph occupying the main viewport; leave detailed UI layout for user design. Minimal shell should support filters, selected node drawer, status overlay, and import/preload buttons. | Page must not duplicate the current crowded Memory Canvas layout. |
| WEB-013.5 | pending | Graph operations on real topology | Add direct graph interactions for select, expand ego graph, focus path, inspect edge, draft edge, edit/delete Node/Edge, tag/label, subgraph/cluster visibility, and source bucket filters. | All writes remain dry-run/operator gated; L1.5 remains the default write path. |
| WEB-013.6 | pending | Audit after each slice | Check route list, docs index, TODO state, candidate queue, frontend secret scan, UI clutter, and whether WEB-011/WEB-013 responsibilities are drifting. | Update this checklist every implementation round. |
| WEB-013.7 | done | React-Force-Graph reference pack and draft skill | Draft skill/reference pack is present at `codex_workspace/codex_skills/react-force-graph-l2b/SKILL.md`. It records React-Force-Graph 2D/3D API anchors, custom canvas nodes, directional particles, click/hover/drag callbacks, d3 force tuning, React Flow editor boundaries, and Obsidian-style filters/groups/local graph behavior. | Indexed under `codex_workspace/codex_skills/`; durable notes also land in the Memory business file and `_tmp` ledger. |
| WEB-013.8 | in_progress | Graphiti natural-language search to subgraph | Web route/UI flow exists for bounded Graphiti subgraph slices with partition filter, selected-hit canvas preview, export/import receipts, and M12 Strategy / Depth / Focal UUID controls. `iterative_hybrid` performs real follow-up Graphiti searches and returns a `search_plan`; M13 adds exact UUID lookup/enrichment, ECS 8790 proxying, and persisted FalkorDB fallback after restart. M15 adds Graphiti SearchConfig recipe support for combined/edge/node/community search families plus Node label / Edge type filters, with receipts reporting whether low-level `_search` or fallback search ran. M18 deploys that adapter to ECS 8790, so remote app-monitor and 7893 passthrough now both prove `_search`, bundle preservation, lookup, and import overlay against true Graphiti data. WEB-014 owns the first implementation/test pipeline and exports selected Graphiti hits through L1.5 into an L2-B subgraph preview. | Search results do not bypass Graphiti API boundaries, directly write FalkorDB, or secretly mutate L2-B outside L1.5. Still in progress for fuller one-click Graphiti bundle import and final source-board UX. |
| WEB-013.9 | pending | Attention/trigger animation layer | Visualize salience, activation, decay, trigger fires, and spreading activation/PPR as renderer-level animation overlays. These are visual/read-model fields unless the attention-schema review promotes shared DTO fields. | Use `dsg-attention-schema-papers` before changing attention/decay semantics. |

### WEB-014 Detail Checklist

This checklist tracks the Graphiti/L1.5 import test upgrade. It deliberately
stays in the Web lane TODO board and the Web business docs so the task does not
turn into scattered scripts and stale notes.

| Step | Status | Scope | Implementation Notes | Audit Signal |
|:--|:--|:--|:--|:--|
| WEB-014.1 | done | Plan reinforcement and research ledger | Created `_tmp/l15_graphiti_import_test_20260515.md` and recorded the unified conclusion before design: `roleplay` is a mode/profile that can group many source packs, not a singleton bucket; Graphiti imports should preserve temporal episode order and state changes. Web research anchors include DeepSeek official V4 API docs, Graphiti episode/reference-time docs, Graphiti/Zep temporal fact docs, and PRTS Wiki `剧情一览`. | Web README indexes the temporary ledger; durable conclusions are promoted into Memory/Graphiti business files. |
| WEB-014.2 | done | Graphiti DeepSeek V4 provider config | Implemented `GraphitiLLMConfig`, sanitized provider status, DeepSeek default provider/model/base URL, Gemini fallback, and Gemini embedding retention in `src/parrot/shared/config.py` and `src/parrot/memory/graphiti_client.py`. | `GET /api/graphiti/status` reports provider/model/secret booleans only; exact secret scan found no committed key. |
| WEB-014.3 | done | `arknights_test` partition | Added `arknights_test` to Graphiti partition constants, status/search validation path, and Web route tests. UI partition selector still needs the React source-board pass. | Existing partitions still pass route tests; unknown partitions still normalize through the documented allowlist. |
| WEB-014.4 | done | Temporal Arknights fixture/import script | Added `src/scripts/import_arknights_to_graphiti.py`, dry-run by default, with compact original summaries, source URL/source description, chapter order, and reference-time metadata. | `uv run python src/scripts/import_arknights_to_graphiti.py --dry-run --limit 2` passed; real import requires `--apply`. |
| WEB-014.5 | done | Graphiti search to L2-B subgraph routes | Added Web-only `/api/graphiti/subgraph/search`, `/api/graphiti/subgraph/export-draft`, and `/api/graphiti/subgraph/export`; export converts selected hits into `USER_EXPLICIT` observations and preserves `graphiti_*` provenance for L1.5 admission. 2026-05-15 continuation: route limit parsing is now tolerant of bad operator input, and export receipts return `subgraph`, `edge_drafts`, and `edge_write_policy` so Graphiti fact edges are visible without pretending they are already L2-B edges. | Default dry-run; real export requires `operator_mode=true`; no direct FalkorDB or direct L2-B writes; edge drafts require resolved L2-B UUIDs before a later operator-gated edge write. Focused Web tests passed. |
| WEB-014.6 | in_progress | L1.5 source board refactor | React Source Board is now a tabbed source workbench instead of a stacked form pile: Graphiti, Obsidian, Google Calendar, Refs/Photos, and Manual Node guidance occupy one active source area. Graphiti card can search partitions, select individual hits, preview selected subgraph Nodes/Edges on the canvas, generate export-draft receipts, show an inline Export plan with L1.5 observations and Graphiti Edge drafts, run dry-run apply receipts through `/api/graphiti/subgraph/export`, and now shows a sanitized Graphiti provider/status strip from `/api/graphiti/status`; failed searches and cleared selections also clear stale Graphiti canvas previews. 2026-05-17 M12 continuation: Graphiti search controls now include Strategy / Depth / Focal UUID; `iterative_hybrid` performs real follow-up Graphiti searches and returns a `search_plan`; selected expanded hits still import through L1.5 and preserve Graphiti/search context. M13 continuation: `Lookup UUID` and backend enrichment now use true partition-scoped Graphiti UUID CRUD lookup; 7893 proxies real Episode writes and operator export to ECS 8790; persisted FalkorDB fallback keeps search usable after container restart. GOSLO Intent `query_memory` uses the same subgraph path. Existing CORE-015 controls remain: IdentityIndex load/counts, ref verification, Graphiti edge endpoint resolution, preview edge apply, operator materialize-to-L2-B, `Ref Scan Plan`, Dispatch Scan / Scan Results, and `Remote probes`. M14 UI continuation adds Graphiti Ref Write-back controls under Export plan so generated fact/entity/episode Ref drafts can be previewed/applied to IdentityRefIndex with editable Ref ID/kind/locator and an explicit audit Episode checkbox. M15 UI continuation adds a separate Graphiti SearchConfig Recipe control plus Node labels / Edge types filter inputs, so Web can test Graphiti combined/edge/node/community retrieval modes while still using local Strategy for multi-hop expansion. M16 backend continuation adds `graphiti_bundle` to search/export/import-plan receipts. M17 UI continuation renders that bundle directly with section counts, search/lookup summary, projection/import overlay, and sample rows. M18 deployed the same SearchConfig/bundle adapter to ECS app-monitor so 8790 and 7893 passthrough both prove remote `_search` mode, bundle sections, UUID lookup, and import overlay against real Graphiti data. | Still needs less crowded final layout and fuller one-click selected-subgraph import UX before marking done. Latest M18 validation: app-monitor tests `9 passed`, Web route tests `83 passed`, ECS 8790 active after restart, and remote/7893 canaries returned `_search`, `fallback=false`, true ECS UUID `0ea2009c-402d-4332-81b4-31fa57e67688`, bundle counts `facts=3/entities=4/episodes=3/communities=0`, UUID lookup `10/10`, and overlay `isolated_compartment`. |
| WEB-014.7 | done | Obsidian vault scan/import preview | Added Web-only `GET /api/l15/obsidian-vault/scan`, `POST /api/l15/obsidian-vault/import-draft`, `POST /api/l15/obsidian-vault/import-plan`, and `POST /api/l15/obsidian-vault/import`. React Source Board can scan the vault, select ready notes, load a note into the single-note draft form, generate a batch import preview, read a compact Import plan before apply, and explicitly run the operator-gated import to L1.5 after review. `daily`/`roleplay` stay UUID-free setting profiles; `ref` remains a binding profile. Bugfixes: scanned notes no longer auto-select hidden rows, import-draft reports invalid/missing selected paths, selected profile mismatches, and selected paths beyond `limit` as explicit receipt errors; invalid `limit` no longer raises route exceptions; UTF-8 BOM Markdown frontmatter is accepted; empty/invalid selected-note import plans now skip CORE-013 policy instead of showing fake destination placement; scan/selection/vault-path changes clear stale import receipts. | Real import is implemented, secondary/operator-gated, and Web-only. It writes through `UserTagFilter -> L15Pool.admit`; runtime sync remains `ObsidianIngestTrigger -> TriggerOutcome.commit_observations`. |
| WEB-014.8 | in_progress | Google Calendar data preview/import preview | Added Web-only `POST /api/google/calendar/fetch`, `POST /api/google/calendar/api-fetch`, `POST /api/google/calendar/nanobot-fetch`, `POST /api/google/calendar/preview`, `POST /api/google/calendar/import-draft`, `POST /api/google/calendar/import-plan`, and `POST /api/google/calendar/import`. React Source Board now has a Google/Nanobot JSON payload box plus buttons for Scheduler fetch preview/operator dispatch, local OAuth2 + official Google Calendar API fetch, ECS Nanobot + Google Workspace MCP fetch, event normalization preview, unified import plan, import-apply preview, and operator import to L1.5. The preview/import receipts return Web-only `mapping_rows` and the import-plan joins those rows with CORE-013 destination/overlay policy so the UI can show raw event -> L1.5 `google_calendar` bucket -> L2-B EVENT action/policy without inventing a shared DTO. | Preserves calendar id, event id, start/end/timezone/location/html link/etag/status/iCalUID/objects; 2026-05-15 bugfix preserves `observed_at` / `time_span` when a Web receipt is rehydrated for real L1.5 import. Cancelled/deleted events now map to `mark_historical_tombstone`: keep the Google identity, keep the EVENT Node for history/reconciliation, and set GHOST/low-attention state rather than evicting by default. 2026-05-16 bugfix: empty Calendar import plans now return `policy_skipped_reason=no_calendar_observations` and no destination policy; UI follow-up shows `policy_skipped_reason`, receipt errors, local fetch/parse exceptions, or `no_calendar_events` directly in the Calendar card and clears stale previews on failure. 2026-05-17 true-connection pass: `api-fetch` uses OAuth2 + official Google Calendar `events.list`; `nanobot-fetch` calls ECS Nanobot over the 8900 tunnel and lets Nanobot use Google Workspace MCP. Real import requires `operator_mode=true` and `dry_run=false`, then writes through `L15Pool.admit(GOOGLE_CALENDAR)`. Browser never holds Google OAuth; watch/syncToken stays phase 2. |
| WEB-014.9 | in_progress | Tests and audit | Backend route tests, provider config tests, secret non-leak tests, partition tests, Graphiti export/import-plan/operator-export/raw-envelope/UUID lookup/fallback search/SearchConfig recipe/bundle tests, Obsidian vault scan/import-draft/import/import-plan/operator-import tests, Calendar preview/fetch/api-fetch/nanobot-fetch/import/import-plan/result-history/tombstone tests, Memory changed-since route tests, RefBinding draft tests, CORE-015 IdentityRefIndex route/verify/merge-conflict/resolve-graphiti/apply-graphiti-edge/ref-scan-plan/ref-scan-dispatch/ref-scan-results/graphiti-ref-writeback tests, fallback NanobotConsumer `ref_scan` tests, photo asset safe-read tests, script dry-run, Obsidian true-connection tests, Calendar true-connection tests, TriggerOutcome tests, frontend typecheck/build, and browser smoke are passing for this slice. 2026-05-17 M13 validation: `py_compile` passed for Graphiti/Web/App monitor files; `tests/test_web_console/test_web_console_server.py -q` -> `79 passed`; `npm run typecheck` and `npm run build` passed earlier in the slice; ECS 8790 was deployed/restarted; 7893 wrote a non-dry-run Episode through ECS, survived a real FalkorDB restart, returned persisted hits with lookup 16/16, and remote operator export succeeded with `dry_run=false`. 2026-05-17 M14 validation: full Web route tests report `82 passed`; frontend typecheck/build passed after UI surfacing and the empty external-ref regression; restarted 7893 returned true ECS `arknights_test` search, M14 draft receipts, and an empty-locator write-back smoke with no ExternalRefRecord / RefMoveEvent draft. 2026-05-17 M15 validation: full Web route tests report `83 passed`; focused regression proves `combined_rrf` + filters are passed into Graphiti `_search(SearchConfig)` and recorded in `search_plan`; frontend typecheck/build passed; restarted 7893 static/route smoke passed. 2026-05-17 M16 validation: `py_compile` passed and full Web route tests report `83 passed`; regressions assert `graphiti_bundle` raw sections, lookup preservation, SearchConfig metadata, and import overlay. 2026-05-17 M17 validation: frontend typecheck/build passed, built assets include the Graphiti bundle UI, Web route tests remain `83 passed`, 7893 restarted with ECS proxy and live search/import-plan canary passed. 2026-05-17 M18 validation: app-monitor focused tests report `9 passed`; Web route tests remain `83 passed`; ECS 8790 deployed/restarted and remote plus 7893 passthrough canaries returned `_search`, `fallback=false`, bundle counts `3/4/3/0`, lookup `10/10`, and import overlay `isolated_compartment`. | Update TODO/business docs after every slice; shared gaps go only to core candidate queue. |
| WEB-014.10 | pending | Obsidian sample source-pack preview/create | Prepare a user-reviewed file preview for `D:/GOSLOParrot/GOSLObsidian` using `GOSLO/Settings/Daily`, `GOSLO/Settings/Roleplay`, `GOSLO/Refs`, and `Worlds/Arknights_test`. Do not write sample files before showing the preview list. | Files should use Obsidian Properties/frontmatter compatible with the parser and preserve source path/labels/tags for L1.5. |
| WEB-014.11 | in_progress | Photo/ref source cards | Added the `Refs/Photos` Source Board tab. It summarizes session RefBindings, session PHOTO RefBindings, resolved L2-B targets, IntentWorkspace PHOTO refs, and L2-B Photo Nodes; it can select existing rows and generate `refs.binding.draft` receipts with target kind/id and `core_candidate=CORE-006`. 2026-05-15 bugfix: unresolved-target drafts are now represented as unresolve/clear previews rather than successful resolution previews. 2026-05-15 continuation: selected Node details now surface related RefBinding/IntentWorkspace refs plus Photo asset/episode hints as read-only badges, and cache-backed photo assets can render through read-only `GET /api/photos/asset/{day}/{photo_id}` thumbnails. This is intentionally draft-only until the shared RefBinding API is reviewed. | Latest validation: Web route tests `37 passed`, `npm run typecheck`, `npm run build`, browser smoke, and secret scan passed. Still needs manual bind/unbind/apply policy. |
| WEB-014.12 | done | Source Board placement cleanup | Source Board remains the source import workbench, but it is no longer mixed with selected Node/Edge details. It now opens from the Memory toolbar's L1.5 Pool dock alongside pool health and bucket cards. | Keeps Graphiti/Obsidian/Calendar import previews separate from direct graph selection/editing. |
| WEB-014.13 | done | Edge metadata and surgery routes | Added Web-only `POST /api/l2b/edge/update` and `POST /api/l2b/edge/delete`; Edge snapshots now expose `created_at` and `meta` in addition to endpoints/kind/strength/source/cross-compartment. | Default dry-run; real update/delete requires operator mode; no RustWorkX edge index is exposed as a shared DTO. |
| WEB-014.14 | in_progress | Google Calendar sync architecture review | Documented the current and target collaboration path: Web BFF -> Scheduler `calendar_fetch` -> Nanobot Google Workspace MCP -> `calendar_result` -> `CalendarTrigger` -> `TriggerOutcome.commit_observations` -> L1.5 `google_calendar` bucket -> L2-B EVENT nodes. Scheduler now also writes a bounded `STREAM_TRIGGER_RESULTS` observability ledger when it fans Nanobot results to `CH_TRIGGER_RESULTS`, and Web exposes `GET /api/google/calendar/results` plus a Source Board `结果记录` button. Official Google docs point to service-side push/watch plus incremental syncToken for realtime, not browser OAuth. | Current implementation is manual/operator dispatch plus periodic trigger polling plus recent result history. Memory changed-since V1 can now make downstream L1.5/L2-B changes visible without full repaint, but true Calendar sync still needs server-side `watch`/syncToken durability before SSE/WebSocket. |
| WEB-014.15 | done | Calendar deletion/tombstone policy | Implemented Web-first backend policy for Google cancelled/deleted events: `CalendarTrigger` still emits a `GOOGLE_CALENDAR` Observation, but marks it as `calendar_lifecycle=cancelled/deleted`, `is_tombstone=true`, `tombstone_policy=historical_event`, `confirmation=GHOST`; `IngestRunner` merges by provider identity and lowers the existing L2-B EVENT Node to peripheral/low-attention instead of deleting it. | This is the safest initial sync policy because Google incremental sync may only return an id/status tombstone. It keeps the reconciliation anchor and history visible. Shared SSOT promotion can now reference the historical tombstone policy, but Web operator controls remain out of App DTOs. |
| WEB-014.16 | done | Graphiti/L2-B Ref identity research note + CORE-015 skeleton | Added `backend_interface_map/web_console/graphiti_l2b_ref_identity_design_20260517.md` after rereading the Parrot Graphiti/RustWorkX/L2-B skills, auditing current `RefTable`, `L15Pool`, `L2BGraph`, ingest runner, and Web Graphiti routes, and checking official Graphiti/rustworkx docs. Implemented `src/parrot/dsg/identity_ref_index.py` plus Web routes `GET /api/memory/identity-ref-index`, `POST /api/memory/identity-ref-index/draft`, and `POST /api/memory/identity-ref-index/apply` as a file-backed CORE-015 prototype. | Route tests cover missing-signal validation, draft no-persist behavior, dry-run apply no-persist behavior, operator apply JSON persistence, and no direct L2-B/Graphiti/App DTO writes. Next: Ref health verification and GraphitiResolver edge materialization. |
| WEB-014.17 | in_progress | CORE-015 staged execution workbench | Added `_tmp/graphiti_l2b_ref_identity_workplan_20260517.md` with a questionnaire index, working-file index, TODO-before gate, TODO-during task list, TODO-after gate, and execution ledger. Completed M1-M20R as recorded in that workbench. M13 adds true partition-scoped Graphiti UUID lookup, raw fact/source/target enrichment, 7893 -> ECS Episode/export proxying, app-monitor export routes, FalkorDB persistent data-path/AOF/noeviction config, and a read-only partition-graph fallback when hybrid search is cold after restart. M14 adds semantic Graphiti-ref write-back routes and UI: `POST /api/memory/identity-ref-index/graphiti-ref/draft` and `/graphiti-ref/apply` preserve raw Graphiti record pointers, bind reviewed ExternalRefRecords only when a real locator/URI/hash signal exists, draft RefMoveEvents for locator changes, and return a Graphiti audit Episode draft while default apply writes only IdentityRefIndex JSON. M15 adds Graphiti SearchConfig recipe/filter support for true configurable retrieval before import, while keeping Graphiti raw data authoritative and keeping local expansion Strategy separate from Graphiti Recipe. M16 adds `graphiti_bundle` as the preserved Graphiti subgraph package for search/export/import-plan receipts, with raw sections and L2-B import overlay in one object. M17 exposes that bundle in the Source Board and live-smokes it through 7893 against ECS Graphiti. M18 deploys the same SearchConfig/bundle/import-plan adapter to ECS 8790 and proves remote `_search` mode directly. M19 adds preview-only Graphiti bundle -> L2-B/RustWorkX projection over the preserved bundle, embeds it in import-plan, and surfaces the projection in the Source Board without persisting topology or rustworkx indices. M20R audits the shape against the original requirement and records why Node/Edge categories are view/algorithm filters, while canonical UUID/ref truth remains in IdentityRefIndex/ExternalRefRecord rather than preview IDs or Graphiti Episodes alone. | This is the control checklist for step-by-step execution. Durable decisions must be promoted back into `graphiti_l2b_ref_identity_design_20260517.md`, Memory/Graphiti business files, TODO board, and CORE-015 candidate notes after each M-task. Next slice: M20 operator-reviewed apply/materialization design for selected projection nodes/refs/edges, with rollback/audit and section-level UI selection. M20R validation: durable docs now record official Graphiti/RustWorkX version/doc anchors, research refs, Node/Edge audit, UUID/ref boundary, and remaining apply/database gaps. M19 validation remains: backend `py_compile`, Web route tests `83 passed`, frontend typecheck/build passed; ECS 8790 and 7893 passthrough both returned `_search`, bundle counts `3/4/3/0`, import-plan success, projection counts `l2b_nodes=3`, `l2b_edges=1`, `episode_links=2`, RustWorkX `nodes=3/edges=3`, and `rwx_idx_policy=ephemeral_do_not_persist`. |

WEB-014.17 pre-implementation rule (2026-05-17): before implementing the next
Graphiti/Ref/UUID slice or adding more TODO items under CORE-015, first read
`web_console/_tmp/graphiti_l2b_ref_identity_workplan_20260517.md` section
`Pre-Implementation Readback`. It summarizes the M14 research/review result:
Graphiti owns temporal/provenance data, RefIndex owns mutable locators,
IdentityBinding owns UUID equivalence, rustworkx indices are volatile handles,
EdgeKind is a view/runtime class rather than Graphiti ontology, and nanobot +
MCP + git stay operator-gated scan/repair control plane.

WEB-014.17 M20R review gate (2026-05-17): the completed work is aligned with
the original requirement up to preview/import proof, because Graphiti search is
real, `_search(SearchConfig)` and recipe/filter controls are visible, bundle
sections preserve Graphiti raw data, and the L2-B/RustWorkX layer is a
preview-only projection. It is not yet complete as durable import/apply:
preview UUIDs must be resolved through IdentityBinding, mutable refs must stay
in ExternalRefRecord/RefIndex rather than only Episodes, and the file-backed
RefIndex should later move to a transactional store before high-volume sync.

WEB-014 current audit note (2026-05-16): Graphiti Source Board selection is now
treated as an invalidation boundary. Changing selected hits, selecting all,
clearing selection, or changing the Graphiti partition/query/limit clears stale
export/import-plan state and canvas preview. Graphiti preview/export/import
buttons remain clickable with no selection so the card can show
`no_hits_selected` inline instead of silently disabling the operator path.
Validation for this continuation: Web route tests `56 passed`, frontend
`typecheck`/`build` passed, exact secret scan found no matches, and browser
smoke confirmed the inline no-selection state with zero console errors.

WEB-014 operator-import note (2026-05-16): Graphiti and Obsidian now have a
visible but secondary `Import to L1.5` control in the Source Board. It uses the
same reviewed backend routes as preview, with `dry_run=false` and
`operator_mode=true`; tests assert the route reaches `L15Pool.admit`. Graphiti
fact Edges are still `edge_drafts` until L1.5-admitted Nodes resolve to stable
L2-B UUIDs and a separate Edge apply route is used.

WEB-014 Calendar dispatch guard note (2026-05-16): the Google Calendar
operator `Dispatch Fetch` button is also treated as a real side-effect path,
because it can enqueue a Scheduler/Nanobot `calendar_fetch` task. The React
Source Board now disables that button while dispatch is in flight and returns a
local `operator_fetch_in_flight` receipt if a second click arrives. This is
separate from Calendar import and does not add browser-side Google OAuth.
Validation: React `typecheck`/`build`, Web route tests (`58 passed`), exact
secret scan, `git diff --check`, and browser smoke on `L1.5 池 -> Google 日程`
passed; the smoke did not click the real dispatch button.

WEB-014 source import-plan matrix note (2026-05-16): added a shared Web route
regression that exercises Graphiti, Obsidian, and Google Calendar
`import-plan` routes with `dry_run=false` and `operator_mode=true`. All three
must still return draft-only receipts, preserve the requested execution under
`requested_execution`, expose `operator_required_for_execute`, use the reviewed
apply route/preconditions, emit CORE-008/CORE-013 candidate references, and not
leak raw secrets or direct FalkorDB writes. Validation: full Web route tests now
report `59 passed`; React `typecheck` and `build`, exact secret scan,
`git diff --check`, and in-app browser smoke passed.

WEB-014 landing-map UI note (2026-05-16): Graphiti, Obsidian, and Google
Calendar source cards now share a compact import Landing map showing
`Source -> L1.5 observations -> L2-B target`, destination policy, Edge draft
count, apply route, and operator gate. This is a Web readability layer only:
it does not add a shared DTO, does not change the L1.5 write path, and keeps
operator apply separate from preview/import-plan. Validation: React
`typecheck`/`build`, full Web route tests (`59 passed`), exact secret scan,
and browser smoke on `L1.5 Pool -> Graphiti -> Preview policy` passed with
zero console errors.

WEB-014 global execution-mode note (2026-05-17): Web Console is now treated as
a test bench. The Settings popover owns the single `Mode`; default is real
operator execution. Graphiti/Obsidian/Google Source Board execute buttons follow
that Mode instead of carrying local dry-run toggles, while Search, Scan,
Preview policy, and Import plan remain explicit preview/read operations.

### WEB-015 Detail Checklist

This checklist tracks the Time-Aligned Evidence / `identify_object` upgrade. It
stays in this TODO board because it crosses Web Console, Brain Intent tools,
ECP payload/meta conventions, SVA/frame-cache producers, and CORE-012 review.

| Step | Status | Scope | Implementation Notes | Audit Signal |
|:--|:--|:--|:--|:--|
| WEB-015.1 | done | Timebase/evidence typed backend layer | Added `parrot.brain.vision.evidence` with `ClockDomain`, `EvidenceKind`, `EvidenceStatus`, `TimebaseStamp`, `SampleRegion`, `TimeAlignedSampleRef`, and bounded `TemporalEvidenceLedger`. Explicit `payload.timebase` / `EcpCommand.meta.timebase` wins; legacy `ts_ms`/`timestamp`/`observed_at` is accepted; fallback to `EcpEvent.created_at` or `EcpCommand.issued_at` is marked `estimated=true` because envelope time is not sample time. | No Unity/App top-level DTO changed; schemas remain Web/backend-first until CORE-012 review. |
| WEB-015.2 | done | Evidence producers V1 | `observer.snapshot`, `observer.photo`, and `FocusBboxThreshold` now mirror snapshot/photo/attention threshold rows into the ledger. Photo assets use storage/HTTP refs; attention rows preserve subject kind/id, ref id, and weight metadata. 2026-05-15 audit fix: HTTP photo upload accepts optional `X-Parrot-Timebase` JSON or discrete `X-Parrot-*` sample-time headers and forwards them into `photo.asset_uploaded.payload.timebase`, so the ledger can keep true capture/sample time instead of only estimated envelope time. | Evidence writes are best-effort and do not change existing ECP/BB public contracts. |
| WEB-015.3 | done | `identify_object` no-snapshot-RPC upgrade | `identify_object` now calls `resolve_identify_evidence()` with optional `evidence_id`, `bbox_ref_id`, `focus_ref_id`, and `target_time_ms`. If no stored image/frame exists, it records a pending evidence request and continues L0/L1 text/Graphiti matching honestly. 2026-05-15 review fix: a missing BBox/Focus ref no longer falls through to the room's newest unrelated frame; it records a pending focus-linked request unless an explicit `target_time_ms` fallback is supplied. Sighting events include secret-free `evidence_id`/asset pointers when available. | Focused tests cover the new evidence stage and missing-ref guard; old `capture_current_frame` is not used by the tool path. |
| WEB-015.4 | done | Web evidence debug routes/UI | Added `GET /api/vision/evidence/status`, `GET /api/vision/evidence/timeline`, `POST /api/vision/evidence/request`, `GET /api/vision/evidence/{evidence_id}`, and `POST /api/app/test/visual-attention`. React Runtime page includes a compact Time/Evidence panel for status, timeline, manual request, and BBox/Focus test events. Vite output now writes stable tracked `assets/app.js` / `assets/styles.css`; the Web BFF serves console assets with `Cache-Control: no-store`, avoiding both stale bundles and untracked hash-file deployments. 2026-05-15 cleanup removed the older tracked hash bundles from `web/console_dist/assets/`; the directory should only carry the stable runtime entrypoints unless a future deployment plan changes. | Web route test covers request/timeline/detail/static asset path; frontend typecheck/build pass; browser smoke confirmed the panel and no console errors. |
| WEB-015.5 | done | Real frame-cache producer | Added storage-backed `parrot.brain.vision.frame_cache` with `record_livekit_frame_bytes()`, bounded in-process metadata, disk-backed image assets, `VIDEO_FRAME` ledger rows, `livekit_track` timebase, room/track/participant/source ids, sequence, media timestamp, and storage path. Web/operator debug route `POST /api/vision/evidence/frame-cache/upload` can validate or cache an encoded test frame and status now reports `frame_cache`. Do not treat Gemini `video_input=True` as auditable frame access. | LiveKit/SVA skills used; no old inline snapshot RPC. Focused tests cover frame cache -> ledger -> nearest evidence lookup and Web route secret non-leak. |
| WEB-015.5b | in_progress | Automatic LiveKit/SVA sampler | Added `parrot.brain.vision.livekit_sampler`: it registers room handlers, scans existing remote video publications, subscribes video tracks, consumes `VideoStream(track)` at a bounded low FPS, encodes LiveKit `VideoFrame` to JPEG with PIL, and calls `record_livekit_frame_bytes()` with room id, track sid/name, participant id, sequence, wall/monotonic time, and media timestamp. Brain `brain_entrypoint()` starts it per room and stops it on disconnect/restart. The sampler now writes a secret-free status file consumed by `/api/vision/evidence/status`; both sampler and frame-cache status expose `fresh_window_ms`, `latest_frame_age_ms`, `latest_frame_fresh`, and per-track latest summaries so Web can show fresh/stale evidence state. 2026-05-15 follow-up: sampler accepts both `screenshare` and `SOURCE_SCREEN_SHARE` style source names, includes `publication_source` in latest-frame summaries, and React Runtime routes its sampling check through read-only BFF verdict `GET /api/vision/evidence/screen-share-smoke` instead of duplicating source heuristics in the browser. `src/scripts/smoke_livekit_frame_sampler.py` can join a real room to verify live video ingestion. | Unit tests cover RGB frame encoding, fake-room automatic camera and screen-share track sampling, persisted sampler status, freshness fields, and the Web read-only screen-share smoke verdict. Still pending live Unity/Web screen-share/LiveKit smoke for track selection, reconnect behavior, and storage pruning under real video load. |
| WEB-015.6 | done | Stored image crop + VLM describe V1 | Added `parrot.brain.vision.evidence_image` with `prepare_evidence_image()` and `describe_evidence_sample()`: it reads local `asset_path`, applies normalized or pixel `SampleRegion` crop, encodes JPEG base64, and feeds existing Gemini VLM `describe_image()`. `identify_object` now uses the returned compact visual hint to enrich L0/L1 search text when stored evidence is available; evidence with only `asset_uri` skips VLM instead of fetching hidden network bytes. | Tests cover normalized crop preparation and `identify_object` feeding VLM detail into L0/L1. No DataChannel inline images and no old snapshot RPC. Reference-image comparison remains a later enhancement. |
| WEB-015.7 | done | GOSLO notification/session policy V1 | Added `parrot.brain.vision.evidence_awareness`: ready evidence can be staged into IntentWorkspace as `role=visual_evidence_hint`, with a compact payload containing evidence id, kind, timebase, asset refs, and description. It writes `transient/evidence_awareness_notice` and uses `session_policy.should_generate_reply()` to decide `notify_goslo` / `allow_react`; `allow_interrupt` stays false. Web adds `POST /api/vision/evidence/stage-hint` and a Runtime Time/Evidence button. | No direct `generate_reply` call in utility code. New BB key is backend/shared candidate and documented under CORE-012 review. |
| WEB-015.7b | done | Session-owned evidence notification delivery | Wired `transient/evidence_awareness_notice` into `ContextInjector`. Allowed notices become rate-limited C3 chat-context hints with evidence/ref ids and an explicit "do not interrupt" instruction. Silent notices stay layer-1 only. V1 deliberately does not call C4 `generate_reply`; safe-turn speech remains a future policy review. | Unit tests cover C3 routing and silent suppression. Live LineA/LineB smoke and conversation-state review remain before any C4 policy. |
| WEB-015.7d | done | Photo Awareness intake delivery | Wired `transient/photo_awareness_notice` into `ContextInjector` as the App menu photo-notification bridge. `UNAWARE_RECORDED` remains layer-1; `AWARE_SILENT` and `AWARE_REACT` become C3 chat-context hints only after a staged preview ref exists or preview is explicitly missing. This documents that IntentWorkspace staging alone is discoverable context, while C3 is the first strong GOSLO notification layer. | Unit tests cover staged preview C3 routing, pending-ref suppression, and unaware suppression. No C4 speech or interrupt is introduced. |
| WEB-015.7c | pending | Evidence safe-turn C4 policy | Decide whether some evidence notices should request a safe-turn spoken reply after current speech/user audio settles. This must reuse session policy, active speech guards, and rate limits, and must never interrupt by default. | Needs live LineA/LineB smoke and user review. |
| WEB-015.8 | in_progress | Live no-camera Web screen-share evidence smoke | Added backend read-only verifier `GET /api/vision/evidence/screen-share-smoke`: it checks sampler status, frame-cache freshness, nearest stored evidence, and screen-share source hints (`web-console-screen`, `SOURCE_SCREEN_SHARE`, `screen_share`/`screenshare`) without writing pending evidence rows. 2026-05-15 bugfix: success now requires the same candidate row to be both fresh and screen-share-looking, so a stale screen-share status plus a fresh camera frame cannot pass; `next_steps` now also treats that case as stale instead of suggesting Memory Draft. 2026-05-15 research/implementation pass: React Runtime now prefers LiveKit's `setScreenShareEnabled(true)` and falls back to `getDisplayMedia + publishTrack` only when needed; Agents `video_input=True` remains non-auditable model input, so frame-cache/evidence-ledger stays canonical. Follow-up bugfix: Web now treats missing `LocalTrackPublication` from `setScreenShareEnabled(true)` as failed publish and suppresses the misleading stop event when the user cancels or publish fails; regression covers LiveKit JS `screen_share` source spelling as well as Python/proto `SOURCE_SCREEN_SHARE`. React Runtime now renders the `检查采样` verdict inline as a small diagnostic card with ready/not-ready state, fresh-evidence/screen-source/fresh-screen chips, sampler/frame-cache counts, age, and server `next_steps`, so failed live smoke is understandable without opening the receipt rail. With Brain/LiveKit Agent running, the remaining user-assisted step is Runtime Flow `LiveKit / Brain Connect` -> `Screen Share` -> `Check Samples` to prove a real browser screen track becomes fresh `VIDEO_FRAME` evidence. | Validation: Web console route tests `46 passed`; Time/Evidence/threshold/identify regression `34 passed`; latest focused Web/Time-Evidence regression `81 passed`; frontend typecheck/build passed; browser smoke opened Runtime, clicked `检查采样`, rendered the diagnostic card, and saw zero dev-console errors. Time/Evidence remains Runtime Flow only; L2-B consumes resulting evidence refs later. |
| WEB-015.9 | in_progress | Evidence-to-Memory promotion path | First safe Web/backend slice implemented: `POST /api/vision/evidence/memory-draft` turns a stored `TimeAlignedSampleRef` into a draft L1.5 `Observation(source=USER_EXPLICIT)`, optional CORE-006 RefBinding draft, and operator-readable mapping from Temporal Evidence Ledger -> IntentWorkspace/Ref -> L1.5 -> L2-B. React Runtime Time/Evidence now has a `Memory Draft` button for the latest evidence row. No apply branch yet: real L1.5/Ref/L2-B writes wait for CORE-012/CORE-006 review and live screen-share smoke. 2026-05-15 audit fix: memory-draft receipts now expose top-level `audit`, `core_candidate`, and `core_candidates`, explicitly marking read-only/no L1.5 mutation/no L2-B mutation/no RefBinding mutation so the receipt rail and later SSOT review cannot confuse preview with execute. | Keep sampling controls in Runtime Flow; Memory Graph gets evidence-linked Node/Ref management only. Validation: focused Web/DSG/Time-Evidence regression `95 passed`; frontend typecheck/build and browser smoke passed; latest focused WEB-015 regression `81 passed` after the audit-field fix. |
| WEB-015.10 | in_progress | CORE-012 promotion review | Web/backend implemented behavior is now consolidated in `.cursor/memory/architecture/Interface/time_aligned_evidence_interface_20260515.md` with writer/source metadata and explicit blockers. Shared Unity/App top-level DTO promotion is still not done: real screen-share/Unity smoke, App lane field review, CORE-006 RefBinding apply rules, ASR/CV semantics, and C4 safe-turn policy remain required. | Candidate queue updated; Interface SSOT added as Web/backend active contract, not a shared App DTO promotion. |
| WEB-015.11 | done | Chain audit: timebase / sampling / photo / snapshot / attention | Audited and tested the current chain end-to-end at module/route level: `EcpEvent.payload.timebase` and `EcpCommand.meta.timebase` parse correctly; frame-cache upload and fake LiveKit sampler write `VIDEO_FRAME`; snapshot metadata and photo asset upload write `IMAGE_ASSET`; BBox/Focus threshold writes `BBOX_FOCUS`, Blackboard hint, and `attention.threshold.crossed`; Web routes expose status/timeline/request/stage-hint; `identify_object` resolves stored evidence and never calls the old snapshot RPC. Latest focused validation after attention-bridge integration: `81 passed` for Time/Evidence, threshold, identify_object, Web console routes, and ContextInjector. | Live screen-share smoke remains WEB-015.8; C4 spoken interruption remains WEB-015.7c; Evidence-to-Memory/L2-B promotion remains WEB-015.9. |
| WEB-015.12 | done | Attention threshold auto evidence bridge | `FocusBboxThreshold` now calls `parrot.brain.vision.evidence_awareness.bridge_attention_threshold_to_goslo()` after writing the ledger row, Blackboard hint, and `attention.threshold.crossed`. The bridge parses the BBox/Focus ref and producer timebase, asks `resolve_identify_evidence()` for the nearest stored frame/photo, stages a `visual_evidence_hint` in IntentWorkspace when evidence is ready, and records a pending request when no stored sample exists. It is deliberately conservative: no frame capture, no direct `generate_reply`, no L2-B mutation, and `allow_interrupt=false`; delivery still goes through ContextInjector C3 policy. | Tests cover direct bridge staging and the real threshold subscriber path. Validation: related pytest set `81 passed`; live screen-share smoke remains separate WEB-015.8. |
| WEB-015.13 | done | Trigger/Awareness taxonomy and default C3 routing | Added the Trigger/Awareness family taxonomy SSOT with delivery levels L0/L1/L2/C3/C4/I0, body-feel fields, current trigger inventory, Web clustering guidance, and App animation hooks. Legacy `notify_gemini` now means a C3 status notice by default; C4 speech/interrupt remains an explicit future policy. | Focused trigger/calendar/message/evidence-awareness pytest set: `23 passed`; no Unity/App DTO changed. |
| WEB-015.14 | done | CORE-014 BBox/MAG visual-tool lifecycle backend/App V1 | Added `parrot.brain.vision.tool_lifecycle` with typed `VisualToolLifecyclePacket`, phase salience policy, BBox/Focus ref binding, evidence-ledger writes, IntentWorkspace staging, C3/no-interrupt delivery, C4 downgrade audit, receipt writing, and `store_visual_tool_asset()` for rendered crop/preview uploads. Added ECP `visual_tool.lifecycle` plus `brain.observer.visual_tool`, App routes `POST /api/app/visual-tool/event` and `POST /api/app/visual-tool/asset/{asset_id}`, Web debug route `POST /api/vision/evidence/tool-lifecycle`, BB schema key `transient/visual_tool_lifecycle_receipt`, and App tool-cabinet `backend_ready` states for MAG/BBox. BBox `confirm` defaults to C3; MAG `confirm` defaults to `intent_only`; `explicit_send` can C3; `cancel`/`release` unbind without notice. | Validation: focused backend/App/Web route set `7 passed`, then wider Time/Evidence/threshold regression `51 passed`. No image bytes in ECP/RPC; no Unity top-level DTO promotion; no DSG L3 attention conflation. Remaining App work is controller UI and phone smoke. |

### WEB-016 Detail Checklist

This checklist tracks CORE-013. It must stay in this board and the Memory
business flow file so graph-policy work does not fragment into scattered design
notes.

| Step | Status | Scope | Implementation Notes | Audit Signal |
|:--|:--|:--|:--|:--|
| WEB-016.1 | done | Mandatory research/architecture gate | Source readback completed from `parrot-cursor-skill-bridge`, `dsg-rustworkx-master`, `dsg-l2b-node-organization-options`, `dsg-l1-5-l2a-conceptgraph-distilled`, `dsg-attention-schema-papers`, and current `src/parrot/dsg/l2b*`, `src/parrot/dsg/l1_5/*`, `src/parrot/web_console/memory_live_state.py`, `web/console_app/src/graphModel.ts`. Key readback: L2-B already has stable UUID/index mapping, endpoint/kind/source Edge surgery, bucket/event/scene/location compartments, read-only WCC clustering, bounded BFS/spreading activation, and Web changed-since polling. Missing surface is typed overlay/rewrite policy and receipts, not new semantic `NodeKind`. | Memory business file now records online/offline algorithm tiers, overlay vocabulary, first implementation order, and when to use LLM instead of graph mutation. No code was started before this gate. |
| WEB-016.2 | in_progress | Import destination policy | Backend draft route implemented at `POST /api/l2b/graph-policy/import-draft` with `workspace_only`, `index_pointer`, `isolated_compartment`, `promote_to_main_graph`, and `connect_by_rule` policies. React Memory Canvas subgraph tool and Graphiti Source Board now expose destination controls and preview receipts. There is still no apply route. | Every import receipt says where it went, why, what edges were drafted, what was withheld, and which policy made the decision. Route tests, frontend typecheck/build, and browser smoke cover the UI entry points. |
| WEB-016.3 | in_progress | Persistent subgraph/cluster overlay model | Backend draft route implemented at `POST /api/l2b/subgraphs/draft` with `GraphOverlay` fields for id, label, kind, membership, refs, source, collapse state, and meta. React Memory Canvas can draft a visual overlay from the selected Node/Edge context, but persistence/fold state storage is still pending. | Subgraph fold/collapse is visible and reversible; App/Web do not persist renderer-only layout as semantic truth. |
| WEB-016.4 | in_progress | Graph transform operations | Backend draft route implemented at `POST /api/l2b/transforms/draft` for wrap, aggregate/compare, draft cross-links, promote, split, tombstone, and send-context-to-LLM preview. React Memory Canvas now has transform-kind selection and dry-run preview receipts. | Operations are bounded, dry-run first, operator-gated for real writes, and never run whole-graph rewrites silently. |
| WEB-016.5 | pending | Incremental update engine | Create a candidate event/receipt model for node/edge/subgraph deltas so imports and evidence can update only affected ego-subgraphs. Reuse Memory changed-since vocabulary where possible. | CORE-009 and CORE-013 stay aligned; no second incompatible realtime shape. |
| WEB-016.6 | in_progress | Useful graph-analysis presets | First read-only health route implemented at `GET /api/l2b/analysis/health`: node/edge counts, orphan Nodes, WCC count/largest WCC, and kind/bucket/source distributions. React state tool now renders a compact graph-health card. 2026-05-15 bugfix: the UI wraps this read-only payload in a local `l2b.analysis.health` record instead of pushing the non-receipt response directly. Degree/centrality/PPR/VF2 remain pending and must stay operator-triggered or offline with call limits. | Each preset documents when it is more useful than simply asking an LLM over selected context. |
| WEB-016.7 | in_progress | UI/receipt design | First UI pass exists: Memory toolbar exposes graph policy, overlay, transform, and health previews; Graphiti Source Board can preview destination policy before export. 2026-05-15 bugfix: Graphiti policy preview now preserves Graphiti source/target/fact provenance in draft Edge `meta`. 2026-05-16 bugfix: the React Source Board now matches selected Graphiti hits against both plain hit keys and `graphiti:<uuid>` preview ids, so proposed Graphiti source/target/fact Edges are not dropped from policy previews. 2026-05-16 continuation replaces the split Graphiti policy preview with `graphiti.subgraph.import_plan`, a single receipt that shows selected hits, L1.5 observations, Edge drafts, import destination, overlay/draft edges, flow steps, and apply preconditions. 2026-05-17 audit/fix: Web Console now has one global Settings `Mode`; because it is a test bench, default Mode is real operator execution. Direct Node create, Edge create/update/delete, Node delete, L1.5 bucket ops, Source Board execute/import, Runtime trigger/message actions, and Plan HITL apply use `dry_run=false` / `operator_mode=true` in default Mode; switching to preview Mode keeps them on dry-run receipts. Remaining work is persistent overlay review and a less crowded full-screen L2-B renderer. | UI makes graph value legible; it does not become a decorative tangle; receipt wording must make preview vs true operator apply obvious. |
| WEB-016.8 | pending | CORE-013 promotion review | After typed schema, tests, and user review, decide which overlay/rewrite fields move from candidate queue into shared SSOT. | No `.cursor/memory/architecture/Interface/**` edit before approval. |
| WEB-016.9 | done | User-reviewed graph policy examples | 2026-05-16 A-G decisions are converted into concrete examples for Memory editor, future L2-B monitor, Graphiti import, Google Calendar import, Obsidian source packs, Arknights test pack, and IntentWorkspace overlays. Write ownership is recorded: L2-B apply, L1.5 admit, Graphiti durable edit, source immutable import, and operator-only DB surgery. | Recorded in the Web major roadmap, Memory/Graphiti/Runtime business files, and CORE-008/009/010/011/013 candidate notes. Persistent overlay/apply routes now wait on WEB-016.10 delta vocabulary and SSE prep. |
| WEB-016.10 | done | Delta vocabulary before SSE | Added Web-only `GraphDeltaOp`, `GraphDeltaEntityKind`, expanded `GraphDeltaEvent`, and `memory_runtime_delta_v1` event rows on `GET /api/memory/live-state/changes`. This keeps current polling but gives SSE a stable event shape. | Verified by Web route tests `48 passed`, `npm run typecheck`, `npm run build`, and `py_compile`. CORE-009 and CORE-013 now describe one compatible event shape; receipt stream stays separate. |
| WEB-016.11 | done | Live L2-B subgraph context route | Added `_tmp/l2b_subgraph_tools_workplan_20260517.md` before implementation, then implemented `POST /api/l2b/subgraphs/context`. The route reads live `get_l2b_graph()`, expands selected stable UUIDs with bounded depth `0..4`, returns live node/edge rows, WCC clusters touching selection, missing UUIDs, true-connection metadata, and a draft overlay. React Memory Canvas subgraph tool now exposes `Depth` and `Inspect context`. | Route is forced read-only (`dry_run=true`, `operator_mode=false`), exposes no RustWorkX index, preserves existing L2-B Graphiti/Ref metadata in node/edge rows, and does not query/write Graphiti or persist overlays. Verification: `py_compile`; Web route tests `84 passed`; React `npm run typecheck`; React `npm run build`; `git diff --check` only LF/CRLF warnings. |
| WEB-016.12 | done | Graphiti Episode/Entity fallback for import wait-state | Added a read-only Graphiti adapter fallback for cold/source-heavy partitions: after Graphiti search and fact-edge fallback return no rows, Web scans Graphiti nodes/Episodes by `name`, `summary`, and `content`, then returns preserved `graphiti_episode` / `graphiti_entity` rows. Import-plan keeps direct Episode/Entity hits as Episode/Entity refs instead of fake fact edges. Deployed the adapter to ECS app-monitor after the import wait window. | This supports "real imported Graphiti data must be findable" without converting Graphiti into L2-B ontology or writing FalkorDB. Verification: `py_compile`; full Web route tests `86 passed`; app-monitor tests `9 passed`; remote compile passed; `parrot-app-monitor` active PID `148490`. Live 7893 -> 8790 proof: `noble_etiquette` search returns real bundle hits (`etiquette` -> 3 hits/8 nodes) and import-plan over 2 hits returns `facts=2/entities=4/episodes=2`, L2-B preview `nodes=6/edges=2`, raw preservation true, direct L2-B write false. Caveat: ECS graph is currently 38 nodes/70 edges, not a full-book-sized import. |
| WEB-016.13 | done | Graphiti true-write provider fallback and full-book canary | Found the ECS write blocker: Graphiti's OpenAI-compatible client sends structured `json_schema` response format, while DeepSeek's documented `response_format.type` values are `text` / `json_object`. Added `GRAPHITI_DEEPSEEK_JSON_SCHEMA_ENABLED`; default requested DeepSeek now falls back to effective Gemini for Graphiti extraction with an explicit status reason. Added deterministic PG35123 importer for `Noble Etiquette/pg35123.txt`; deployed provider fix plus refreshed 8790 import-plan route/preview files to ECS app-monitor. | Verification: `py_compile`; full Web route tests `87 passed`; ECS status now reports `requested_provider=deepseek`, `provider=gemini`, `fallback_reason=deepseek_json_schema_response_format_disabled`. True writes through 8790 succeeded for two `noble_etiquette_pg35123_*` Episodes (~9k chars each). FalkorDB `noble_etiquette` rose to `158` nodes / `282` edges; search `Florence Hartley etiquette politeness` returns `11 hit(s), 24 node(s)`. 8790 import-plan now returns preview-only `graphiti_bundle_to_l2b_rustworkx_preview` with `l2b_nodes=6`, `l2b_edges=2`, RustWorkX `nodes=6/edges=6`, raw Graphiti preserved, and direct L2-B write false. |
| WEB-016.14 | done | Noble etiquette importer bugfix | Found and fixed importer chunking/Ref/exit-code bugs before full import. The script was skipping body Chapter I because it cropped to `INTRODUCTION.` but still used a hard-coded TOC line threshold; it also wrote local absolute `source_file` paths into Graphiti Episode bodies and could exit 0 on API `success=false`. | New default corrected prefix is `noble_etiquette_pg35123_v2_*`; Episode bodies use repo-relative `source_file: Noble Etiquette/pg35123.txt`; `--source-file-ref` supports future MCP/git-managed locators; API-level failure exits non-zero. Added `tests/test_scripts/test_import_noble_etiquette_to_graphiti.py`; focused tests `3 passed`, combined Web/script tests `89 passed`. Updated script deployed to ECS; remote dry-run confirms v2 prefix and stable source file ref. |
| WEB-016.15 | done | Ladies etiquette importer handoff check | Checked the user-provided `src/scripts/import_ladies_etiquette_book_to_graphiti.py` from ECS and copied it into the local workspace. Verified installed Graphiti supports the script's native `add_episode` parameters for prescribed entity/edge types, edge map, custom extraction instructions, saga, and `saga_previous_episode_uuid`. Added public `get_llm_clients` export in `graphiti_client.py` and kept `_build_llm_clients` as a compatibility alias. | Dry-run confirms 26 chapters, 76 text chunks/sub-episodes, no missing chapter numbers, Chapter XXI split into 11, and XIV/XIX/XXIV present. Fixed default-mode mismatch: plain `--apply` now writes the stated 76 text episodes; optional glossary episodes require `--with-glossary` and total 102. Remote dry-run verifies both modes. |
| WEB-016.16 | done | Codex ECS true-connection release workflow | Added `infra/ecs-release.ps1` as the post-audit release gate replacing stale `sync-castle.ps1` assumptions: Codex must audit local changes, stage only owned files, run tests, commit, push, then SSH-update ECS from git and restart all Parrot services before claiming true-connection validation. | The script verifies local `HEAD == origin/<branch>`, allows unrelated local dirty files only with `-AllowLocalDirty`, refuses ECS tracked/index drift by default, and with `-ForceResetWorktree` backs up tracked ECS drift before resetting tracked files. Untracked ECS runtime files are reported and preserved. It restarts `parrot-orchestrator`, `parrot-app-monitor`, `parrot-scheduler`, `parrot-maid`, `parrot-goslo-chat`, and `parrot-brain`, then smokes `7890`, `8790/api/graphiti/status`, and Redis/Falkor `6380`. Durable docs updated in `.cursor/memory/commit_guidelines.md` and `infra/systemd/README.md`. |

### WEB-017 Detail Checklist

This checklist is the Web Console coordination spine. It exists so the large
Web task order stays visible while Memory Graph, Runtime Flow, Graphiti/L1.5,
realtime, and graph-rewrite work continue across many rounds.

| Step | Status | Scope | Implementation Notes | Audit Signal |
|:--|:--|:--|:--|:--|
| WEB-017.1 | done | Mainline roadmap file and indexes | Created `backend_interface_map/web_console/web_console_major_roadmap_20260515.md` with owner/status/category/scope/source metadata. Indexed it in the Web Console README and root backend interface index. | The roadmap is discoverable from `web_console/README.md` and `backend_interface_map/README.md`; it is not a hidden temporary note. |
| WEB-017.2 | done | Fixed big-task order | Roadmap fixes the order as: RustWorkX/L2-B graph policy and examples -> incremental delta/realtime transport -> Memory Graph operation page cleanup -> source imports -> Runtime Flow collaboration page -> full-screen L2-B monitor -> SSOT promotion review. | Future implementation status should reference this order when explaining why a task starts now or waits. |
| WEB-017.3 | done | Mandatory Pre-Implementation Gate | Documented the per-round gate: relevant Parrot skills, current code, interface docs/candidate queue, UX pattern reference, and verification definition must be read before large code changes. | Any WEB-011/012/013/014/016 major slice that skips this gate should be treated as process drift. |
| WEB-017.4 | done | True-vs-fake implementation standard | Roadmap defines concrete completion standards for realtime, CRUD, source import, graph transform, Runtime collaboration, and HITL. | A feature is not "done" unless it has a route/state path, receipt, tests or smoke, docs, and no App DTO pollution if Web-only. |
| WEB-017.5 | done | Timing answer for RustWorkX graph transform research | Decided that the RustWorkX/L2-B "subconscious" graph-policy research and examples are the next architectural base. It should produce Web page examples before adding real apply routes. | WEB-016 stays the next backend/architecture priority after this roadmap unless the user explicitly redirects. |
| WEB-017.6 | done | Timing answer for realtime | Decided that SSE/WebSocket comes after stable delta vocabulary and before the full-screen L2-B monitor can be called real-time. SSE is the likely first read-only transport; WebSocket is reserved for bidirectional low-latency needs. | CORE-009 remains draft; no second incompatible realtime shape should be introduced. |
| WEB-017.7 | done | Timing answer for Memory/Runtime interaction redesign | Split quick bugfix polish from serious interaction redesign. Serious layout work starts from graph-policy and Runtime trace semantics, not from moving panels around. | WEB-011/012 UI work should cite the operation semantics it exposes. |
| WEB-017.8 | done | Timing answer for Google/Graphiti/Obsidian/Arknights imports | Existing source routes are preview-first, but not all are fake: Graphiti/Google/Obsidian import buttons have operator-gated apply routes, and 2026-05-17 clarified the same for direct L2-B Node/Edge and L1.5 bucket operations. Graphiti natural-language search on a lightweight 7893 Web BFF still needs either local Graphiti deps or `PARROT_WEB_CONSOLE_GRAPHITI_URL` pointing at the app-monitor Graphiti API (for example local/ECS 8790); without that env, the BFF intentionally reports local dependency missing instead of silently reaching a remote service. Arknights real import needs approved compact summaries/facts/source URLs and temporal chapter/state metadata. | WEB-014 remains in progress; do not label source import "complete" until preview, apply, receipt, tests, visible target subgraph, and correct runtime target (local BFF vs ECS/app-monitor) are all obvious in the UI/docs. |
| WEB-017.9 | done | User decision questionnaire | Roadmap records the user-reviewed answers for page split, right-side toolbar, editor vs monitor split, graph policy, SSE-first realtime, source imports, Runtime Flow/HITL, visual language, and operator safety. | The questionnaire is no longer pending; future design should cite the resolved decisions unless the user changes policy. |
| WEB-017.10 | done | Next implementation round kickoff | WEB-016 next slice is started: user decisions are recorded, RustWorkX/L2-B/attention/Graphiti/React-Force-Graph sources were reread, official docs were linked in the roadmap, and concrete graph-policy examples/write-ownership notes were added to Memory/Graphiti/Runtime business files plus candidate queue. | Persistent overlay/apply routes still wait on WEB-016.9 / WEB-016.10; do not skip directly to SSE or source-import apply. |
| WEB-017.11 | in_progress | Realtime design gate | After WEB-016 delta/event terms are stable, design `changed-since -> SSE` read stream for Memory and Runtime with reconnect/stale/fresh behavior and route tests. Memory SSE uses `memory_runtime_delta_v1`; Runtime SSE uses `runtime_flow_delta_v1`; React connects to both through EventSource with polling fallback. | Browser smoke after local service restart shows Memory `SSE` and Runtime `实时 / SSE · LIVE`, both with zero console errors. Promote only as Web-only until App confirms a shared stream consumer. Remaining: separate receipt stream design, if/when operator receipts need realtime delivery. |
| WEB-017.12 | pending | Big audit cadence | Every three major rounds reconcile TODO, Web README, root index, module business docs, candidate queue, route ledger, secret scan, and product drift. | Prevents stale documents and "done" claims that no longer match the UI/API. |

### WEB-012 Detail Checklist

This checklist tracks the React Runtime Flow / collaboration-flow upgrade. It
must stay visible here because the task spans frontend, BFF read models, Plan
dispatch, Scheduler/Nanobot result reporting, HITL, docs, and later core review.

| Step | Status | Scope | Implementation Notes | Audit Signal |
|:--|:--|:--|:--|:--|
| WEB-012.1 | done | Research and route planning | Read Parrot bridge, py-trees, Nanobot, Bus orchestration, and DSG L2-B skill constraints. Web research anchors: React Flow subflows/state/reconnect, Cytoscape.js compound/layout, OpenTelemetry traces/span events/span links, LangSmith agent traces, and LangChain/LangGraph HITL approve/edit/reject patterns. | Durable conclusions recorded in `web_console/_tmp/runtime_flow_memory_upgrade_research_20260513.md` and promoted into the Runtime/Memory business files. |
| WEB-012.2 | done | Temporary ledger and doc hygiene | Created only one temporary archive under `web_console/_tmp/` and indexed it from `web_console/README.md`; no scattered docs. | Root and lane README indexes still point to the same active Web files. |
| WEB-012.3 | done | React + Vite shell | Added `web/console_app/` with Vite 2.9-compatible React 18, TypeScript, API client, zh/en setting, two workspaces, and receipt rail. Build output goes to `web/console_dist/`. | `npm run typecheck` and `npm run build` pass in `web/console_app/`. |
| WEB-012.4 | done | Static hosting switch | `src/parrot/web_console/server.py` now prefers `web/console_dist/index.html` when present and falls back to legacy `web/console/`. | Web Console keeps the same local URL/port while the vanilla console remains reference/transition. |
| WEB-012.5 | done | Runtime Flow read model V1 | Added Web-only `src/parrot/web_console/runtime_flow.py` and routes for `/api/runtime/flow` and `/api/runtime/flow/changes?since=...`. V1 returns lanes, graph nodes/edges, events, pending gates, and full snapshot only when the stable content signature changes. The review pass now sorts signature input, normalizes duplicate/dangling graph edges, includes event `source`/`writer` in diff signatures, and carries CORE-010-style `trace_id` / `payload_ref` hints without ratifying a shared DTO. | Route tests assert shape, secret non-leak, unique/valid graph edges, Plan trace hints, and no-op `changed=false`. Still polling/diff, not SSE/WebSocket. |
| WEB-012.6 | done | HITL V1 Web BFF | Added `/api/runtime/hitl/pending`, `/api/runtime/hitl/draft-decision`, and `/api/runtime/hitl/apply-decision`. Decisions support approve, approve_and_start, reject, revise, cancel, and resume. Default remains dry-run/operator-safe. Pending gate buttons now reuse the same state-aware policy as draft/apply validation. Draft rejects unknown Plan ids, invalid Plan-state/action pairs, and non-Plan gate targets such as trigger/message until those target state machines are designed. | Tests cover pending gates, button policy consistency, draft receipts, missing-plan draft rejection, non-Plan target rejection, state-aware draft rejection, dry-run apply, and Web-only audit fields. |
| WEB-012.7 | done | Plan -> Scheduler/Nanobot dispatch capability | `PlanRegistry.start_executing()` can dispatch ready steps through an injectable dispatch function. Dispatched steps carry `plan_id`, `step_id`, and result channel metadata. Dispatch failure now settles the step/Plan to failed, unsupported tool types fail fast instead of hanging in `DISPATCHED`, and empty plans complete instead of staying in executing. | `tests/test_brain/test_plan_lifecycle.py` verifies ready-step dispatch, task ids, dependencies, cascade dispatch, dispatch-failure failure transition, unsupported-tool failure transition, and empty-plan completion. |
| WEB-012.8 | done | Nanobot result/timeout return path | Scheduler Nanobot result listener and timeout path report back to the Plan registry when task metadata includes `plan_id` and `step_id`. | Focused tests pass; cross-process durable trace DTO remains a CORE-010/011 candidate, not a ratified SSOT field. |
| WEB-012.9 | done | Runtime Flow React workspace V1 | React Flow swimlane/DAG renders Intent, Plan/HITL, Blackboard, IntentWorkspace, Scheduler, Nanobot, Message/Trigger lanes with event tape, manual trigger palette, HITL gate cards, detail JSON, and receipt timeline. 2026-05-14 UI passes fixed zh/en copy rendering, localized the action group titles, shows the backend refresh interval in the live pill, positions Runtime nodes by lane-local row, renders HITL buttons from backend `options`, exposes scene/roleplay/calendar/scheduler trigger presets, and shows registered trigger chips from `/api/dsg/triggers/catalog`. Runtime action/HITL failures now produce local failure receipts instead of unhandled browser errors. | Browser smoke: Runtime page opens, trigger palette is visible, registered trigger chips include `event_driven` and `on_demand`, LLM trigger draft returns a receipt, zh/en text is readable, localized group titles show `消息/运行/模式`, and console errors stay at zero. Visual polish remains future work. |
| WEB-012.10 | done | Memory Graph React workspace V1 | React Flow L2-B canvas, L1.5 bucket board, node/edge dry-run drafts, preview/detail drawer, receipt timeline, and direct drag/connect edge draft preview exist in React. | Browser smoke: React Memory page loads from `web/console_dist` on port `7893`, Chinese copy renders, node draft receipts work, and console errors stay at zero. Selected edge reconnect/retarget, graph health metrics, and Evidence Board remain future WEB-011 slices. |
| WEB-012.11 | in_progress | Runtime event transport upgrade | Added Web-only Runtime Flow SSE at `GET /api/runtime/flow/stream`, reusing the typed `runtime_flow_delta_v1` changed-since envelope. React Runtime connects with EventSource only while the Runtime page is active and falls back to polling on disconnect/unavailable. | Route test covers the SSE event schema; browser smoke after restart shows Runtime `实时 / SSE · LIVE` and zero console errors. Do not promote CORE-009/010 unless App lane confirms a shared stream consumer. Remaining: later receipt stream if operator receipts need realtime delivery. |
| WEB-012.12 | pending | Human-in-the-loop data model review | Decide whether `RuntimeHumanGate` becomes shared core after Web V1 proves useful. | Until confirmed, HITL remains Web BFF/operator-safe and does not enter Unity/App DTOs. |
| WEB-012.13 | done | Browser and UI audit | Smoked `http://127.0.0.1:7893/` after restarting the Web Console service on the React dist. Checked console errors, zh/en toggle, Memory/Runtime navigation, manual LLM trigger dry-run, empty-data states, no obvious secret rendering, HTTP no-op changed-since, review-pass dangling-edge cleanup, L1.5 pool health rendering, Obsidian draft card receipts, trigger-catalog chips, and localized Runtime action group labels. | Latest slice: frontend typecheck/build pass, Web route tests report `17 passed`, Memory browser smoke clicked Obsidian daily draft and ref missing-UUID draft with expected receipts and zero console errors. Repeat after every major React UI slice. |
| WEB-012.14 | done | Big audit | Reconciled TODO status, route matrix, business-interface ledger, candidate queue, implementation tests, product drift, secret exposure, and App DTO boundaries for the React checkpoint. Indexed `web/console_dist/` hosting and `GET /api/memory/blackboard/activity` in the Web README. | Current slice is compliant enough to continue Web Runtime/Memory work. Do not promote CORE-010/011 or call the whole migration stable until WEB-012.17/012.18 and the remaining WEB-011 interaction slices are reviewed. |
| WEB-012.15 | done | Runtime Flow typed schema / DTO preparation | Added Web-only `parrot.web_console.runtime_flow_models` dataclasses for Runtime Flow nodes, edges, events, snapshots, changed-since envelopes, HITL gates, and receipts while preserving the current route wire shape. Runtime Flow audit now advertises the typed schema module. | `48 passed`; route smoke confirms `/api/runtime/flow` returns `typed_schema=parrot.web_console.runtime_flow_models` and no-op changed-since stays `changed=false`. No Unity/App DTOs changed. |
| WEB-012.16 | done | Plan HITL typed receipt and action policy | HITL receipts now serialize through the typed receipt model and expose `core_candidate=CORE-011` when relevant. Pending/draft/apply still share one state-aware action policy; real apply remains dry-run/operator gated; non-Plan targets keep `unsupported_hitl_target`. | Tests cover typed model serialization, pending button policy consistency, missing-plan rejection, invalid-state rejection, non-Plan target rejection, and dry-run apply. Promote only the Plan gate subset as CORE-011 candidate after user review. |
| WEB-012.17 | pending | Runtime event ledger / cross-process trace durability | Tighten Plan -> Scheduler -> Nanobot -> Plan result/timeout trace fields without adding a new global bus protocol. | Shared trace gaps go to CORE-010 candidate queue only. |
| WEB-012.18 | pending | CORE-010/011 SSOT promotion review | After typed schema and tests pass, review whether Web-first Runtime Flow and Plan HITL are ready for candidate SSOT wording. | No `.cursor/memory/architecture/Interface/**` edit until reviewed in this Chat. |
| WEB-012.19 | pending | Runtime Flow ComfyUI-like workflow lane | Expand Runtime Flow into a larger collaboration/workflow page: manual Plan import, manual Nanobot task dispatch, destination choice for results (`view_only`, `return_to_goslo`, `return_to_app`, or future workspace return), message send/receive, HITL gate, and simplified node-workflow wiring. | This page owns whole collaboration state; it must not become an L2-B graph editor or detailed Ref-file manager. |
| WEB-012.20 | pending | Nanobot task destination and receipt model | Audit existing Scheduler/Nanobot metadata, then add a Web-only draft/receipt model for manually sending tasks and choosing where the result returns. Shared durable trace gaps go to CORE-010; HITL/action gates go to CORE-011 only after state-machine review. | No browser credentials, no raw Redis admin, and no App DTO promotion without review. |

## Shared Blockers

| ID | Status | Blocker | Owner | Linked lane item | Resolution |
|:--|:--|:--|:--|:--|:--|
| _none yet_ | | | | | Add only real cross-lane blockers. |

## Core Candidates Index

Core candidates live in:

`codex_workspace/design_workspace/backend_interface_map/core_interface_candidate_queue_20260513.md`

When approved, move the contract into `.cursor/memory/architecture/Interface/**`
and link the ratified doc back here.
